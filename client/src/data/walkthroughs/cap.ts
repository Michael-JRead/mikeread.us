import type { WalkthroughDoc } from "./types";

export const cap: WalkthroughDoc = {
  slug: "cap",
  name: "Cap",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Easy",
  retired: "Oct 2021",
  ip: "10.10.10.245",
  tags: [
    "IDOR",
    "PCAP analysis",
    "Wireshark",
    "FTP cleartext creds",
    "credential reuse",
    "Linux capabilities",
    "cap_setuid",
    "Python privesc",
  ],
  lede: "A network-security dashboard that captures packets for you also lets you read everyone else's captures. One decremented URL leaks an admin's PCAP, the PCAP leaks a cleartext FTP password, and a stray capability on the Python interpreter turns that foothold into root.",
  summary: "An IDOR on a Flask/Gunicorn security dashboard leaks a PCAP with cleartext FTP creds for nathan, reused over SSH, then cap_setuid on /usr/bin/python3.8 gives root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“The packets don’t lie — and neither does a capture you were never meant to see.”" },
        { kind: "p", text: "**Cap** is an Easy-rated Linux box that reads like a lesson in trusting user input and least privilege. The front door is a **Security Dashboard** — a Flask app behind Gunicorn that captures live network traffic on demand — and every stage of the chain is a small, realistic mistake rather than a headline CVE." },
        { kind: "p", text: "The dashboard hands each capture a sequential, globally-shared id in the URL. Decrement it and you are reading someone else's traffic; that traffic happens to be an unencrypted FTP login. From there it is straight credential reuse into SSH, and a single misplaced Linux capability to finish. Here is the full route before we start:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap → FTP (21), SSH (22), Gunicorn web (80)",
            "**web** — Security Dashboard, `/capture` → `/data/<id>`",
            "**IDOR** — walk the id down to `/data/0`",
            "**download** — pull `0.pcap` from `/download/0`",
            "**PCAP** — follow the FTP stream in Wireshark",
            "**creds** — `nathan : Buck3tH4TF0RM3!` in cleartext",
            "**ssh nathan** — credential reuse → `user.txt`",
            "**getcap** — `cap_setuid` on `/usr/bin/python3.8`",
            "**os.setuid(0)** — root shell → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "Theme", text: "The box is all about *packet capture* — the name **Cap** does double duty, nodding both at the PCAP that leaks the FTP creds and at the Linux **cap**ability that hands us root. Two very different meanings of the same three letters, and both are the whole box." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "We start with a full TCP sweep to catch anything off the beaten path, then a targeted service/version scan on what comes back." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 10.10.10.245",
            "$ nmap -sC -sV -p 21,22,80 10.10.10.245",
            "PORT   STATE SERVICE VERSION",
            "21/tcp open  ftp     vsftpd 3.0.3",
            "22/tcp open  ssh     OpenSSH 8.2p1 Ubuntu 4ubuntu0.2 (Ubuntu Linux; protocol 2.0)",
            "80/tcp open  http    Gunicorn 20.0.4",
            "|_http-server-header: gunicorn/20.0.4",
            "|_http-title: Security Dashboard",
          ],
          highlight: [3, 5],
        },
        { kind: "p", text: "Three ports, and the shape of the box is already visible: an **FTP** service, **SSH**, and a security-themed web app served by **Gunicorn**. Gunicorn is a Python WSGI server, so port 80 is a Python (Flask) application — a detail worth banking, because the eventual privesc lands on the Python interpreter itself." },
        { kind: "callout", variant: "warn", title: "Don't chase the vsftpd CVE", text: "`vsftpd 3.0.3` is *not* the backdoored 2.3.4 build, and anonymous login is disabled. FTP is a decoy here — it only becomes useful once we have recovered credentials for it. Likewise SSH is current and unremarkable. The entire foothold lives in the web app on port 80." },
      ],
    },
    {
      id: "dashboard",
      num: "02",
      title: "The Security Dashboard",
      blocks: [
        { kind: "p", text: "Port 80 is a **network security dashboard** — a slick, single-purpose app that reports the host's IP configuration, network status, and interface details. It is clearly a Python/Flask application (the Gunicorn server header confirmed as much), and it reads as an internal tooling page rather than a public site. Most of the tabs are static readouts, but one is interactive: **Security Snapshot**." },
        { kind: "p", text: "Nothing on the dashboard demands authentication, and content discovery is largely wasted effort — the app is small and the interesting surface is right in front of us. Rather than fuzz for hidden paths, we click through the app and watch how it addresses its own resources." },
        { kind: "callout", variant: "tip", title: "Skip the dirbuster", text: "There is no need to grind `gobuster` on Cap. The whole box hinges on how the app numbers one particular object, not on a hidden endpoint. When an application is this small, reading its URLs beats brute-forcing them." },
      ],
    },
    {
      id: "snapshot",
      num: "03",
      title: "The Security Snapshot feature",
      blocks: [
        { kind: "p", text: "**Security Snapshot** is the one feature with real behaviour: clicking it makes the server run a live **packet capture** for a few seconds, then redirects us to a results page that tallies the captured packets by protocol and offers a **Download** link to the raw PCAP." },
        {
          kind: "code",
          lang: "text",
          label: "app map",
          lines: [
            "http://10.10.10.245/               # dashboard home",
            "http://10.10.10.245/capture        # triggers a live capture, 302 -> /data/<id>",
            "http://10.10.10.245/data/<id>      # results page: per-protocol counts + Download",
            "http://10.10.10.245/download/<id>  # serves the raw .pcap for that snapshot",
          ],
        },
        { kind: "p", text: "Triggering `/capture` bounces us to something like `/data/1`, with its raw PCAP available at `/download/1`. Run it and the capture is mostly our own benign traffic — a handful of packets, nothing sensitive. The important observation is not the contents but the **URL**: that `id` is a small, sequential integer, and the app is trusting us with it." },
        { kind: "callout", variant: "note", title: "A fresh capture is a dead end", text: "Your own snapshot only records a few seconds of your own traffic, so it is almost always empty or benign. The value is never in the capture *you* trigger — it is in the ones you were not meant to see, reached by editing that id." },
      ],
    },
    {
      id: "idor",
      num: "04",
      title: "IDOR — reading someone else's capture",
      blocks: [
        { kind: "p", text: "The snapshot id is not scoped to our session — it is a **globally shared, sequentially assigned** identifier. Nothing ties `/data/1` to the browser that created it, and no authentication guards lower ids. So we simply walk the number down. The most valuable target is **`/data/0`**: the very first capture ever taken on the box, made long before any of our own." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# our own fresh capture is near-empty; walk the id down to 0 instead",
            "$ curl -s http://10.10.10.245/data/0 | grep -i packets",
            "# /data/0 reports non-zero TCP/other counts — this one has real traffic",
            "",
            "# pull the raw capture directly from the download endpoint",
            "$ curl http://10.10.10.245/download/0 -o 0.pcap",
            "$ file 0.pcap",
            "0.pcap: pcap capture file, ... (Ethernet, capture length 262144)",
          ],
          highlight: [5],
        },
        { kind: "p", text: "This is a textbook **Insecure Direct Object Reference (IDOR)**: the server exposes an internal object identifier directly in the URL and enforces no ownership check on it. Where our own snapshot was empty, `0.pcap` is full of an earlier user's traffic — including, crucially, an FTP session." },
        { kind: "callout", variant: "warn", title: "id 0 is the money file", text: "Higher ids are other players' throwaway captures; `/data/0` predates them all and holds the original FTP login the box author left behind. If `0` looks empty on a heavily-used instance, the capture may have been reset — but the flaw (unauthenticated access to *any* id) is the point, so the low ids are always where the value lives." },
      ],
    },
    {
      id: "pcap",
      num: "05",
      title: "Cleartext credentials in the PCAP",
      blocks: [
        { kind: "epigraph", text: "“FTP over the wire is a password read aloud in a crowded room.”" },
        { kind: "p", text: "FTP is a plaintext protocol — the client sends `USER` and `PASS` in the clear on the control channel, with no encryption. Anyone holding a capture of that exchange holds the credentials. We open `0.pcap` in Wireshark and follow the FTP conversation; from the command line, `tshark` does the same in one line." },
        {
          kind: "code",
          lang: "bash",
          label: "wireshark / tshark",
          lines: [
            "$ wireshark 0.pcap",
            "# in Wireshark: apply display filter 'ftp',",
            "# or right-click a packet -> Follow -> TCP Stream",
            "",
            "# from the CLI, filter the FTP control channel directly:",
            "$ tshark -r 0.pcap -Y ftp",
            "220 (vsFTPd 3.0.3)",
            "USER nathan",
            "331 Please specify the password.",
            "PASS Buck3tH4TF0RM3!",
            "230 Login successful.",
          ],
          highlight: [7, 9],
        },
        { kind: "p", text: "There it is, in plaintext: username **nathan** and password **Buck3tH4TF0RM3!**, followed by `230 Login successful.` These are real, working credentials for the box — and on Cap they are reused well beyond FTP." },
        { kind: "callout", variant: "warn", title: "Copy the password exactly", text: "`Buck3tH4TF0RM3!` is leetspeak with symbols — a capital B, threes for e's, and a trailing exclamation mark. Copy it character-for-character, including that `!`. The creds are in the FTP **control** channel, so filter on `ftp`, not on a data-transfer stream." },
        { kind: "callout", variant: "key", title: "Recovered", text: "FTP credentials `nathan : Buck3tH4TF0RM3!`, pulled in cleartext from `0.pcap`. We will reuse this exact pair to log in over SSH." },
      ],
    },
    {
      id: "user",
      num: "06",
      title: "Foothold as nathan · user.txt",
      blocks: [
        { kind: "p", text: "The credentials authenticate against FTP, but FTP only gives us a file listing. The real prize is **credential reuse**: the same `nathan : Buck3tH4TF0RM3!` pair works over **SSH**, dropping us into a full interactive shell. This is the intended pivot — one leaked secret, reused across two services." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ssh nathan@10.10.10.245",
            "# password: Buck3tH4TF0RM3!",
            "nathan@cap:~$ id",
            "uid=1001(nathan) gid=1001(nathan) groups=1001(nathan)",
            "nathan@cap:~$ cat /home/nathan/user.txt",
            "b7a9f0c2····························",
          ],
          highlight: [5],
        },
        { kind: "p", text: "We are `nathan`, uid 1001 — an ordinary user with no group memberships of note. A quick `sudo -l` confirms nathan has no sudo rights, so the escalation is not going to come from the sudoers file. Time to enumerate the host for something quieter." },
        { kind: "callout", variant: "note", title: "Why reuse works here", text: "Password reuse across FTP and SSH is exactly what turns a passive network leak into an active foothold. The capture never contained an SSH session — but people reuse passwords, and the box counts on it. Always try recovered creds against every service that's listening." },
      ],
    },
    {
      id: "capabilities",
      num: "07",
      title: "Privilege escalation — hunting capabilities",
      blocks: [
        { kind: "p", text: "With no sudo access, we look at the next most common Linux misconfiguration after SUID binaries: **file capabilities**. Capabilities slice root's powers into discrete units and attach them to individual executables, so a binary can hold one specific privilege without being SUID-root. We enumerate the whole filesystem with `getcap`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "nathan@cap:~$ getcap -r / 2>/dev/null",
            "/usr/bin/python3.8 = cap_setuid,cap_net_bind_service+eip",
            "/usr/bin/ping = cap_net_raw+ep",
            "...",
          ],
          highlight: [1],
        },
        { kind: "p", text: "The `ping` line is normal. The one that matters is **`/usr/bin/python3.8 = cap_setuid,cap_net_bind_service+eip`**. That capability set was almost certainly granted so the Python dashboard could bind low ports and sniff packets — but `cap_net_bind_service` came bundled with **`cap_setuid`**, and `cap_setuid` is the whole game." },
        { kind: "callout", variant: "warn", title: "cap_setuid on an interpreter == root", text: "`cap_setuid` lets a process call `setuid()` to *any* UID — including 0 — with no password and no SUID bit. Handing that to a general-purpose interpreter like Python is functionally identical to handing out root: we can simply ask Python to become uid 0 and then run a shell. This is the exact technique GTFOBins documents for Python `cap_setuid`." },
      ],
    },
    {
      id: "root",
      num: "08",
      title: "Becoming root · root.txt",
      blocks: [
        { kind: "p", text: "The capability only grants the *ability* to change UID — it does not run our code as root automatically. So we must explicitly call `os.setuid(0)` first, dropping our effective UID to 0, and only then spawn a shell. We invoke the exact interpreter that carries the capability, by full path." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "nathan@cap:~$ /usr/bin/python3.8 -c 'import os; os.setuid(0); os.system(\"/bin/bash\")'",
            "root@cap:~# id",
            "uid=0(root) gid=1001(nathan) groups=1001(nathan)",
            "root@cap:~# cat /root/root.txt",
            "3f4c1e88····························",
          ],
          highlight: [2, 4],
        },
        { kind: "p", text: "`uid=0(root)` — we own the box. The group id still reads `nathan` because we never changed it, but the effective UID of 0 is all that matters for reading `/root/root.txt`. Capture captured." },
        { kind: "callout", variant: "warn", title: "Use the full path, and call setuid(0) yourself", text: "Run `/usr/bin/python3.8` specifically — a different `python` on the `PATH` may not carry the capability and will leave you as nathan. And remember the capability is only permission, not action: without the explicit `os.setuid(0)`, Python runs as your own UID no matter how it's launched." },
      ],
    },
    {
      id: "defense",
      num: "09",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Enforce authorization on every object reference.** The dashboard trusted a sequential, guessable `id` with no ownership check. Scope identifiers to the authenticated session/owner and verify access on every `/data/<id>` and `/download/<id>` request — or use unguessable random ids as defence-in-depth.",
            "**Never send credentials over cleartext protocols.** FTP put `nathan`'s password on the wire in plaintext, and a captured PCAP handed it straight over. Use FTPS or SFTP so an intercepted capture yields nothing usable.",
            "**Don't reuse passwords across services.** One leaked FTP secret became full SSH access purely through reuse. Distinct credentials per service would have contained the breach to FTP alone.",
            "**Grant Linux capabilities with least privilege.** `cap_setuid` on `/usr/bin/python3.8` is equivalent to giving out root. If the app only needs to bind low ports, grant `cap_net_bind_service` alone — never bundle `cap_setuid` onto a general-purpose interpreter.",
            "**Treat packet captures as sensitive data.** Capture tooling and stored PCAPs routinely contain secrets. Restrict who can trigger captures, who can read the results, and how long they persist.",
            "**Audit capabilities and SUID/SGID binaries routinely.** A scheduled `getcap -r /` and SUID sweep as part of host hardening would have surfaced this misconfiguration long before an attacker did.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "service enumeration · IDOR discovery & exploitation · PCAP analysis with Wireshark/tshark · cleartext credential recovery · credential reuse · Linux capability enumeration (`getcap`) · `cap_setuid` privilege escalation." },
      ],
    },
  ],
};
